---
title: "Beyond Single-Model Injection: A Threat Model and Defense Architecture for Prompt Injection in Multi-Agent Systems"
title_zh: 超越单模型注入：多智能体系统中提示注入的威胁模型与防御架构
authors: "Rudrendu Kumar Paul, Sourav Nandy"
date: 2026-09-19
pdf: "https://arxiv.org/pdf/2609.22949v1"
tags: ["query:ai-agents"]
score: 10.0
evidence: 多智能体系统提示注入的威胁模型与防御架构
tldr: "现有提示注入研究多聚焦单模型聊天场景，忽视多智能体系统中消息传递、共享工具与信任传播带来的放大风险。本文构建覆盖四类14种攻击向量的威胁模型，并在6智能体生产级系统中测试。结果显示，即使有系统提示级防护，67%智能体仍存在越权漏洞，工具输出间接注入成功率达43%。四项架构防御将整体注入成功率从31.2%降至4.2%。"
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.22949v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 699, \"height\": 798, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.22949v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 865, \"height\": 369, \"label\": \"Table\"}]"
motivation: 单模型提示注入研究忽略多智能体消息传递、共享工具和信任传播带来的新攻击面，需系统化威胁建模。
method: 构建四类14种攻击向量的威胁模型，在6智能体生产级系统测试，并评估四项架构防御。
result: "系统提示级防护下67%智能体至少存在一种越权，工具输出间接注入成功43%，整体注入成功率31.2%。"
conclusion: "消息签名、边界净化、权限隔离和通信异常检测将注入成功率降至4.2%，为多智能体安全提供架构防御路径。"
---

## 摘要
现有提示注入研究主要关注单模型聊天机器人场景，其中攻击者通过精心构造的输入操纵单个 LLM。多智能体系统通过三种单模型设置中不存在的机制放大了这一威胁：智能体间消息传递创建了边界防御无法察觉的注入通道，共享工具访问使权限提升能够跨越智能体边界，信任传播则允许被攻陷的智能体影响上游编排器。我们构建了一个威胁模型，列举了四类共 14 种攻击向量：通过用户输入的直接注入（3 种向量）、通过工具输出的间接注入（4 种向量）、通过消息传递的智能体间注入（4 种向量），以及通过编排器操纵的级联注入（3 种向量）。针对一个由 6 个智能体组成的、具有生产代表性的系统测试全部 14 种向量，我们发现，即使有系统提示级别的防护栏，仍有 67% 的智能体易受至少一种作用域违规影响，且通过工具输出的间接注入在 43% 的尝试中成功。四种架构防御将总体注入成功率从 31.2% 降至 4.2%：带来源追踪的消息签名（智能体间注入下降 91%）、智能体边界处的输入/输出净化（间接注入下降 78%）、按智能体角色限定权限的工具访问（完全消除权限提升），以及智能体间通信模式异常检测（捕获 84% 的级联尝试）。

## Abstract
Existing prompt injection research focuses on single-model chatbot scenarios, where an attacker manipulates one LLM through crafted input. Multi-agent systems amplify this threat through three mechanisms absent from single-model settings: inter-agent message passing creates injection channels invisible to perimeter defenses, shared tool access enables privilege escalation across agent boundaries, and trust propagation allows a compromised agent to influence upstream orchestrators. We construct a threat model enumerating 14 attack vectors across four categories: direct injection via user input (3 vectors), indirect injection via tool outputs (4 vectors), inter-agent injection via message passing (4 vectors), and cascading injection through orchestrator manipulation (3 vectors). Testing all 14 vectors against a 6-agent production-representative system, we find that 67% of agents are vulnerable to at least one scope violation even with system-prompt-level guardrails, and indirect injection via tool outputs succeeds in 43% of attempts. Four architectural defenses reduce overall injection success from 31.2% to 4.2%: message signing with provenance tracking (inter-agent injection down 91%), input/output sanitization at agent boundaries (indirect injection down 78%), privilege-scoped tool access per agent role (privilege escalation eliminated entirely), and anomaly detection on inter-agent communication patterns (84% of cascading attempts caught).

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义

- **研究动机**：现有提示注入研究主要针对“单模型聊天机器人”场景，即攻击者通过构造输入操纵一个 LLM。但多智能体系统引入了三类单模型不存在或弱得多的放大机制：
  - **智能体间消息传递**：形成边界防御无法观察的注入通道。
  - **共享工具访问**：使权限提升可跨智能体边界发生。
  - **信任传播**：被攻陷智能体可影响上游编排器，导致局部污染扩散为系统级失败。
- **核心问题**：多智能体 LLM 架构中的提示注入不是单模型注入的简单叠加，而是由架构属性产生的新型攻击面；仅靠系统提示级防护栏不足，需要架构层防御。
- **整体含义**：论文构建了多智能体提示注入的威胁模型，给出 14 种攻击向量的实测成功率，并提出四类架构防御，将整体注入成功率从 **31.2%** 降至 **4.2%**，为 MCP/A2A 等规模化多智能体部署提供安全基线。

## 2. 方法论

### 2.1 核心思想

- 将多智能体系统形式化为 \(S=\{a_1,\dots,a_n\}\)，每个智能体 \(a_i\) 具有角色 \(r_i\)、允许工具集合 \(T_i\)、信任边界 \(B_i\)。
- 攻击者不控制智能体本身，但控制至少一个智能体处理的内容，如用户输入、检索文档、API 响应、数据库记录。
- 攻击目标：让至少一个智能体执行越权行为，包括数据外泄、绕过合规检查、权限提升、污染下游输出。
- 防御思路：不只在提示层过滤，而是在**消息层、边界层、工具权限层、通信图异常层**部署架构控制。

### 2.2 威胁模型：四类
