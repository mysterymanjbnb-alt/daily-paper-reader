---
title: "Typed Federated Artifacts for the Agentic Web:Sharing Tool-Routing Knowledge Across Frozen,Heterogeneous LLM Agents"
title_zh: 面向代理式 Web 的类型化联邦工件：在冻结、异构的 LLM 智能体之间共享工具路由知识
authors: "Abhijit Chakraborty, Ni Trieu, Vivek Gupta"
date: 2026-09-06
pdf: "https://arxiv.org/pdf/2609.06815v1"
tags: ["query:ai-agents"]
score: 8.0
evidence: 通过联邦工件在冻结异构LLM智能体间共享工具路由知识
tldr: "面向智能体Web，冻结异构LLM智能体需在保护私有历史的前提下共享工具路由知识，但扁平文本难以区分噪声、合并规则与文档，权重和适配器也无法跨平台迁移。本文提出类型化联邦工件，以模式校验对象承载逐字段隐私、争议解决与跨模型迁移，并实例化为SYNAPSE1工具路由知识。在StableToolBench上，联邦知识仅比集中式低1.1分，每客户端每轮20MB JSON；类型化字段展示比扁平字符串在干净数据和60%矛盾注入下分别高8.5和7.4分，τ-bench retail每步工具调用准确率至少提升6.7分。但TF-IDF基线在带标签查询池上远超LLM路由，揭示基准泄漏，说明该设置尚无法衡量真正无标签工具路由。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.06815v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1449, \"height\": 508, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.06815v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1168, \"height\": 699, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.06815v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1431, \"height\": 342, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.06815v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1106, \"height\": 244, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.06815v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1220, \"height\": 443, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.06815v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1447, \"height\": 234, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.06815v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1059, \"height\": 236, \"label\": \"Table\"}]"
motivation: 冻结异构LLM智能体需跨平台共享工具路由经验，但扁平文本、权重或适配器都无法安全迁移并区分噪声、合并规则与文档。
method: 提出类型化联邦工件：模式校验的对象，含逐字段隐私、争议解决与跨模型迁移字段，并实例化为SYNAPSE1工具路由知识。
result: StableToolBench上联邦路由仅比集中式低1.1分，每客户端每轮20MB；类型化字段比扁平字符串高8.5分，矛盾注入下高7.4分，τ-bench零售每步准确率至少+6.7。
conclusion: 类型化展示与合并不可分割；冲突策略不敏感。但TF-IDF在带标签池上碾压LLM路由，暴露基准泄漏，无法衡量真正无标签工具路由。
---

## 摘要
一个开放、互联的 Web 将允许智能体运行来自多个供应商的冻结模型，保持自身历史私密，并相互教授调用哪个工具以及何时调用。扁平文本（提示、示例池）使协议难以区分噪声统计、合并规则和文档。权重和适配器无法在平台之间迁移这类知识。我们建议共享类型化联邦工件——即经过模式验证、具有明确定义字段的对象，用于逐字段隐私（本文描述，但已度量）、争议解决和跨模型迁移——并将其实例化为 SYNAPSE1，一种通用工具路由知识。在删除 192 个垃圾条目和 1,916 个与测试查询重复或几乎重复的训练项后，在 StableToolBench（3,180 个工具）上，联邦汇编每轮每客户端 20 MB JSON 的路由表现与集中式汇编相差在 1.1 个百分点以内。同样的经验若以类型化字段而非单个扁平字符串合并并展示给路由器，在干净数据上价值 8.5 个点，在注入 60% 矛盾时价值 7.4 个点。交叉合并与渲染表明两半不可分割（以扁平形式展示的类型化合并是最差分支），而三种冲突策略无法区分，因此推动这项工作的冲突日志并不是瓶颈。在 τ-bench retail 上，每个汇编分支都将 GPT-4o 智能体的每步工具调用准确率至少提升 6.7 个百分点，这归因于格式而非联邦经验。论文以两项警示性发现作结：在带主题标签的数学代理任务和 StableToolBench 上，基于相同标注经验训练的 TF-IDF 分类器击败了所有 LLM 路由分支（分别高出 48 和 26 个点，主要来自检索召回率），因为基准的池中在我们过滤之前，为每个据称未见过的工具都保存了带标签的查询，并且逐字保存了每个测试查询。它无法衡量对无标签工具的路由，而这正是路由存在的目的。

## Abstract
An open, networked web will allow agents to run frozen models from multiple vendors, keep their history private, and teach each other which tool to call and when. Flat text (prompts, example pools) makes it difficult for the protocol to distinguish between noise statistics, merging rules, and documentation. Weights and adapters cannot transfer that knowledge between platforms. We suggest sharing typed federated artifacts, schema-validated objects with well-defined fields for per-field privacy (described here, but measured), dispute resolution, and cross-model transfer, and instantiating them as SYNAPSE1, a common tool-routing knowledge. After deleting 192 garbage entries and 1,916 training items that duplicate or almost duplicate test queries, a federated compendium routes within 1.1 points of a centralized one at 20 MB of JSON per client each round on StableToolBench (3,180 tools). The same experience merged and shown to the router as typed fields rather than one flat string is worth 8.5 points on clean data and 7.4 under 60% injected contradiction. Crossing merge and rendering shows the halves are inseparable (the typed merge shown flat is the worst arm), while three conflict policies are indistinguishable, so the conflict log that motivated this work is not the On τ-bench retail, each compendium arm improves GPT-4o agents' per-step tool-call accuracy by at least 6.7 points, attributed to format rather than federated experience. Two cautionary findings conclude the paper: on a topic-labeled math proxy and StableToolBench, a TF-IDF classifier over the same labeled experience beats every LLM routing arm (by 48 and 26 points, mostly retrieval recall) because the benchmark's pool holds labeled queries for every supposedly unseen tool and every test query verbatim before our filter. It cannot measure routing to tools without labels, which routing exists for.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义（研究动机与背景）

- **背景**：开放、网络化的“代理式 Web”中，智能体可能运行来自不同供应商的**冻结、异构 LLM**，各自保留私有交互历史，但仍需要互相学习“该调用哪个工具、何时不该调用”。
- **核心问题**：现有共享方式都不适配：
  - **权重/适配器联邦**：要求共享架构，冻结且异构模型之间不可迁移。
  - **原始交互轨迹**：泄露隐私。
  - **扁平文本（prompt、示例池）**：协议无法区分哪些是需加噪的统计量、哪些是合并时必须保留的规则、哪些只是文档说明。
- **论文主张**：联邦交换的基本单位不应是模型参数或扁平文本，而应是**类型化联邦工件（typed federated artifact）**：一个带 schema、字段有角色/类型/校验规则的对象，使逐字段隐私、逐字段冲突解决、跨模型迁移成为定义良好的操作。
- **实例化**：作者提出 **SYNAPSE**，用于工具路由知识共享。每个客户端把私有工具使用经验构建为 compendium，边缘聚合器按字段合并并广播全局 compendium；任意冻结 LLM 在推理时检索并重排候选工具。

## 2. 方法论：核心思想与关键技术细节

- **类型化 compendium 结构**：每个客户端贡献对象 \(C=(M,U,P,T,A)\)：
  - \(M\)：工具元数据与使用统计；
  - \(U\)：工具成功适用的场景；
  - \(P\)：何时不应使用的注意事项；
  - \(T\)：调用模板；
  - \(A\)：结构化附录。
- **Schema 校验**：每个 scenario、precaution、template 必须引用已注册工具；数值字段必须在声明范围内；无效贡献在合并前被拒绝。
- **联邦合并**：
  - 边缘聚合器按工具聚类 scenario，余弦相似度阈值 \(\ge 0.85\)。
  - 在每个簇内对 `{scenario, precaution
