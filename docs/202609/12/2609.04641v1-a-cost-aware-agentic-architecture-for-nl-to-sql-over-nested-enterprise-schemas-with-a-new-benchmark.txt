Title: A Cost-Aware Agentic Architecture for NL-to-SQL over Nested Enterprise Schemas, with a New Benchmark

URL Source: https://arxiv.org/pdf/2609.04641v1

Published Time: Mon, 07 Sep 2026 00:24:08 GMT

Number of Pages: 17

Markdown Content:
# A Cost-Aware Agentic Architecture for NL-to-SQL over Nested Enterprise Schemas, with a New Benchmark 

Yoga Sri Varshan Varadharajan * Ajay Yadav * Ritesh Goru † Prateek Chaudhury ‡

Constantine Caramanis * Prateek Jain † Divyateja Pasupuleti ‡ Sunil Kumar Pandey ‡

Abstract 

Natural-language-to-SQL systems have ad-vanced rapidly on academic benchmarks, yet production enterprise schemas exhibit graph-like, semi-structured, deeply nested structure that current benchmarks do not measure. We make two complementary contributions. First, we introduce the DevRev NL2SQL bench-mark: 900 execution-verified queries with nested-type and link-graph structure, accom-panied by the Semantic Depth Score (SDS), a schema-agnostic rubric for analytical reasoning depth. Second, we present a cost-aware single-generation agentic architecture whose schema-selection, metadata-retrieval, and error-repair components are designed for the requirements this regime imposes. On the DevRev NL2SQL benchmark the system attains 91.7% answer correctness, a margin of 54.6 percentage points over the next-best baseline; on the Spider 2.0 Snowflake public dataset, it is competitive with leading systems at a single-generation operat-ing point. 

1 Introduction 

Natural-language interfaces to SQL have advanced rapidly in recent years, with strong results on aca-demic benchmarks including Spider 1.0 (Yu et al., 2018), BIRD (Li et al., 2024), and Spider 2.0 (Lei et al., 2025). Deploying NL2SQL on production enterprise data exposes a class of structural challenges that the current benchmark suite does not character-ize. Modern enterprise systems of record are of-ten platforms organized around tickets, issues and other workflow objects. These evolve into graph-like, semi-structured data models. Their schemas contain typed ARRAY[STRUCT] columns whose ele-ments have explicit sub-field signatures, polymor-phic foreign-key relationships mediated through 

> *University of Texas at Austin
> †DevRev, USA
> ‡DevRev, Bengaluru, India

typed link arrays, and deeply nested objects with multiple levels of field nesting. Analytical ques-tions over such systems require reasoning over ar-ray elements, traversing typed link graphs, and pro-ducing SQL that combines LATERAL FLATTEN , con-ditional aggregation, and enum-constrained filters. Existing benchmarks emphasize flat relational schemas with primitive-typed columns. Spider 2.0-Snow, for example, contains 7,860 tables across 152 databases, but every table is defined by a flat CREATE TABLE DDL with primitive-typed columns, only 5% of tables carry table-level de-scriptions, and the corpus contains essentially no queries that exercise nested-type traversal. DevRev fields, in contrast, carry English descriptions that document operational intent. The semi-structured, link-graph regime, together with this schema-as-documentation property, is largely unmeasured by the current benchmark suite. This regime poses important challenges. Graph-like and polymorphic schemas require an iterative, feedback-driven schema-selection mechanism, to better surface the set of tables a query needs. When columns are typed ARRAY[STRUCT] , producing the correct LATERAL FLATTEN requires the sub-field schema and representative sample values inside the array and not the column type signature alone. Fail-ure modes specific to nested types are diverse and structured (improper LATERAL use, single-element 

UNNEST , malformed type conversions over nested fields, ambiguous flattening aliases). Generic retry-with-error-message strategies become suboptimal. These observations motivate the architecture: itera-tive schema discovery, metadata-rich retrieval, and a structured error model. Inference cost is a second consideration. Many leading Spider 2.0 systems employ tournament-style candidate selection (Pourreza et al., 2025; Gao et al., 2024b; Wang et al., 2025). On enterprise data with longer schema, longer candidate SQL bodies, slower nested-type executions, and more arXiv:2609.04641v1 [cs.AI] 4 Sep 2026 failure modes that each trigger a retry, the per-query base cost is elevated; this motivates studying single-generation architectures. Our approach could in principle be composed with multi-candidate gener-ation approaches. 

A new benchmark for nested enterprise schemas. We introduce and will release the De-vRev NL2SQL dataset: 900 execution-verified natural-language queries grounded in a production Snowflake schema with 30 core entity types (190 to-tal schema variants including custom subtypes and snap-in integrations), 13,898 fields, 2,036 typed 

ARRAY[STRUCT] columns, 1,412 STRUCT -typed or nested fields, and up to four levels of nesting (re-lease details in Appendix A). 

A cost-aware single-generation agentic archi-tecture. The system has five components. An LLM-driven schema-discovery loop browses a pre-computed knowledge graph through tool calls and re-invokes those tools on schema-coverage errors, broadening context in response to execution feed-back. A structured error taxonomy of 15+ classes maps each execution failure to a targeted repair directive.A pre-execution query plan verifier au-dits the SQL for structural flaws. A deterministic checker chain combines static checks with rewrit-ers that auto-fix cross-dialect functions and identi-fier casing. A dynamic cheatsheet (Suzgun et al., 2026) accumulates transferable SQL rules across queries. A semantic validator inspects executed results against the original question and rejects clear contradictions. The system uses a single generation trajectory per query and does not re-quire fine-tuning. On the DevRev benchmark our system attains 91.7% correctness on the full 900-query corpus and 91.5% on the high-SDS subset (SDS ≥ 11 ); on the Spider 2.0 Snowflake public leaderboard it is competitive with leading systems at a single-generation operating point. 

2 Related Work 

Multi-Candidate and Test-Time Scaling. 

Tournament-style selection has become a dominant NL2SQL paradigm. CHASE-SQL (Pourreza et al., 2025) combines multi-path reasoning with preference-optimized candidate selection. XiYan-SQL (Gao et al., 2024b) runs multiple generators with diverse prompting strategies and ensembles their outputs. Agentar-Scale-SQL (Wang et al., 2025) scales test-time compute through parallel synthesis followed by sequential refinement. DeepEye-SQL (Li et al., 2025) pairs 

N -version SQL generation with execution-guided confidence-aware selection. 

Agentic and Tool-Augmented NL2SQL. Acomplementary line of work frames NL2SQL as a multi-step tool-calling process. MAC-SQL (Wang et al., 2023) introduces multi-agent collaboration with a decomposer, refiner, and selector. DIN-SQL (Pourreza and Rafiei, 2023) decomposes the task into sub-problems and applies in-context learning with self-correction. DAIL-SQL (Gao et al., 2024a) systematizes the design space of LLM-based gen-eration strategies. AskData (Shkapenyuk et al., 2025) targets enterprise NL2SQL through offline metadata extraction that enriches schema context. Our two-tier orchestrator design (Section 4.2) fol-lows this agentic flow, separating workflow control from SQL synthesis; the contribution is in how the workflow is structured around the regime proper-ties characterized in Section 3: in particular, the iterative schema-expansion loop and the structured error taxonomy that drives it. 

NL2SQL Benchmarks. Spider 1.0 (Yu et al., 2018) established the cross-domain NL2SQL eval-uation paradigm with 200 databases of mod-erate complexity and primitive-typed columns. BIRD introduced messier schemas and external-knowledge questions but retained the flat-table as-sumption. Spider 2.0 (Lei et al., 2025) expanded to production-scale schemas; its Snowflake subset (Spider 2.0-Snow) contains 7,860 tables across 152 databases and shifts the schema-linking challenge to large scale. Its tables remain flat in the DDL sense: no declared sub-field schemas, no typed link arrays. The DevRev benchmark we introduce in Section 3 complements Spider 2.0-Snow by stress-ing nested-type traversal and link-graph reasoning on one production schema; the two settings exer-cise different aspects of enterprise NL2SQL, and to-gether provide better coverage of the design space than either alone. 

Synthetic NL query generation. SING-SQL (Cafero˘ glu et al., 2025) is a concurrent automated framework for generating in-domain NL2SQL training data. It hierarchically partitions a database into sub-schemas. and synthesizes SQL across multiple complexity tiers. Our DevRev generation pipeline differs in: (1) we target evaluation queries rather than fine-tuning data; (2) we use our SDS dimension targets, prioritizing analytical reasoning depth; and (3) we apply six-persona rewriting to introduce lexical variation across business roles. Finally, SING-SQL operates on flat schemas, while DevRev is designed specifically for the nested-type, link-graph regime. 

3 The DevRev Benchmark 

3.1 The Enterprise Schema Regime 

Existing NL2SQL benchmarks evaluate systems on flat, tabular schemas with primitive-typed columns. Spider 2.0-Snow spans 547 queries over 152 databases and 7,860 tables. Every table is defined by a flat CREATE TABLE DDL; the corpus contains no STRUCT columns, and where semi-structured data appears the columns are typed as opaque 

VARIANT with no sub-field schema in the DDL. Production enterprise schemas are structurally different. Work-item platforms (organized around tickets, issues, and other workflow objects) are graph-like: core entities are nodes and typed link ar-rays, tags, and group memberships are edges. Their schemas contain ARRAY[STRUCT] columns with ex-plicit sub-field signatures (not opaque VARIANT ), polymorphic relationships mediated through typed link arrays whose type sub-field discriminates the linked entity, objects nested up to four levels deep, and per-field English descriptions that encode op-erational intent (e.g., whether an INTEGER column is a count or a boolean flag, or what the labels in a stage enum denote). Analytical questions over such schemas require LATERAL FLATTEN over array columns, enum-constrained filtering over em-bedded vocab, and multi-hop typed-link traversal patterns absent from the existing benchmark suite. Table 1 summarizes the contrast: Spider 2.0-Snow exercises large-scale schema linking over many flat tables; DevRev exercises nested-type traversal and link-graph reasoning over one pro-duction schema. 

3.2 Schema Overview 

The DevRev production database contains 30 core entity types and 190 schema variants; our bench-mark uses the 15 for which detailed m-schema documentation was available. The schema counts appear in Table 1. Two structural patterns dominate. First, the 

polymorphic link graph : a ticket’s links field is an ARRAY[STRUCT] whose elements carry a type 

sub-field ( blocks , relates_to , resolves , . . . ) and a display_id pointing at the linked entity. Because the same array can link to issues, en-                               

> Property DevRev Spider 2.0-Snow
> Databases / domains 1152 Tables 15 †7,860 Typed ARRAY[STRUCT] fields 2,036 0
> STRUCT / nested fields 1,412 0Opaque VARIANT columns 06,180 Max nesting depth 41
> Table 1: Schema comparison. DevRev’s
> ARRAY[STRUCT] fields have explicit sub-field schemas; Spider 2.0-Snow’s VARIANT columns are opaque semi-structured types with no DDL sub-field definition. †15 of 30 core entity types (190 total schema variants); 15 used for benchmark generation.

hancements, or other tickets, resolving the join target requires filtering on type and consulting an external entity mapping a pattern with no ana-logue in flat-table benchmarks. Second, non-link 

ARRAY[STRUCT] fields : surveys_aggregation 

holds survey-response records, tags holds la-bel structs with type and source attribution, and 

owned_by holds typed user references. Each re-quires LATERAL FLATTEN to access, with a distinct sub-field signature per entity. 

3.3 Four-Stage Generation Pipeline 

Queries are generated by a four-stage pipeline en-forcing analytical depth: (1) Column Planning se-lects column subsets; (2) Formula Retrieval derives structural patterns; (3) Query Generation generates natural language queries using DevRev-specific col-umn names and business concepts; and (4) Persona Rewriting rewrites each slot query into a distinct business role voice. We provide the complete de-tails in the Appendix. 

3.4 The Semantic Depth Score (SDS) 

SDS is a seven-dimensional rubric that measures the analytical reasoning required to formulate a correct SQL query, independent of the resulting query’s syntactic complexity. Each dimension is scored 0–5 (max of 35). The dimensions are: • D1 Eligibility: how many intersecting conditions define the subject of analysis. • D2 Derived Metric: whether a non-trivial value (ratio, delta, unit conversion) must be computed rather than read directly. • D3 Population Scoping: how tightly the analy-sis population is restricted by independent filters. • D4 Multi-Stage Logic: whether multiple analyt-ical steps are chained (find winner, then compute for winner only). • D5 Output Precision: how many distinct output fields are explicitly named or implied. • D6 Temporal Precision: whether a precise time window, date, or rolling period is required. • D7 SQL Construct Diversity: how many SQL constructs (window functions, CTEs, 

LATERAL FLATTEN , set operations) are needed. SDS decouples semantic depth from syntac-tic clause counts, which is a limitation of the easy/medium/hard/extra-hard labels used by earlier benchmarks: a query with a single deeply chained derivation can score high on D1, D2, and D4 while using few clauses, and a query with many simple aggregations can score low on D2 and D4 despite producing long SQL. SDS supports diagnostic anal-ysis: a system can be evaluated separately on high-D6 queries (temporal-reasoning failures) versus high-D7 queries (advanced-construct failures) to identify targeted improvement opportunities. 

4 System Design for Nested Enterprise Schemas 

The design principles derive from the schema regime characterized in Section 3. Figure 1 shows the architecture. 

4.1 Design Principles from the Regime 

The architecture is organized around four regime-inspired principles: (1) Iterative schema selec-tion. Polymorphic typed-link arrays prevent the required table set from being predictable before generation, so the system must expand schema con-text in response to execution feedback rather than committing to a fixed set up front. (2) Sub-field-aware retrieval. For ARRAY[STRUCT] columns, DDL type signatures are insufficient; the generator needs nested sub-field schemas, enum vocabular-ies, and sample values. (3) Structured, history-aware error handling. Nested-type failure modes are diverse yet classifiable; a taxonomy with tar-geted repair directives and full attempt history out-performs generic retry-with-raw-error. (4) Single-generation operating point. Elevated per-query cost on enterprise schemas shifts the tradeoff away from multi-candidate tournaments; principles 1–3 reduce the variance that N -candidate generation would otherwise absorb. 

4.2 Two-Tier Agentic Architecture 

The system separates workflow orchestration from SQL synthesis. A top-level orchestra-tor agent receives the natural-language question and manages the full query lifecycle by invoking three tools: KGSchemaGetter (catalog browsing), 

NodeSchemaGetter (per-table schema retrieval), and NLToSQL (SQL generation). It inspects execu-tion results, classifies errors, and decides whether to expand schema context, repair the query, or ac-cept the result. The orchestrator is the only compo-nent that holds the full attempt history and interacts with the database. The NL2SQL generator is a tool call, not a sep-arate agent: it takes assembled context (schemas, question, cheatsheet rules, structured feedback from prior failures) and returns a single SQL query. It does not call tools or see execution results. This separation keeps each component’s prompt focused: meta-level decisions (e.g., “fetch addi-tional tables after a table_not_found error”) be-long to the orchestrator and do not contaminate the generator’s SQL synthesis task. The orchestrator and generator use a frontier model for deep rea-soning; constrained verification tasks (Query Plan Verifier, Semantic Validator, error feedback genera-tion) are routed to a faster, cheaper model, keeping the iterative repair loop cost-efficient. 

4.3 LLM-Driven Iterative Schema Discovery 

Schema selection is driven by the orchestrator LLM through tool calls against a precomputed knowl-edge graph. There is no embedding-based ranker in the runtime hot path. The LLM itself selects which tables to expose to the generator and re-vises that selection in response to execution feed-back. The knowledge graph is built offline from source schema documentation via three transfor-mations: partition collapsing (reducing millions of date-partitioned physical tables to thousands of logical entries), key-column scoring (surfac-ing the most informative columns in the catalog), and type normalization (mapping source-specific types to a canonical vocabulary). Details are in Appendix B.1. 

Stage 1: Catalog browsing. The orchestrator calls 

KGSchemaGetter(database_name) to retrieve the curated table catalog. Each entry contains the table name, a one-line description, and the table’s top key columns from offline scoring. 

Stage 2: Smart table selection in context. The NL Question 

> Top-Level Orchestrator Agent
> Manages workflow · Dispatches tools · Owns full attempt history · Retries ≤K
> Primary LLM
> Agentic Schema
> Selection
> S1: Database
> Filter
> S2: Smart Table
> Selection
> KGSchemaGetter
> S3: Metadata
> Retrieval
> NodeSchemaGetter
> S4: Schema
> Expansion
> (on schema error)
> NL2SQL
> Generator
> schemas + question +
> cheatsheet + attempt history
> Primary LLM
> context
> Validation & Execution Chain
> Query Plan
> Verifier
> Secondary LLM
> Deterministic
> Checkers (10)
> Database
> Execution
> SQL
> structural
> feedback
> Error Taxonomy & Repair
> Rule-Based
> Classifier
> 20+ error types
> (schema / syntax / dialect / runtime)
> LLM Feedback
> Generator
> Secondary LLM
> Contextual one-shot
> repair examples
> FAIL
> retry with full
> attempt history
> schema-coverage
> error → expand
> Semantic
> Validator
> Secondary LLM
> PASS
> REJECT
> Final SQL ACCEPT
> Dynamic Cheatsheet
> (test-time learning)
> DC-Cu: Accumulates transferable
> SQL patterns across queries
> inject
> rules
> record
> pattern
> Offline Preprocessing (KG Curation)
> Schema Parsing
> & Type Mapping
> Partition
> Collapse
> Metadata Enrichment
> (key cols, enums, samples)
> Index Construction
> (catalog, cache, schemas)
> Table Catalog · Per-Table Schemas · Column Cache · Domain Guidance
> feeds runtime
> Knowledge
> Graph
> Raw Database
> Schemas

Figure 1: System architecture. The top-level agent manages the workflow by invoking three tools: KGSchemaGetter 

(catalog browsing), NodeSchemaGetter (compressed per-table schema retrieval), and NLToSQL (SQL generation). 

orchestrator reads the catalog inline and decides which tables are relevant to the question. Selection is LLM-driven and conditioned on the question, the catalog (which includes top-K key columns to aid selection), and the cheatsheet (Section 4.7). 

Stage 3: Per-table schema retrieval. The orches-trator calls NodeSchemaGetter(table_id) in par-allel for each chosen table. Each call returns a com-pressed schema in a one-line-per-field Schemonic 

format that captures the field name, type, enum vo-cabulary, English description, and sub-field schema for nested columns. The compression is essential because a single DevRev entity (e.g., ticket ) has hundreds of fields and would otherwise dominate the context budget. 

Stage 4: Error-guided schema expansion. When generated SQL fails with a schema-coverage error (table_not_found , column_not_found ,

unresolved_alias ), the orchestrator re-invokes 

KGSchemaGetter or NodeSchemaGetter for addi-tional tables, guided by the identifiers the failed SQL referenced, and reassembles the context. This makes schema discovery iterative, handling the polymorphic-link case (Section 3.2) where the re-quired table set cannot be predicted before genera-tion. 

4.4 Pre-Execution Query Plan Verifier 

Before a generated query is passed to the determin-istic checkers or executed against the database, it is audited by an LLM-based Query Plan Verifier. This component acts as a structural linter, analyz-ing the SQL for logical flaws that would otherwise result in silent semantic failures or expensive exe-cution errors. The verifier checks for three specific conditions: • Join Safety: Ensures that joins are performed on valid key columns rather than arbitrary fields, preventing accidental Cartesian products. • Grain Correctness: Verifies that the level of de-tail (grain) of the query matches the user’s ques-tion, particularly when aggregating over flattened arrays or joined tables. • Deduplication Placement: Checks that 

DISTINCT or GROUP BY clauses are correctly positioned to handle fan-out from one-to-many joins or array unnesting. If the verifier detects a structural flaw, it rejects the query and provides targeted feedback (e.g., “The join on status will cause a Cartesian product; join on id instead”). This feedback is immedi-ately routed back to the generator, bypassing the database entirely. By catching logical errors pre-execution, the verifier reduces both database com-pute costs and the incidence of plausible-looking but incorrect result sets. 

4.5 Dynamic Error Taxonomy and History-Aware Feedback 

When generation produces SQL that fails on execution, the orchestrator classifies the failure into one of 15+ error types organized along two axes: failure phase (schema coverage, nested-type, syntax/dialect, runtime) and corrective ac-tion. The full taxonomy is in Appendix B.2. The key routing decision is: schema-coverage errors (table_not_found , column_not_found ,

unresolved_alias ) trigger Stage 4 schema ex-pansion, addressing the root cause (missing con-text) rather than the symptom; all other error types trigger in-place repair with a targeted directive. Each error class maps to a concise repair direc-tive. Together with the full history of prior failed attempts, this is injected as structured context into the next generator call. This prevents the genera-tor from repeating classified mistakes and enables progressive refinement across retries. The taxonomy is dynamic: error definitions and repair directives live in a configuration file that can be extended without code changes. Section 5.5 evaluates the contribution of structured, history-aware feedback against a generic retry-with-error-message baseline. 

4.6 Deterministic Checker Chain 

Generated SQL passes through a chain of deter-ministic stages (summarized in Table 4 in the Ap-pendix) before the orchestrator accepts a result. The chain mixes two kinds of stages. Blockers are pure functions that either pass the SQL through or return a classified error that re-enters the repair loop. Rewriters silently transform the SQL and hand the rewritten form to the next stage; they never fail. Blockers prevent silent semantic failures 

(queries that execute successfully but return in-correct results, e.g., due to overly permissive fuzzy matching on enum columns). Rewrit-ers silently fix cross-dialect slippage: a Col-umn Identifier Fixer normalizes casing, and a Dialect Auto-Rewriter maps BigQuery/DuckDB functions to their Snowflake equivalents (e.g., 

DATE_DIFF → DATEDIFF ). Execution is the chain’s final stage; cheap static checks run first so that detectable failures are absorbed before incurring database cost. 

4.7 Test-Time Learning 

We use the Dynamic Cheatsheet framework of Suz-gun et al. (2026): a persistent, evolving memory of patterns curated at test time. In our setting the entries are SQL rules. When the orchestrator classifies an execution error, it extracts a concise, transferable rule (e.g., “divide THELOOK times-tamps by 1000000 before casting” or “use LATERAL FLATTEN for the cpc array”). These rules are per-sisted across the evaluation session. Before every generation call, the top rules from the cheatsheet are injected into the generator’s prompt. 

4.8 Semantic Validation 

After a generated query successfully executes against the database, the system performs a final semantic plausibility check. An LLM validator is presented with the user’s original question, the gen-erated SQL, and a preview of the execution results (column names and the first few rows). The val-idator is instructed to be lenient (accepting empty result sets if the filters are restrictive, and accept-ing any result that is directionally plausible) but to reject clear contradictions. For example, if the user asks for “how many tickets” and the result set contains hundreds of raw ticket rows instead of a single aggregate count, the validator rejects the re-sult. Rejections are treated as semantic errors: the validator’s explanation is appended to the attempt history, and the generator is re-invoked. 

5 Experiments 

The complete description is in the Appendix. 

5.1 Experimental Setup Baselines. We compare against APEX-SQL (Cao et al., 2026a) (Hypothesis-Verification loop, run with num_votes=1 to match our single-path com-pute budget), FlexSQL (Pham et al., 2026) (plan-and-stitch with four candidate plans, default con-figuration), and ReFoRCE (Deng et al., 2025a) (self-refinement with majority-vote consensus). 

Models. All four systems use 

gpt-5.2-2025-12-11 as the primary model. Our system additionally routes verification and feedback tasks (query plan verifier, semantic validator) to the secondary model – gpt-4.1-mini 

to reduce cost. No fine-tuning is performed. 

Metrics. Our primary metric is Answer Cor-rectness (AC) : the final verdict of a multi-stage evaluation pipeline. Generated SQL must first pass the deterministic checker chain (10 static/rewrite stages), then execute successfully against the live database, and finally pass a semantic LLM valida-tion (GPT-5.2 judge) that receives the NL query, generated SQL, and a preview of result rows and is-sues a binary verdict on whether the result answers the question. AC is therefore strictly stronger than Execution Accuracy (EX): it requires syntactic va-Figure 2: Mean judge score (% of 50-point maximum) by SDS bucket for all four systems. 

lidity, successful execution, and semantic correct-ness. We additionally report Mean Score (%) , the continuous judge score normalised to 0–100% of a 50-point rubric, as a secondary quality indica-tor. Full judge criteria are in Appendix C.8. For Spider 2.0-Snow we report EX using the official evaluation script provided by Lei et al. (2025). 

5.2 Main Results 

Table 2 reports results on the full 900-query DevRev benchmark. Our system achieves An-swer Correctness of 91.7% (825/900 queries), a margin of 54.6 pp over the next-best system (FlexSQL, 37.1%). The baselines cluster be-tween 27–37% despite very different architectures, hypothesis-verification loop (APEX-SQL), plan-and-stitch (FlexSQL), and majority-vote consensus (ReFoRCE), suggesting all three hit the same struc-tural ceiling: the inability to reason over nested 

ARRAY[STRUCT] fields and typed link traversal, rather than any flaw intrinsic to their respective generation strategies. The $/correct q column makes the produc-tion deployment case directly. Using veri-fied gpt-5.2-2025-12-11 pricing ($1.75/M input, $14.00/M output), our system costs $0.57 per cor-rectly answered query : 8 × cheaper than APEX-SQL ($4.60), 27 × cheaper than FlexSQL ($15.35), and 1.6 × cheaper than ReFoRCE ($0.93). Notably, ReFoRCE is the cheapest system per query ($0.27) but only 29.1% of its queries are correct, so the effective cost per useful answer is 1.6 × ours. 

5.3 Difficulty-Stratified Analysis 

Figure 2 stratifies results by SDS total into three buckets: Easy ( ≤12, n=139), Medium (13–18, 

n=250), and Hard ( >18, n=511), and reveals two different responses to increasing complexity. All three baselines degrade monotonically: Re-FoRCE most steeply ( −21.3 pp, from 48.5% to 27.2%), followed by APEX-SQL ( −15.1 pp) and FlexSQL ( −12.3 pp). The full per-bucket break-down is in Appendix D.2. Our system’s mean score remains essentially flat across buckets (80.1% →

80.9% → 78.1%, a 2.8 pp range), and Answer Correctness similarly spans less than 6 pp (89.2% 

→ 94.4% → 91.0%). As a result, the gap over FlexSQL grows with complexity: 44.6 pp on Easy, 54.8 pp on Medium, and 57.1 pp on Hard. 

5.4 SDS as an Actionable Improvement Signal 

SDS as a diagnostic can improve agent perfor-mance without modifying core architecture. 

Experimental protocol. For each baseline, we identify its set of originally failing queries (Re-FoRCE: 638 queries; APEX-SQL: 655 queries). For each failing query, we inspect its D1–D7 scores and inject a targeted natural-language hint into the system’s schema context. Hints are dimension-specific: for example, a query with high D4 (multi-stage logic, score ≥ 3) receives the directive “ this query requires a multi-stage CTE pipeline; plan the stages before writing SQL ”. 

Results. On the 638 originally failing ReFoRCE queries, SDS-guided hints achieve 48.1% AC on that subset, lifting the score from 29.1% to 63.2%. For APEX-SQL’s 655 failing queries, hints achieve 19.5% on that subset, raising AC from 27.2% to 41.4%. The larger gain for ReFoRCE reflects the severity of its original dialect gap. Table 3 breaks down the gains by category. Im-provements are consistent across all six discrimina-tive categories, with Ranking/Top-N (+39.6 pp for ReFoRCE) and Date/Time (+35.5 pp) showing the largest gains. These categories map directly to SDS dimensions D4 (multi-stage logic) and D6 (tem-poral precision), confirming that the SDS rubric correctly identifies the loci of difficulty. The upshot: (I) The SDS rubric is predictive :the failing queries that respond most strongly to SDS hints are those with high D4 and D2 (derived metrics) scores. (II) SDS is actionable : dimension-score inspection at query time is sufficient to con-struct targeted guidance that measurably improves accuracy without architectural changes. 

5.5 Ablations (A) Schema Expansion. We disable Stage 4: on schema-coverage errors ( table_not_found ,Accuracy Cost & Latency System DevRev AC (%) DevRev Mean (%) Spider 2.0-Snow EX (%) Wall (s/q) Tokens (K/q) $/correct q 

Ours 91.7 79.2 85.8 (103/120) ‡ 161 108 $0.57 

APEX-SQL 27.2 47.2 73.13 § 200 260 $4.60 FlexSQL 37.1 46.4 65.45 § 1,653 1,183 $15.35 ReFoRCE 29.1 33.0 62.89 § 74 56 $0.93 

Table 2: Results on DevRev-900 and Spider 2.0-Snow. $/correct q = API cost per correctly answered query (gpt-5.2 pricing: $1.75/M input, $14.00/M output). ‡120-query gold-answer subset; not comparable to full leaderboard. 

> §

Best published leaderboard submission per system. 

Category ReFoRCE APEX-SQL FlexSQL 

∆AC (pp) ∆AC (pp) ∆AC (pp) Rel./Network +34.9 +13.9 +14.3 Classif./Grouping +31.1 +13.6 +11.5 Date/Time +35.5 +15.2 +13.3 Ranking/Top-N +39.6 +12.1 +13.9 Statistical +33.6 +12.7 +17.1 Comparison +36.6 +15.1 +16.6 

Overall (all 900) +34.1 +14.2 +13.3 

Table 3: Per-category AC improvement (SDS-guided minus baseline) for ReFoRCE, APEX-SQL, and FlexSQL. All Rel/Net 20 40 60 80 100        

> Accuracy (%)
> +8.5 +11.4
> (A) Schema Expansion
> All D7 ≥420 40 60 80 100
> +15.0 +64.4
> (B) Metadata Retrieval
> AC (%) Converged (%) 20 40 60 80 100
> +13.2 +13.2
> (C) Error Taxonomy
> AC (%) 20 40 60 80 100
> +5.9
> (D) Checker Chain
> Ablated Full System (91.7%)

Figure 3: Component ablations on the DevRev-900 benchmark. Each panel compares the ablated configura-tion (red) against the full system at 91.7% AC (blue). 

column_not_found , unresolved_alias ), the or-chestrator returns the error to the generator with-out fetching additional tables. Accuracy drops by 8.5 pp, with a larger 11.4 pp drop on Relation-ship/Network queries, confirming that polymorphic link traversal is the primary beneficiary of iterative schema expansion. 

(B) Metadata-Enriched Retrieval. We replace Stage 3 with type-signature-only context: column names and DDL types, but no sub-field schemas, enum vocab, sample values, or join hints. This pro-duces the largest ablation effect: −15.0 pp overall and a catastrophic −64.4 pp on high-D7 queries. 

(C) Error Taxonomy. We replace the structured taxonomy with a generic retry loop: on any failure, the error message is appended and the generator retries without classification, targeted directives, or history preservation. Accuracy drops by 13.2 pp and mean repair rounds increase from 1.4 to 3.2, showing that structured feedback improves conver-gence rate and reduces wasted iterations. 

(D) Deterministic Checkers. We remove the pre-execution checker chain and send generated SQL directly to the database. Accuracy drops by 5.9 pp, but the cost impact is more significant: mean DB executions per query increase from 1.2 to 2.8. The checkers absorb failures cheaply before they reach the database, reducing both cost and latency. 

6 Conclusion 

We present two complementary contributions for NL-to-SQL on nested enterprise schemas. The De-vRev benchmark fills a measurement gap, surfac-ing nested-type traversal, polymorphic link-graph reasoning. The cost-aware single-generation ar-chitecture responds to the regime properties the benchmark exposes, with iterative schema selec-tion, sub-field-aware metadata retrieval, and a struc-tured history-aware error taxonomy. The architecture composes naturally with tournament-style multi-candidate generation: in our view the most direct path to extend the work. We also expect the design principles to transfer to other dialects and enterprise platforms whose schemas exhibit the same properties. 

Limitations 

The system relies on an execution API for the iter-ative error-feedback loop, and thus cannot run in offline settings. The evaluation covers two bench-marks, both on Snowflake, and generalization to other SQL dialects should be validated. The De-vRev benchmark was generated with LLM assis-tance and verified by execution against the produc-tion database. This is a different verification stan-dard than fully human-curated benchmarks. Cost comparisons against multi-candidate systems de-pend on disclosed candidate counts and execution-call counts. For systems whose costs are not pub-licly reported, our comparisons in Section D.6 are inferential and treat the candidate count N as a lower bound on inference cost. 

References 

Hasan Alp Cafero˘ glu, Mehmet Serhat Çelik, and Özgür Ulusoy. 2025. SING-SQL: A synthetic data gener-ation framework for in-domain text-to-SQL transla-tion. arXiv preprint arXiv:2509.25672 .Bowen Cao, Weibin Liao, Yushi Sun, Dong Fang, Haitao Li, and Wai Lam. 2026a. Apex-sql: Talk-ing to the data via agentic exploration for text-to-sql. 

arXiv preprint arXiv:2602.16720 .Bowen Cao, Weibin Liao, Yushi Sun, Dong Fang, Haitao Li, and Wai Lam. 2026b. APEX-SQL: Talk-ing to the data via agentic exploration for text-to-SQL. 

arXiv preprint arXiv:2602.16720 .Minghang Deng, Ashwin Ramachandran, Canwen Xu, Lanxiang Hu, Zhewei Yao, Anupam Datta, and Hao Zhang. 2025a. Reforce: a text-to-sql agent with self-refinement, consensus enforcement, and column exploration. arXiv preprint arXiv:2502.00675 .Minghang Deng, Ashwin Ramachandran, Canwen Xu, Lanxiang Hu, Zhewei Yao, Anupam Datta, and Hao Zhang. 2025b. ReFoRCE: A text-to-SQL agent with self-refinement, consensus enforcement, and column exploration. arXiv preprint arXiv:2502.00675 .Dawei Gao, Haibin Wang, Yaliang Li, Xiuyu Sun, Yichen Qian, Bolin Ding, and Jingren Zhou. 2024a. Text-to-SQL empowered by large language models: A benchmark evaluation. Proceedings of the VLDB Endowment , 17(5):1132–1145. Yingqi Gao, Yifu Liu, Xiaoxia Li, Xiaorong Shi, Yin Zhu, Yiming Wang, Shiqi Li, Wei Li, Yuntao Hong, Zhiling Luo, Jinyang Gao, Liyu Mou, and Yu Li. 2024b. A preview of XiYan-SQL: A multi-generator ensemble framework for text-to-SQL. arXiv preprint arXiv:2411.08599 .Fangyu Lei, Jixuan Chen, Yuxiao Ye, Ruisheng Cao, Dongchan Shin, Hongjin Su, Zhaoqing Suo, Hongcheng Gao, Wenjing Hu, Pengcheng Yin, Victor Zhong, Caiming Xiong, Ruoxi Sun, Qian Liu, Sida Wang, and Tao Yu. 2025. Spider 2.0: Evaluating lan-guage models on real-world enterprise text-to-SQL workflows. In Proceedings of ICLR .Boyan Li, Chong Chen, Zhujun Xue, Yinan Mei, and Yuyu Luo. 2025. DeepEye-SQL: A software-engineering-inspired text-to-SQL framework. arXiv preprint arXiv:2510.17586 .Jinyang Li, Binyuan Hui, Ge Qu, Jiaxi Yang, Binhua Li, Bowen Li, Bailin Wang, Bowen Qin, Ruiying Geng, Nan Huo, and 1 others. 2024. Can LLM already serve as a database interface? A BIg bench for large-scale database grounded text-to-SQLs. Advances in Neural Information Processing Systems , 36. Quang Hieu Pham, Yang He, Ping Nie, Canwen Xu, Davood Rafiei, Yuepeng Wang, Xi Ye, and Joce-lyn Qiaochu Chen. 2025. FlexSQL: Flexible explo-ration and execution make better text-to-SQL agents. 

arXiv preprint arXiv:2605.02815 .Quang Hieu Pham, Yang He, Ping Nie, Canwen Xu, Davood Rafiei, Yuepeng Wang, Xi Ye, and Joce-lyn Qiaochu Chen. 2026. Flexsql: Flexible explo-ration and execution make better text-to-sql agents. 

arXiv preprint arXiv:2605.02815 .Mohammadreza Pourreza, Hailong Li, Ruoxi Sun, Yeounoh Chung, Shayan Talaei, Gaurav Tarlok Kakkar, Yu Gan, Amin Saberi, Fatma Ozcan, and Sercan O. Arik. 2025. CHASE-SQL: Multi-path rea-soning and preference optimized candidate selection in text-to-SQL. In Proceedings of ICLR .Mohammadreza Pourreza and Davood Rafiei. 2023. DIN-SQL: Decomposed in-context learning of text-to-SQL with self-correction. Advances in Neural Information Processing Systems .Vladislav Shkapenyuk, Divesh Srivastava, Theodore Johnson, and Parisa Ghane. 2025. Automatic meta-data extraction for text-to-SQL. arXiv preprint arXiv:2505.19988 .Mirac Suzgun, Mert Yuksekgonul, Federico Bianchi, Dan Jurafsky, and James Zou. 2026. Dynamic cheat-sheet: Test-time learning with adaptive memory. In 

Proceedings of EACL , pages 7080–7106. Bing Wang, Changyu Ren, Jian Yang, Xinnian Liang, Ji-aqi Bai, LinZheng Chai, Zhao Yan, Qian-Wen Zhang, Di Yin, Xing Sun, and Zhoujun Li. 2023. MAC-SQL: A multi-agent collaborative framework for text-to-SQL. arXiv preprint arXiv:2312.11242 .Pengfei Wang, Baolin Sun, Xuemei Dong, Yaxun Dai, Hongwei Yuan, Mengdie Chu, Yingqi Gao, Xiang Qi, Peng Zhang, and Ying Yan. 2025. Agentar-scale-SQL: Advancing text-to-SQL through orchestrated test-time scaling. arXiv preprint arXiv:2509.24403 .Tao Yu, Rui Zhang, Kai Yang, Michihiro Yasunaga, Dongxu Wang, Zifan Li, James Ma, Irene Li, Qingn-ing Yao, Shanelle Roman, Zilin Zhang, and Dragomir Radev. 2018. Spider: A large-scale human-labeled dataset for complex and cross-domain semantic pars-ing and text-to-SQL task. In Proceedings of EMNLP .A Dataset Release 

The DevRev NL2SQL benchmark will be released publicly upon publication. The release includes: (1) all 900 natural-language queries with SDS an-notations and complexity-category labels; (2) the full schema documentation (m-schema format) for all 15 entity types used in generation; and (3) the SDS scoring rubric and LLM judge prompt for re-producible evaluation. The dataset will be hosted on Hugging Face under a permissive open-source license. A leaderboard for community submissions will accompany the release. 

B System Design Details 

B.1 Offline Knowledge Graph Construction 

A knowledge graph is built once per deployment from the source schema documentation. Three transformations matter for enterprise schemas: • Partition Collapsing. Date-partitioned tables (e.g., GA_SESSIONS_YYYYMMDD ,

EVENTS_YYYYMMDD ) are collapsed into asingle logical entry in the catalog, with the partition pattern captured as metadata. This reduces a catalog of millions of physical tables to thousands of logical ones. • Key-Column Scoring. Each table’s columns are scored as candidate keys based on uniqueness, naming heuristics, and foreign-key references; the highest-scoring keys are surfaced directly in the catalog to aid table selection without requir-ing full schema retrieval. • Type Normalization. Source-specific type strings are mapped to a canonical type vocabulary (ARRAY[STRUCT] , STRUCT , primitive types) so that downstream consumers reason about nested structure uniformly. 

B.2 Error Taxonomy Classes 

When generation produces SQL that fails on execu-tion, the orchestrator classifies the failure into one of 15+ error types. The taxonomy is organized by failure phase, with each class mapped to a correc-tive action: • Schema-coverage errors: table_not_found ,

column_not_found , unresolved_alias . Trig-ger Stage 4 schema expansion. • Nested-type errors: forbidden_lateral 

(incorrect LATERAL placement), 

unnest_single_list (flattening a single-ton array), nested_conversion_error (type                               

> Checker Type Purpose
> Multi-Statement Blocker Rejects multiple statements
> SELECT * Guard Blocker Rejects SELECT * patterns
> VALUES Guard Blocker Rejects VALUES(...) in CTEs Placeholder Guard Blocker Rejects bind parameters/stubs Dialect Rewriter Rewriter Silently fixes dialect mistakes Reserved Word Guard Blocker Rejects reserved word aliases Syntax Validator Blocker Local parse check ( sqlglot )Fuzzy Search Guard Blocker Rejects LIKE /ILIKE patterns Heuristic Detector Blocker Regex checks for anti-patterns Execution Validator Blocker Executes against the database

Table 4: Deterministic Checker Chain. Blockers reject queries and return errors to the orchestrator; Rewriters silently mutate the SQL. 

coercion over a sub-field). Trigger repair with a construct-specific directive. • Identifier errors: reserved_word_alias (alias collides with a Snowflake reserved word), 

ambiguous_flatten_alias (multiple flattens with conflicting aliases). Trigger alias-rewriting repair. • Semantic errors: empty_result ,

wrong_aggregation_grain , missing_filter .Trigger semantic repair with the question re-presented alongside the failed SQL. 

B.3 Deterministic Checker Chain 

Table 4 lists all ten stages of the deterministic checker chain (Section 4.6). Stages are executed in order; blockers reject the SQL and return classi-fied errors to the orchestrator’s repair loop, while rewriters silently transform the SQL before pass-ing it to the next stage. Execution against the database is the final stage, ensuring that all stat-ically detectable failures are absorbed before incur-ring database cost. 

C Dataset Generation Pipeline: Full Details 

This appendix provides the complete specifica-tion of the four-stage formula-anchored generation pipeline introduced in Section 3.3, together with the full SDS scoring rubric (Section C.7), the LLM judge rubric used for evaluation (Section C.8), and annotated examples of each intermediate artifact (Section C.9). 

C.1 Stage 0: Sub-Schema Generation 

Before the four-stage query pipeline begins, an 

LLM sub-schema generator produces a semanti-cally coherent column subset from each of the 14 table combinations (e.g., account + enhancement + ticket , account + meeting + opportunity ). The generator receives the full MSCHEMA source for the relevant tables, a structured text format that records, for each field, its type, description, enum vocabulary, and sub-field schema if the column is an ARRAY[STRUCT] , and is instructed to select 2–5 analytically useful non-connection columns per ta-ble while automatically retaining all primary-key and foreign-key columns. The output is a JSON sub-schema ( sub_schema.{table}.{columns} )that supplies Stage 1 with a pruned, focused view of the schema. Across 14 table combinations, 150 sub-schemas are produced, forming the atomic unit of the pipeline. 

C.2 The Formula Book 

A formula book of 25 structural patterns is derived from the 25 highest-SDS Spider 2.0-Snow queries (all with SDS ≥ 27 on the 0–35 scale). Each for-mula is stored in two representations. 

Human-readable representation. Each entry contains: (i) the source Spider 2.0 query text; (ii) a Level 1 macro pattern : a multi-stage logic skeleton expressed in slot-variable notation; and (iii) Level 2 micro patterns : a list of semantic-depth phrases from the source query, each anno-tated with the SDS dimension it triggers and the SQL pattern it forces. 

Example: Formula F_001 (snapshot eligibility + percentage change). 

Source query (abbreviated): “For each U.S. state, find how the number of active financial branch entities has changed from March 1, 2020 to De-cember 31, 2021. An entity is considered active on a specific date if its start date is on or before that date and its end date is either null or on or after that date.” 

Macro pattern (Level 1): 

1. Define [ CONCEPT ] using a plain-English el-igibility rule: start_date ≤ date AND 

(end_date IS NULL OR end_date ≥ date). 2. Evaluate [ CONCEPT ] at [ SNAPSHOT _DATE _1] for each [ GROUPING _DIMENSION ]. Evaluate at [ SNAPSHOT _DATE _2]. 3. Compute [ DERIVED _METRIC : % CHANGE ] be-tween snapshots per [ GROUPING _DIMENSION ]. 4. Output: grouping, snapshot-1 value, snapshot-2 value, % change. 

Micro patterns (Level 2, selected): 

• “A [ ENTITY ] is considered [ CONCEPT ] on a spe-cific date if . . . and its end date is either null or on or after that date” → D1 trigger (multi-condition NULL eligibility). • “the percentage change in these counts” → D2 

trigger (derived metric = (v2 − v1)/v 1 × 100 ). • “find how [ METRIC ] has changed from [ DATE _1] to [ DATE _2]” → D6 trigger (two precise snap-shot dates). 

Filled example on DevRev schema: “For each ac-count tier, find how the number of actively open tickets changed between July 1, 2023, and Decem-ber 31, 2023. A ticket counts as actively open on a given date if it was created on or before that date and its resolution date is either missing or falls after that date. For each tier, show the open count on July 1st, the open count on December 31st, and the percentage change, sorted by the largest drop.” 

Machine-readable representation. 

The same 25 formulas are stored as 

SPIDER2_FORMULA_BOOK.json , with fields: 

formula_id , sds_score , sds_dimensions (per-dimension scores), complexity_categories ,

macro_pattern , micro_patterns (list), 

filled_example , slot_type_categories (lists of required column types: date, categorical, nu-meric, geographic), and recommended_for_slots 

(complexity-category tags used by Stage 2 to score compatibility). 

C.3 Stage 1: Six-Slot Query Planning 

Stage 1 issues a single LLM call per sub-schema and produces a six-slot plan (plan.json ). Each slot specifies the analytical work that one of the six generated queries must do. 

Slot fields. Each slot in plan.json contains: • slot_number (1–6) and length_slot :

SHORT (15–25 words), SHORT-MEDIUM (30– 50), MEDIUM (50–80), MEDIUM-LONG (80–120), 

LONG (120–150), VERY-LONG (150–175). • complexity_categories : subset of the eight analytical categories (Aggregation, Relation-ship/Network, Date/Time, etc.). • sds_target : per-dimension targets D1–D7, ex-pressed on a 0–2 scale (planning scale; scoring uses 0–5). • columns_to_use : mapping of table → column list for this slot, drawn from the sub-schema. • analytical_intent : one-sentence description of the computation the slot must express. • required_formula_patterns : a list of structural keywords (e.g., two_stage_drill ,

cohort_retention ) used by Stage 2 to score formula compatibility. 

Global coverage constraint. The Stage 1 prompt enforces minimum category coverage across all six slots: Aggregation in all six; Relationship/Network in at least five; Classification/Grouping in at least four; Date/Time in at least three; Ranking/Top-N, Statistical, and Comparison each in at least two. A distribution_check field in the plan output surfaces whether these constraints were satisfied; plans that violate any constraint are discarded and regenerated. 

C.4 Stage 2: Formula Retrieval (Programmatic) 

Stage 2 is a pure Python program with no LLM calls. For each slot it scores every formula in 

SPIDER2_FORMULA_BOOK.json on two axes and assembles an ordered list: 1. Category overlap. The Jaccard similarity be-tween the slot’s complexity_categories and the formula’s complexity_categories .2. Pattern compatibility. Whether the slot’s 

required_formula_patterns intersect the formula’s macro-pattern keywords, and whether the formula’s required column types (slot_type_categories ) are satisfied by the columns available in the slot’s sub-schema. Up to five formulas per slot are retained (samples_per_category = 5). A global no-repeat constraint ensures no formula is as-signed to more than one slot within a sub-schema. The output is formula_examples.json ,keyed by slot, with each entry containing 

formula_id , macro_pattern , micro_patterns ,and filled_example (SDS scores are stripped to prevent the generator from over-fitting to them). 

C.5 Stage 3: Per-Slot Query Generation 

Given the slot plan and the retrieved formula examples, Stage 3 issues one LLM call per slot. The prompt provides: the slot’s sub-schema (column names, types, descriptions, enum vocabularies), the analytical_intent and 

required_formula_patterns from the plan, and the formula macro-pattern plus a filled example from Stage 2. The filled example is instantiated on a different schema to provide structural guidance without leaking DevRev vocabulary. 

Per-slot output (slot_queries_from_examples.json ). Each slot record contains: • column_mapping : each column labeled with its role: GROUPING , METRIC , FILTER , OUTPUT , or 

ANCHOR .• query_plan : a 2–5 sentence structural blueprint (“Scope: . . . ; Stage 1: . . . ; Stage 2: . . . ; Output: . . . ”). • query : the natural-language query text, bounded by the slot’s target word count. • formula_ids_used , categories_used ,

reasoning : provenance metadata. All generated queries are executed against the De-vRev database; queries returning empty results are discarded (7.6% rejection rate across the full cor-pus). 

C.6 Stage 4: Persona Rewriting 

Stage 4 issues a single LLM call that receives all six slot queries simultaneously and rewrites each into a distinct business-role voice. The six personas are: VP of Sales , Customer Success Manager ,

Product Manager , Operations Analyst , Finance / Revenue Ops , and C-Suite Executive . Each persona is used exactly once per sub-schema. 

Key rewriting rules. 

• Enum-value ban. Categorical field values (sta-tus enums, segment labels, milestone names) must not appear in the rewritten query. The rewrite names the dimension (“by account tier”) without listing its values. The only exception is a single-value filter that is the analytical focus of the entire query. • Structural diversity. Opening styles are varied across slots: question, imperative, declarative need, narrative framing, concern. • Preservation invariants. The following prop-erties are protected and must survive rewriting: exact date ranges and temporal grain; every sta-tus and eligibility filter; all numeric thresholds; aggregation grain (per-account vs. per-category); ratio denominators; sort order and ranking direc-tion. • Interpretability test. A rewrite is valid if and only if the original query remains a natural in-terpretation of it: a SQL agent reading only the rewrite must be able to produce the same SQL as the original. Self-check protocol. The Stage 4 prompt in-cludes a mandatory 15-item pre-return checklist covering: enum-value scan, time-window preser-vation, temporal grain, filter completeness, aggre-gation grain, ratio denominator, sort direction, and absence of SQL keywords in the output. 

Quality audit. An automated column-plan au-dit verifies that 100% of rewritten queries pass their SDS slot threshold (as scored by the SDS rubric in Section C.7) and that 76.9% achieve ex-act column-plan match (i.e., the query implies the same columns as the original plan without addi-tional retrieval). 

C.7 SDS Scoring Rubric (Full Specification) 

The Semantic Depth Score (SDS) is assigned by an LLM judge that receives the natural-language query text only (no SQL and no schema). Each of the seven dimensions is scored 0–5 (max total: 35). The rubric includes calibration examples anchoring Easy ≈ 7, Medium ≈ 15 –18, and Hard ≈ 29 . The complete rubric is reproduced below. 

Score-5 anchor examples (abbreviated). 

• D1 = 5: “Groups whose ticket volume grew by at least 10% quarter-over-quarter, excluding any group that also appears in the bottom quartile for enhancement-linked revenue.” • D4 = 5: “A CTE chain that (1) computes quar-terly totals, (2) computes per-partner normalized counts, (3) intersects both ranked lists, and (4) breaks results by month within each quarter.” • D7 = 5: “Two window functions + correlated subquery + conditional aggregation + three-CTE chain.” 

Scoring guidance. The rubric instructs the scorer to award scores of 3 or higher only when specific phrases in the query text justify the level, and to resist inflating scores for query length or business jargon. The cross-check step compares the can-didate total against the Hard anchor ( ≈ 29 ) and requires downward revision if the candidate total exceeds it for a query of lesser complexity. 

C.8 LLM Judge Rubric 

The LLM judge used for evaluation (Section 5.1) scores each generated SQL on a maximum of 50 points across four criteria. 

Evaluation protocol. The judge receives: the natural-language question, the generated SQL, the ground-truth schema (DDL), and a preview of the first five result rows. Before scoring, the judge com-pletes a mandatory checklist: entity scope, filter conditions, grouping keys, metric definitions, rank-ing directives, output columns, multi-stage struc-ture, and comparison direction. Each checklist item is mapped to a specific SQL clause before a score is assigned. A J1 hard cap is applied if one or more checklist items are entirely unaddressed. 

C.9 Annotated Pipeline Examples 

Table 7 traces one instance (account_enhancement_ticket_1 , Slot 6) through all four stages to illustrate how a formula macro-pattern becomes a final persona-rewritten query. 

D Additional Experimental Results 

D.1 Results on DevRev: Detailed Breakdown 

The system attains 91.7% Answer Correctness (825/900 queries) on the full 900-query DevRev benchmark, and 91.5% (745/814) on the high-SDS subset (SDS ≥ 11 , 90.4% of the corpus). Table 8 shows the cumulative contribution of each compo-nent. Table 9 stratifies accuracy by queries that score at the maximum (5) on each SDS dimension. Fail-ures on high-D2 queries (derived metrics) impli-cate the generator’s reasoning over computed val-ues; failures on high-D6 queries (temporal preci-sion) implicate date-function handling; failures on high-D7 queries (advanced constructs including 

LATERAL FLATTEN ) implicate the nested-type ma-chinery the architecture is designed for. 

D.2 SDS-Stratified Per-Bucket Breakdown 

Table 10 reports mean judge score for all four systems across the three SDS buckets. All three baselines decline monotonically as SDS increases, while our system remains within a 2.8 pp range. 

D.3 Query Complexity Categories 

Each generated query is tagged with one or more 

complexity categories that identify the kind of ana-lytical reasoning the query requires. We distinguish two orthogonal axes. 

Eight analytical categories capture the type of computation the SQL must perform: Dim. Name What a score of 5 requires 

D1 Eligibility Rank/percentile-based, anti-join, or recursive set-logic eligibility (e.g., groups whose ticket volume grew by at least 10% QoQ, excluding the bottom quartile for enhancement-linked revenue). D2 Derived Metric Cascaded derivation: a formula whose inputs are themselves derived metrics (e.g., percentile rank of a ratio, running total expressed as % of grand total, or ratio of two computed rates). D3 Population Scoping Nested scope: the eligible population is defined by the result of an inner aggregation or ranking (e.g., enhancements whose linked ticket count places them in the top decile of ticket volume). D4 Multi-Stage Logic Three or more interdependent CTEs, a recursive structure, or multiple fully independent aggregations joined together (e.g., a CTE chain that computes quarterly totals, per-partner normalized counts, intersects both ranked lists, then breaks by month). D5 Output Precision Output broken out across two grouping dimensions simultaneously, with computed fields for each cell (e.g., for each group × month combination, show ticket count and revenue share). D6 Temporal Precision Multi-dimensional temporal logic: cross-period comparison with sub-period breakdown on both axes, or entity-relative windows combined with calendar periods. D7 SQL Construct Diversity Full complexity: three or more of the following required together: recursive CTE, multiple window functions, set operations ( UNION /INTERSECT ), anti-join, conditional aggregation, dependent CTE chain. 

Table 5: SDS dimension definitions at maximum score (5/5). Lower scores are proportionally less complex; a score of 0 indicates the dimension is entirely absent from the query. 

Dim. Name Max Focus 

J1 Semantic Completeness 35 Does the SQL answer every part of the question? Hard caps apply if one or more requirements are entirely absent. J2 Analytical Logic 8 Are CTEs, window functions, percentage-change formulas, and multi-stage logic correctly implemented? J3 Tables & Joins 4 Are the correct tables joined via the right keys without spurious cross-products? J4 Output Precision 3 Are the output columns correctly named, typed, and ordered as the question specifies? 

Total 50 

Table 6: LLM judge rubric dimensions (max 50 points). Answer Correctness (AC) is the binary verdict derived from this rubric: AC = 1 if and only if the judge’s overall assessment is that the SQL semantically answers the question. 

• Aggregation. Summarizing raw records into a computed value. The most universal category; enforced in all six generation slots. • Statistical. Distributional statistics (std devia-tion, median, percentile, growth rate). In DevRev, this covers CSAT score distributions, ticket-volume trends, and resolution-time dispersion. • Comparison. Contrasting two groups, two time periods, or two metrics (“Q1 vs. Q2,” “Customer vs. Partner tickets,” “before and after escalation”). Requires conditional aggregation or a self-join, and is a primary source of multi-stage logic. • Classification/Grouping. Partitioning entities into labeled groups defined by a categorical col-umn or a stated rule. Tests GROUP BY on the right dimension, including over enum-constrained string columns. • Ranking/Top N. Top or bottom N entities by a metric, or the N th-ranked entity. Requires ORDER BY ... LIMIT or a window function. • Date/Time. A specific temporal scope, time difference, or date arithmetic (rolling windows, quarter boundaries, age-in-days). Tests transla-tion of natural-language time expressions into correct Snowflake date functions. • Relationship/Network. Multi-hop traversal: linking tickets to enhancements via the typed-link graph, joining accounts to their opportunities and conversations, navigating group-to-vista hi-erarchies. Characteristic of the work-item graph regime. • Geographic. Filtering or grouping by a geo-graphic dimension. Applied only when a ge-ographic column is present in the sub-schema; Stage Artifact Stage 1 (Plan) Slot: 6 Length: VERY-LONG (150–175 words) 

Categories: Aggregation, Relationship/Network, Date/Time, Ranking/Top-N, Statistical 

SDS targets: D1=2, D2=2, D3=2, D4=2, D5=2, D6=2, D7=2 (total 14 on planning scale) 

Intent: Identify the top enhancements by Q2 volume normalized by active-partner count, restricted to groups with ≥25% QoQ growth, with per-account breakdowns. 

Stage 2 (Formula) Formula assigned: F_002 (find-winner-then-re-aggregate at multiple granularities) 

Macro pattern: Stage 1 finds the winning dimension by a metric; Stage 2 re-aggregates for that winner at finer granularity; Stage 3 applies a unit conversion and outputs the maximum per granularity. 

Stage 3 (Raw query) “For Q1 and Q2 2025, only include ticket ownership groups where the number of tickets rose by at least 25% from Q1 to Q2. Within those qualifying groups, rank enhancements by their Q2 volume of linked tickets normalized by the number of active partners selling into the associated accounts, and return the top 10. For each of those top enhancements, report the theme they belong to and include the enhancement’s short description. Also break the results out by account name: for each linked account, show its Q2 rate of tickets per $1M of annual revenue and its Q2 average of tickets per active partner for that enhancement. Order by the largest Q2 normalized volume; within each enhancement list accounts from highest to lowest tickets per $1M revenue.” 

Stage 4 (Persona rewrite) Persona: C-Suite Executive “For the exec briefing and midyear planning (using tickets’ creation dates owned by each group), compare Q1 2025 to Q2 2025 and only keep ticket ownership groups whose ticket count rose by at least 25% from Q1 to Q2. Within those qualifying groups, rank enhancements by their Q2 linked-ticket volume normalized by the number of active partners selling into the associated accounts, and return the top 10 enhancements. For each, include its theme and a short description, and break results out by linked account: for each account show its Q2 rate of tickets per $1M of annual revenue and its Q2 average of tickets per active partner for that enhancement. Order enhancements by the largest Q2 normalized volume; within each enhancement list accounts from highest to lowest by tickets per $1M revenue.” 

Table 7: End-to-end pipeline trace for one instance ( account_enhancement_ticket_1 , Slot 6), showing how the Stage 1 plan and Stage 2 formula combine to produce the Stage 3 raw query, which Stage 4 then rewrites into a C-Suite voice. 

System AC (%) 

Single-pass generator (no pipeline) 38.2 

+ schema selection (Stages 1–3) 46.6 

+ error taxonomy 86.8 

+ Stage 4 expansion (full system) 91.7 

Table 8: Cumulative component contribution on DevRev-900. 

rare in DevRev (a B2B SaaS product without location-bearing entities). 

Domain categories identify which DevRev business objects the query is primarily about: Ticket/Support, Account/Customer, Opportu-nity/Sales, Product/Feature, Meeting/Engagement, Dashboard, and so on. These categories are not enforced by the generation pipeline; they emerge from the table combination selected, and are useful for downstream per-domain failure-rate analysis. 

Enforced coverage. Stage 1 enforces minimum analytical-category coverage across the six slots of every sub-schema: Aggregation in all six; Relationship/Network in at least five; Classifica-tion/Grouping in at least four; Date/Time in at least 

Subset Queries AC (%) 

All 900 91.7 D1 = 5 (eligibility) 277 93.5 D2 = 5 (derived metric) 26 84.6 D3 = 5 (population) 122 90.2 D4 = 5 (multi-stage) 136 94.9 D5 = 5 (output precision) 96 95.8 D6 = 5 (temporal) 22 90.9 D7 = 5 (constructs) 24 91.7 

Table 9: DevRev accuracy stratified by SDS dimension at maximum score (5 on the 0–5 scale). 

three; Ranking/Top-N, Statistical, and Comparison each in at least two. This prevents the corpus from collapsing to a single query type and ensures ev-ery table combination exercises the full range of analytical reasoning patterns. Table 11 shows the per-query distribution across the final 900-query corpus. 

D.4 Dataset Statistics 

The pipeline produces 900 execution-verified natural-language queries over 14 schema combina-tions. Table 12 compares corpus-level statistics for DevRev and Spider 2.0-Snow. SDS Bucket Ours APEX-SQL FlexSQL ReFoRCE                      

> Easy ( ≤12, n=139) 80.1 56.5 55.1 48.5 Medium (13–18, n=250) 80.9 53.7 48.9 36.2 Hard ( >18, n=511) 78.1 41.4 42.8 27.2 Drop (Easy →Hard) 2.0 15.1 12.3 21.3

Table 10: Mean judge score (% of 50-point max) by SDS bucket. Corresponding Answer Correctness figures for our system: 89.2% (Easy), 94.4% (Medium), 91.0% (Hard).                   

> Category Queries (of 900) %
> Aggregation 900 100.0 Relationship/Network 719 79.9 Classification/Grouping 705 78.3 Date/Time 594 66.0 Ranking/Top N 323 35.9 Statistical 292 32.4 Comparison 205 22.8 Geographic 10.1

Table 11: Per-query complexity-category distribution. Each query is assigned multiple categories; percentages reflect the fraction of all 900 queries that include each category. Aggregation is universal by design.             

> Metric DevRev Spider 2.0-Snow
> Queries 900 547 Mean SDS 19.15 20.20 Avg. word count 29.3 54.0 Self-BLEU 0.039 0.023 Execution pass rate 92.4% N/A

Table 12: Corpus-level statistics for DevRev and Spi-der 2.0-Snow. Self-BLEU measures n-gram redundancy (lower is more diverse). 

When scored on the same 0–5-per-dimension rubric, DevRev and Spider 2.0-Snow exhibit com-parable analytical depth (mean SDS 19.15 vs. 20.20). The corpora differ in their difficulty profile 

rather than their average depth. Spider 2.0-Snow concentrates difficulty in SQL-construct diversity (D7: 3.67 vs. 2.57) and multi-stage pipeline logic (D4: 3.12 vs. 2.72), reflecting its cross-domain schema variety and advanced SQL patterns. De-vRev concentrates difficulty in derived-metric con-struction (D2: 2.92 vs. 2.65), output-specification precision (D5: 3.45 vs. 2.88), and temporal reason-ing (D6: 1.51 vs. 1.33), reflecting the CRM domain where percentage-change comparisons and rolling-window time filters are standard. The self-BLEU of 0.039 confirms lexical diversity is maintained despite all queries sharing DevRev domain vocabu-lary. 

D.5 Results on Spider 2.0-Snow 

Spider 2.0-Snow comprises 547 queries, but ground-truth SQL and expected results are pub-licly available for only 120 of them (Lei et al., 2025). We evaluate our system on this 120-query gold-answer subset and report that score alongside the full-leaderboard figures for the three baselines. Table 13 shows the results.                    

> System EX (%) Eval.
> Genloop Sentinel v2 Pro 96.70 547 Native mini 96.53 547 QUVI-3 + Gemini-3-pro 94.15 547 TCDataAgent-SQL 93.97 547
> . . .
> Ours (single gen.) 85.8 (103/120) 120 †
> . . .
> APEX-SQL (Cao et al., 2026b) 73.13 547 FlexSQL (Pham et al., 2025) 65.45 547 ReFoRCE (Deng et al., 2025b) 62.89 547

Table 13: Spider 2.0-Snow leaderboard snapshot (May 2026). EX = Execution Accuracy. Leaderboard en-tries for all other systems are evaluated on the full 547 queries. †Our system is evaluated on the 120-query gold-answer subset for which ground-truth SQL and re-sults are publicly available; the score is therefore not di-rectly comparable to full-leaderboard figures and is not submitted to the ranked leaderboard. Baseline entries are each system’s best leaderboard submission: APEX-SQL (rank 17, default config), FlexSQL (rank 22, with 

gpt-oss-120b ), ReFoRCE (rank 24, with o3 ). 

Our system answers 103 of 120 gold-answer queries correctly (85.8% EX). Because this evalua-tion covers only the publicly released subset rather than the full 547-query leaderboard set, the fig-ure is indicative rather than directly ranked. The three baselines that score 27–37% AC on DevRev score 63–73% EX on Spider 2.0-Snow’s full leader-board, a regime they were optimised for. This con-trast confirms that the performance gap on DevRev reflects the structural properties of the enterprise nested-schema regime rather than a general weak-ness in those systems. Our 85.8% on the gold subset suggests the architecture is competitive on the flat-table, large-scale schema-linking regime while using one generation pass per query. 

D.6 Cost Comparison 

Table 14 compares LLM API calls and database executions per query across all four systems, mea-sured on the same stratified 50-query cost sample. Counts are medians; LLM calls are measured from API-call logs and DB executions from per-query execution traces.                

> System AC (%) LLM/q DB/q
> APEX-SQL 27.2 16 18 FlexSQL 37.1 46 20 ReFoRCE 29.1 44
> Ours 91.7 24
> Table 14: Cost comparison on the 50-query stratified sample (median values). LLM/q = LLM API calls per query; DB/q = database executions per query. AC = An-swer Correctness on the full 900-query DevRev bench-mark.

Our system matches ReFoRCE on DB execu-tions (median 4) while achieving 3 × higher AC, and uses 8–23 × fewer LLM calls than APEX-SQL and FlexSQL. The multi-candidate baselines in-cur high DB costs because each of their K candi-date plans independently probes the schema with exploratory sub-queries before generating final SQL; our architecture avoids this through a sin-gle schema-selection pass guided by the metadata-enriched retriever. APEX-SQL’s hypothesis-verification loop issues a mean of 16 LLM calls per query (median 16), each paired with a DB execution to verify or refute a schema hypothesis, yielding 18 DB calls in the median case. FlexSQL generates 4 candidate plans, each of which independently executes sub-queries per involved table to ground column values before SQL synthesis, resulting in a median of 20 DB executions and 46 LLM calls. ReFoRCE’s self-refinement loop (up to 5 iterations) issues one LLM call and one DB execution per iteration, matching our system’s DB footprint despite using 2 × more LLM calls. Our system issues a single LLM call for SQL generation; additional calls occur only when the pre-execution checker or execution failure triggers a repair round (median 2 calls total, max 3 in this sample).