Title: Membership Inference via Pairwise Likelihood Ratios

URL Source: https://arxiv.org/pdf/2609.12367v1

Published Time: Mon, 14 Sep 2026 00:36:41 GMT

Number of Pages: 33

Markdown Content:
MEMBERSHIP INFERENCE VIA PAIRWISE LIKELIHOOD RATIOS 

BY SHENGJIE NIU 1,a , Z EBIN YUN 2,d , Y EHENG GE1,b AND JIAN HUANG 1,c    

> 1The Hong Kong Polytechnic University, Hong Kong, China, ashengjie.niu@connect.polyu.hk, byeheng.ge@polyu.edu.hk,
> cj.huang@polyu.edu.hk
> 2Tel Aviv University, Israel , dzebinyun@mail.tau.ac.il

Membership inference attacks (MIAs) are the standard tool for auditing the privacy risks of machine learning models. Given a query point, an MIA aims to determine whether that point was used to train the target model. In practice, such inference must rely on the statistical signals exposed by the model’s outputs, such as confidence scores, logits, and intermediate feature representations. However, existing methods often fail to efficiently summa-rize and combine these statistical signals. To address this limitation, we pro-pose Pairwise Likelihood MIA (PL-MIA), a unified method that combines a Gaussian likelihood-ratio (GLR) statistic with population calibration and the Cauchy combination test. We characterize theoretically how the GLR re-tains variance-contraction signals and establish conditions under which pop-ulation calibration and Cauchy combination improve attack power. We obtain 

p-values from pairwise comparisons between the query point and reference points not used for training, and aggregate these continuous signals using the Cauchy combination test. This preserves the evidence strength that is dis-carded when each pairwise comparison is reduced to a binary vote. Extensive experiments demonstrate that PL-MIA outperforms strong baselines, improv-ing the true positive rate (TPR) by over 25% in the critical low-false-positive regime, corroborating our theoretical findings. These results demonstrate how statistical principles can turn noisy model outputs into more powerful, cali-brated, and reproducible evidence for membership privacy auditing. 

1. Introduction. Machine-learning models trained on individual-level data may reveal whether a particular record was included in their training sets. Such membership information can itself be sensitive. For example, identifying an individual as a member of a biomedical, genomic, or financial dataset may disclose private information about that individual or their participation in a sensitive study (Homer et al., 2008; Backes et al., 2016; Hernandez Aros et al., 2024). Membership inference attacks (MIAs) (Shokri et al., 2017) provide a data-driven tool for auditing this risk: given black-box access to a trained model and a query point, an au-ditor attempts to determine whether that point was used for training. A central practical ques-tion is therefore whether an auditor can make reliable membership inferences while keeping false accusations of non-members rare. This requirement makes the low false-positive-rate (low-FPR) regime particularly important. An attack with satisfactory average discrimination ability may still be unsuitable for privacy auditing if most of its positive findings are unsup-ported. Therefore, effective MIAs must achieve high power in this low-FPR regime. However, this objective is challenging in practical black-box settings, where membership can only be inferred from model outputs that reflect not only membership status but also intrinsic proper-ties of the query point. For instance, an intrinsically easy point may receive high confidence even when it was not used for training, whereas a difficult training point may receive rel-atively low confidence. A credible audit must therefore separate membership-related signal from query-level heterogeneity. 

Keywords and phrases. Membership Inference Attack, Hypothesis Test, Cauchy Combination Test. 

1arXiv:2609.12367v1 [stat.ML] 11 Sep 2026 2

Membership inference is fundamentally a statistical decision problem: we must effectively extract membership information from limited and heterogeneous model outputs, reliably ag-gregate diverse statistical evidence, and translate it into sound membership decisions. This objective motivates the development of principled statistical methodologies, each addressing a distinct part of the pipeline. First, likelihood theory guides the construction of efficient test statistics that extract membership signal from noisy model outputs, while population calibra-tion separates the membership-related component of the signal from query-level heterogene-ity arising from query difficulty. Second, combination tests aggregate multiple, potentially dependent statistical signals into a single membership score, maximizing the information re-tained in the final statistic. Third, statistical uncertainty quantification converts the resulting scores into decisions with controlled FPRs, a particularly critical capability in the extreme low-FPR regime. While these three components form a coherent framework, the signal ag-gregation via combination tests and decision-making with rigorous FPR control constitute the primary methodological contributions of our article. In particular, existing MIAs typi-cally determine decision thresholds on additional held-out data (Carlini et al., 2022), which both impose extra data requirements and introduce variability into threshold estimation. We therefore derive decision thresholds analytically, without held-out data, while keeping the realized FPR at or below its nominal level across heterogeneous queries. This statistical perspective also reveals the limitations of existing strong MIAs. Recent methods increasingly formulate membership inference as a hypothesis-testing problem (Car-lini et al., 2022). For each query point, reference models are used to estimate the distributions of model outputs when the point is included in or excluded from the training set, referred to as the IN and OUT distributions, respectively (Sablayrolles et al., 2019; Carlini et al., 2022; Wat-son et al., 2021; Zarifzadeh, Liu and Shokri, 2024). LiRA (Carlini et al., 2022) models these distributions separately and uses a Gaussian likelihood ratio (GLR) as a test statistic to ex-tract a fine-grained membership signal. This statistic exploits both the shift in predictive con-fidence and the empirical contraction of predictive variance observed for training members. However, directly applying a global threshold to this statistic does not account for query-level heterogeneity. RMIA (Zarifzadeh, Liu and Shokri, 2024) addresses query-level heterogeneity through population calibration by comparing the query point with randomly sampled popu-lation points, but its test statistic does not explicitly exploit the variance-contraction signal. Moreover, its population calibration reduces each pairwise comparison to a binary indica-tor, treating a marginal comparison and an overwhelmingly strong comparison as equally informative. Thus, existing methods do not simultaneously extract the available membership signal, adjust for query-level heterogeneity, and preserve the strength of pairwise evidence. Motivated by this gap, we propose a unified framework that decomposes MIAs into two components: (1) a pointwise statistic that extracts membership signal for each query point, and (2) a population calibration strategy that normalizes the query point’s statistic using pop-ulation data to reduce intrinsic query-level heterogeneity. Building on this unified framework, we propose Pairwise Likelihood MIA ( PL-MIA ). First , we adopt the GLR as the pointwise statistic. We theoretically show that the GLR explicitly captures the variance-contraction sig-nal and therefore yields greater separation between the member and non-member statistic dis-tributions than the BLR. Second , we design an improved population calibration strategy based on continuous pairwise evidence. For each pairwise comparison between the query point and a randomly sampled population point, we convert the pairwise difference into a continuous 

> p

-value and then aggregate multiple p-values using the Cauchy combination rule (Liu and Xie, 2020). In contrast to the threshold-based binary evidence (Zarifzadeh, Liu and Shokri, 2024), continuous p-values preserve the strength of each pairwise comparison. Meanwhile, the Cauchy combination rule improves sensitivity to subtle membership signals (Liu et al., 2019) and enables an analytic decision threshold at a target FPR. Theoretically , we show that PAIRWISE LIKELIHOOD MIA 3

population calibration centers non-member scores around their corresponding neutral base-lines, providing a common scale for thresholding membership evidence. We further derive a unified expression for attack power across strong attacks, showing that PL-MIA improves over LiRA and RMIA by simultaneously exploiting the variance-contraction signal and re-ducing query-level heterogeneity. The contributions of this study are summarized as follows: (1) We propose a unified frame-work that characterizes MIAs as the composition of a pointwise statistic and a population cal-ibration strategy. (2) We propose Pairwise Likelihood MIA (PL-MIA), which integrates the Gaussian likelihood ratio, population calibration, and Cauchy combination into a statistically principled attack. (3) We theoretically characterize the advantage of PL-MIA over strong baselines by simultaneously exploiting variance contraction and reducing query-level hetero-geneity. (4) Extensive experiments demonstrate that PL-MIA consistently improves attack performance across datasets and achieves substantial gains in the critical low-FPR regime. 

2. Preliminary. Throughout the paper, we denote the target model, parameterized by θt,as fθt . Membership Inference Attacks (MIAs) (Shokri et al., 2017) aim to determine whether a query point was used to train the target model. We formalize MIA through a security game between a challenger (model owner) and an adversary (privacy auditor) (Yeom et al., 2018; Jayaraman et al., 2020; Carlini et al., 2022). DEFINITION 2.1 (Membership Inference Game). Let π be the underlying data distri-bution, T the challenger’s training algorithm, and A the adversary’s membership inference attack. The MIA game proceeds as follows: 1. The challenger samples a training dataset D ∼ πn and trains the target model fθt ← T (D).2. The challenger samples a membership bit b ∼ Bernoulli(1 /2) . If b = 0 , it samples a fresh query point q ∼ π subject to q / ∈ D ; if b = 1 , it samples q uniformly at random from D.3. The challenger provides the query point q and access to the target model fθt to the adver-sary, according to the specified threat model. 4. The adversary outputs a membership prediction ˆb ← A (fθt , q ) and wins the game if ˆb = b.The membership bit b determines the membership status of the query point: when b = 1 ,

q is sampled from the target model’s training set D and is therefore a member; when b = 0 ,

q is sampled from the underlying distribution π but excluded from D and is therefore a non-member. The choice b ∼ Bernoulli(1 /2) is a standard convention that assigns equal prior probability to the member and non-member cases (Jayaraman et al., 2020). 

Threat Model. A threat model specifies which information is available to the adversary for constructing statistical evidence of membership. Following prior work (Watson et al., 2021; Ye et al., 2022; Bertran et al., 2023; Zarifzadeh, Liu and Shokri, 2024), we consider a worst-case black-box threat model. The adversary has access to the underlying data distribution π

and can replicate the target model’s architecture and training algorithm (Zhu et al., 2025), as in settings where both the adversary and challenger use the same machine learning-as-a-service provider (Salem et al., 2018). However, the adversary has no access to the target model’s training set or trained parameters and is limited to querying the target model and observing its outputs. 

Membership Score. Inputting a query point q into a target model, a MIA assigns a mem-bership score Score( q), where a larger score indicates stronger evidence of membership. The adversary converts this score into a membership prediction by thresholding it at a decision threshold τα (Zarifzadeh, Liu and Shokri, 2024): (1) ˆb = A(fθt , q ) = I(Score( q) ≥ τα),4

where I(·) is the indicator function and τα is chosen to target a false positive rate (FPR) of 

α. Since practical audits often require near-zero false accusations, we prioritize attacks that achieve high TPR under stringent FPR constraints. For existing MIAs, τα is typically esti-mated using held-out data or derived analytically (Carlini et al., 2022). In PL-MIA, pairwise 

p-values are aggregated using the Cauchy combination rule, yielding an analytic decision threshold for nominal FPR control without requiring additional held-out data. 

Statistical Hypothesis. We formalize the MIA test by defining two distributions over model parameters: Θ(q)IN and Θ(q)OUT . For a given query point q, Θ(q)IN denotes the distribution of parameters of reference models trained on datasets that include q, whereas Θ(q)OUT denotes the analogous distribution when q is excluded . The null and alternative hypotheses are (2) H0 : θt ∼ Θ(q)OUT versus H1 : θt ∼ Θ(q)IN .

Formally, a model parameter θt is drawn from the IN distribution if trained on a dataset containing the query point q, and from the OUT distribution otherwise. By the Neyman-Pearson lemma (Neyman and Pearson, 1933), the most powerful test can be constructed using the likelihood ratio (LR): (3) Λ∗(θt, q ) = pdf( θt|Θ(q)IN )pdf( θt|Θ(q)OUT ) ,

where pdf( θt|Θ(q)b ) denotes the density of observing θt under hypothesis b ∈ { IN , OUT }.When q is a training member, θt is typically more likely under Θ(q)IN than under Θ(q)OUT , leading to a larger value of Λ∗(θt, q ).Despite this optimality, directly modeling Θ(q)IN and Θ(q)OUT is generally intractable in the black-box setting, where the target model parameters θt are not observable. Following Carlini et al. (2022), we therefore construct an observable statistic from the target model output for a query point q = ( xq, y q):

conf (q) 

> t

= ϕ(fθt (xq)[ yq]) ,

where fθt (xq)[ yq], referred to as the true label confidence (TLC) 1, denotes the probability assigned by the target model to the true label yq for the input xq , and ϕ(p) = log p 

> 1−p

is the logit transform. Motivated by empirical evidence that the logit-scaled TLC values are well approximated by Gaussian distributions (Carlini et al., 2022), we introduce the following as-sumption to make the hypothesis-testing problem analytically tractable. We present normality diagnostics and evaluate robustness to violations of this Gaussian assumption in Section S2 of the Supplementary Material. ASSUMPTION 2.2 (Gaussian Distribution). For any query point q, the logit-transformed confidence scores follow Gaussian distributions conditioned on membership status: (4) conf (q)IN ∼ N 



μ(q)in , (σ(q)in )2

, conf (q)OUT ∼ N 



μ(q)out , (σ(q)out )2

,

where conf (q)IN and conf (q)OUT denote the induced distributions of ϕ(fθ(xq)[ yq]) when θ ∼ Θ(q)IN 

and θ ∼ Θ(q)OUT , respectively.      

> 1For a classification model, the input xqrepresents the observed features, and the model outputs a proba-bility vector over possible classes. In this work, we consider the probability assigned to the true class yqas the observable signal for membership inference under the black-box setting. PAIRWISE LIKELIHOOD MIA

5

This assumption shifts the distinction between the IN and OUT model populations onto their output distributions for the query point q, which are characterized by three quantities. (1) the normalized mean shift η(q) := ( μ(q)in − μ(q)out )/σ (q)out > 0, representing the normalized increase in predictive confidence of members; (2) the contraction ratio ρ(q) := σ(q)in /σ (q)out ≤ 1,capturing the reduced predictive uncertainty of members; and (3) the variation coefficient 

ξ(q) := [1 − sigmoid( μ(q)out )] σ(q)out , representing the leading-order coefficient of variation of the raw TLC under H0. We restrict attention to tests whose evidence depends on θt only through the statistic ϕ(fθt (xq)[ yq]) .Within this class of tests, membership testing based on θt reduces to testing the induced observable statistic conf (q) 

> t

. Therefore, the likelihood ratio in Equation 3 becomes (5) Λ( θt, q ) = pdf(conf (q) 

> t

|Θ(q)IN )pdf(conf (q) 

> t

|Θ(q)OUT ) .

The LR statistic is based on the empirical observation that models trained with a query point 

q tend to assign higher confidence to its true label, whereas models trained without q tend to assign lower confidence to the same point. The statistic conf (q) 

> t

is calculated from the model output and depends on the underlying parameter θt. It serves as an observable surrogate for parameter-level membership evidence in the black-box setting. 

3. Methodology. 

3.1. A Unified Framework of MIA. We present a unified framework that characterizes MIAs as the composition of two components: a pointwise statistic and a population calibra-tion strategy . This decomposition clarifies differences among prior attacks and guides our attack design. We summarize recent strong attacks in Table 1.                                      

> TABLE 1
> Comparison of representative MIA methods under the unified framework. We compare Attack-R and Attack-P (Ye et al., 2022), LiRA (Carlini et al., 2022), RMIA (Zarifzadeh, Liu and Shokri, 2024), and PL-MIA in terms of their pointwise statistics and population calibration strategies; for attacks with population calibration, the latter is further decomposed into evidence generation and evidence aggregation.
> Attacks Attack-R Attack-P LiRA RMIA PL-MIA (OURS) Pointwise Statistic LOSS LOSS Gaussian LR Bayes LR Gaussian LR
> Estimates IN Dist. ✘✘✔✔✔
> Exploits Contraction ( ρ)✘✘✔✘✔
> Population Calibration ✘✔✘✔✔
> Evidence Generation -Binary indicator -Binary indicator Continuous p-value
> Evidence Aggregation -Mean -Mean Cauchy combination

Pointwise Statistic. In our framework, each MIA first constructs a scalar pointwise statistic 

φ(d) that extracts the individual membership signal for a query point d. A larger value of 

φ(d) indicates stronger evidence for membership. The pointwise statistic serves as the fun-damental building block of an MIA, and different attacks correspond to different choices of 

φ(d). Recent strong attacks construct φ(d) through hypothesis testing, using multiple refer-ence models to extract finer-grained membership signals (Carlini et al., 2022; Watson et al., 2021; Zarifzadeh, Liu and Shokri, 2024). As discussed in Section 2, the most powerful tests are based on likelihood ratios (LRs); accordingly, we focus on attacks that use LR estimators as the pointwise statistic (Carlini et al., 2022; Zarifzadeh, Liu and Shokri, 2024): 6

• LiRA (Carlini et al., 2022) uses the Gaussian LR: φ(d)GLR = log pdf(conf (d)       

> t|N (μ(d)in ,(σ(d)in )2)) pdf(conf (d)
> t|N (μ(d)out ,(σ(d)out )2))

. It estimates the LR by fitting Gaussian models to the conf (d)IN and conf (d)OUT distributions. • RMIA (Zarifzadeh, Liu and Shokri, 2024) employs the Bayes LR: φ(d)BLR := log( fθt (xd)[ yd]¯c(d) ),where ¯c(d) := E[fθ(xd)[ yd]] is the average raw TLC over all reference models. 

Population Calibration Strategy. This strategy compares the statistics of the query point with those of population points to neutralize intrinsic query-level variability. It transforms the pointwise statistic φ(d) into the membership score used in Equation 1 through two steps: (i) Evidence Generation , which forms membership evidence from each pairwise comparison between the query point and a randomly sampled population point, and (ii) Evidence Aggre-gation , which combines the generated evidence from multiple pairwise comparisons into a single membership score. • Without Calibration (Carlini et al., 2022): The membership score is set to the statis-tic itself, Score LiRA (q) := φ(q)GLR , and is compared to a global decision threshold to infer membership. As noted by Zarifzadeh, Liu and Shokri, this approach does not calibrate against population data. • With Calibration (Zarifzadeh, Liu and Shokri, 2024): The query point’s statistic φ(q)BLR is calibrated against the statistics {φ(z)BLR }z∈Z of a set of population points Z,(6) Score RMIA (q) := Pr 

> z∈Z

[φ(q)BLR − φ(z)BLR > γ ].

Here, population calibration tests whether the query point q γ -dominates a randomly sam-pled population point z. Each pairwise comparison yields one binary indicator, and evi-dence aggregation takes the mean of these bits to produce the final membership score. 3.2. Designing PL-MIA. Under this unified framework, we propose Pairwise Likelihood MIA (PL-MIA) by integrating statistically principled designs for both components. It adopts the GLR pointwise statistic and uses improved population calibration with a continuous ev-idence generation and aggregation mechanism. We provide the pseudocode of PL-MIA in Algorithm 1. 

Pairwise Difference as Membership Evidence. The core of population calibration lies in comparing the query point q against a set of population points Z to neutralize the intrin-sic query-level heterogeneity. We start by defining the pairwise difference as the difference between the pointwise statistic of q and a randomly sampled population point z ∈ Z:(7) ∆( q, z ) = φ(q)PL − φ(z)PL ,

where we adopt the GLR as the pointwise statistic, φPL = φGLR . This difference ∆( q, z )

serves as the fundamental unit of membership evidence for each pairwise comparison. Under 

H1, where q is a member, its statistic tends to exceed that of non-member points, resulting in positive pairwise differences on average. Under H0, where both q and z are non-members, 

∆( q, z ) is approximately symmetric around zero. 

From Binary Voting to Continuous p-values. A standard population calibration strategy to aggregate these pairwise differences into a membership score is to count the proportion of population points that the query point q can γ-dominate (Zarifzadeh, Liu and Shokri, 2024): (8) Score Binary (q) := Pr 

> z∈Z

(∆( q, z ) > γ ).

While effective, this binary voting mechanism has a limitation: it discretizes the pairwise evi-dence using a hard threshold γ. Consequently, a population point that marginally exceeds the PAIRWISE LIKELIHOOD MIA 7

Algorithm 1 MIA Score Computation with PL-MIA. Inputs: reference models Θ with training-membership records, target model θt, query point q = ( xq, y q), population dataset 

Dpop , attack mode m ∈ { online , offline }, and optional offline hyperparameters η, ρ.

1: Randomly choose a hold-out subset Z ⊂ D pop \ { q}; C ← 0

2: // Step 1: Estimate IN/OUT distributions for all relevant data points 

3: for each data point d ∈ { q} ∪ Z do 

4: confs (d)IN ← ∅ ; confs (d)OUT ← ∅ 

5: for each reference model θ′ ∈ Θ do 

6: conf ← ϕ fθ′ (xd)[ yd] # Query reference model 7: if d ∈ training set of θ′ then 

8: confs (d)IN ← confs (d)IN ∪ { conf }

9: else 

10: confs (d)OUT ← confs (d)OUT ∪ { conf }

11: end if 

12: end for 

13: μ(d)OUT ← mean(confs (d)OUT ); σ(d)OUT ← std(confs (d)OUT )

14: if m = online then 

15: μ(d)IN ← mean(confs (d)IN ); σ(d)IN ← std(confs (d)IN )

16: else 

17: σ(d)IN ← ρ · σ(d)OUT ; μ(d)IN ← μ(d)OUT + η · σ(d)OUT 

18: end if 

19: end for 

20: // Step 2: Query target model and compute the GLR pointwise statistic 

21: for each data point d ∈ { q} ∪ Z do 

22: conf (d) 

> t

← ϕ fθt (xd)[ yd] # Query target model 23: φ(d)PL ← log pdf  conf (d) 

> t

| N (μ(d)IN , (σ(d)IN )2) − log pdf  conf (d) 

> t

| N (μ(d)OUT , (σ(d)OUT )2)

24: end for 

25: bσ∆ ← √2 · std  {φ(z)PL : z ∈ Z} # Null scale for pairwise p-values 26: // Step 3: Pairwise comparison and Cauchy aggregation 

27: for each sample z ∈ Z do 

28: ∆( q, z ) ← φ(q)PL − φ(z)PL 

29: p(q, z ) ← Φ  − ∆( q, z )/bσ∆



30: C ← C + tan  (0 .5 − p(q, z )) · π

31: end for 

32: return Score PL (q; θt) ← C/ |Z|

threshold contributes the same evidence as one that exceeds it substantially. This binarization discards the magnitude of ∆( q, z ), which reflects the strength of the membership evidence. To preserve this information, we consider the p-value as a continuous measure of statistical significance. We emphasize that it retains the strength information of each pairwise compari-son compared with the binary voting. Because under H1 the relevant departure is directional (∆( q, z ) > 0), we employ the upper-tail test to compute the one-sided p-value for each pair-wise comparison. Specifically, let e∆ be the random variable representing the pairwise dif-ference between the statistics of two non-members. Under H0, both the query point and the population point are non-members, and e∆ is centered around zero. We approximate its null distribution by a centered Gaussian, which is reasonable since e∆ is the difference of two log-likelihood-ratio statistics (Van der Vaart, 2000). For the observed difference ∆obs = ∆( q, z ),the one-sided p-value is computed as (9) p(q, z ) = Pr( e∆ ≥ ∆obs | H0) ≈ Φ



− ∆( q, z )ˆσ∆



,8

where ˆσ∆ is the estimated standard deviation of the pairwise differences under H0, and Φ( ·)

is the standard normal cumulative distribution function (CDF). This transformation maps the raw difference ∆( q, z ) to a probability scale p(q, z ) ∈ [0 , 1] , where smaller values indicate stronger membership evidence. The binary score in Equation 8 can be viewed as a coarse tail probability over population draws z ∈ Z, whereas the p-value in Equation 9 retains how strongly each pairwise comparison departs from the non-member null. Thus, this continu-ous p-value serves as a smoothed surrogate for the binary indicator, preserving the evidence strength of each pairwise comparison. We empirically evaluate the validity of these pairwise 

p-values in Section 5.3.3. 

Evidence Aggregation via Cauchy Combination. For a given query point q and a set of continuous p-values {p(q, z )}z∈Z , we aggregate them into a membership score using the Cauchy Combination rule (Liu et al., 2019; Liu and Xie, 2020): (10) Score PL (q) = 1

|Z|

X

> z∈Z

tan  (0 .5 − p(q, z )) · π,

where the Cauchy transformation T (p) = tan  (0 .5 − p) · π maps each p-value to R via the inverse CDF of the standard Cauchy distribution. This transformation maps near-zero 

p-values, indicating strong membership evidence, to large positive values, while p-values near 0.5, representing neutral evidence, are mapped to values near zero. This aggregation is particularly effective when membership evidence is concentrated in a subset of pairwise com-parisons: strong evidence from a small number of comparisons can dominate the aggregated score, thereby reducing dilution from numerous weak or uninformative comparisons. 3.3. Why PL-MIA Is More Powerful. We argue that PL-MIA achieves stronger attack power by combining a more informative pointwise statistic with effective population calibra-tion and practical evidence aggregation. 

Statistical Efficiency of GLR. The GLR provides a more informative pointwise statistic by exploiting both the IN and OUT distributions. Under Assumption 2.2, GLR jointly cap-tures two sources of membership signal: the normalized mean shift η and the variance-contraction ratio ρ. These signals reflect the tendency of overfitting to increase confidence in training members while reducing their predictive uncertainty relative to non-members. In contrast, BLR (Zarifzadeh, Liu and Shokri, 2024) primarily relies on the normalized mean shift and does not explicitly exploit the variance-contraction signal. Our theoretical analy-sis (Lemma 4.1 and Lemma 4.2) shows that GLR provides stronger statistical separation than BLR when variance contraction or nuisance variation is present. This stronger point-wise separation produces more informative pairwise evidence for the subsequent population calibration. 

Population Calibration for Neutralizing Intrinsic Heterogeneity. A strong pointwise statis-tic alone is insufficient because model outputs also reflect intrinsic query-level heterogeneity unrelated to membership (Watson et al., 2021). PL-MIA addresses this limitation through population calibration, which compares the statistic of the query point with that of the popu-lation points and transforms the raw difference into relative membership evidence. This cal-ibration factors out query-level heterogeneity, allowing the final membership score to focus on the signal attributable to membership status. Our theoretical analysis (Lemma 4.4) for-malizes this effect by showing that population calibration centers non-member scores around their corresponding neutral baselines. PAIRWISE LIKELIHOOD MIA 9

Practical Advantages. Beyond statistical improvements, PL-MIA provides practical advan-tages for privacy auditing. By converting pairwise differences into continuous p-values and aggregating them through the Cauchy combination rule, PL-MIA preserves the strength of in-dividual comparisons. This design improves sensitivity to informative membership evidence while avoiding the pairwise comparison threshold γ required by binary voting. As shown in Section 5.3.1, PL-MIA achieves the strongest performance without tuning γ. Moreover, un-der the idealized Cauchy null, the Cauchy-combined score Score PL (q) provides an analytic decision threshold at a target FPR α: τα = tan( π(0 .5 − α)) , which eliminates the need for additional held-out data. As shown in Section 5.3.3, when the exact-Cauchy null conditions are only approximately satisfied, the analytic threshold provides conservative finite-sample FPR control, with the realized FPR remaining below the nominal level. 

4. Theoretical Results. In this section, we establish the theoretical advantages of PL-MIA over LiRA and RMIA through a unified analysis of attack power. We first characterize the GLR and BLR statistics, then examine how population calibration centers non-member scores, and finally compare their attack power. All results are derived under Assumption 2.2, with proofs deferred to Section S3 of the Supplementary Material. 4.1. Pointwise Statistic Distributions. We begin by deriving the GLR and BLR statistics and comparing their statistical efficiency. We assume a global variance contraction ratio ρ,while modeling the normalized mean shift η(d) and variation coefficients ξ(d) as random vari-ables with variances σ2 

> η

and σ2 

> ξ

to capture query-level heterogeneity. We quantify statistical efficiency by the mean separation , defined as ∆μ(d) = E[φ(d) | H1] − E[φ(d) | H0]. A larger mean separation indicates greater pointwise distinguishability. LEMMA 4.1 (Gaussian LR). Let W ∼ N (0 , 1) . The Gaussian LR statistic takes the form 

φ(d)GLR | H0 = − log ρ + W 2

2 − (W − η(d))2

2ρ2 ,(11) 

φ(d)GLR | H1 = − log ρ + (η(d) + ρW )2

2 − W 2

2 .(12) 

Its mean separation is ∆μ(d)GLR = (ρ2−1) 2+( η(d))2(1+ ρ2)2ρ2 . When ρ < 1, ∆μ(d)GLR grows as 

O(1 /ρ 2), allowing GLR to capture the signal induced by variance contraction. 

LEMMA 4.2 (Bayes LR). Let W ∼ N (0 , 1) . Assuming equal numbers of IN and OUT reference models, a first-order Taylor expansion of the Bayes LR statistic gives 

φ(d)BLR | H1 ≈ η(d)ξ(d)

2 + ρξ (d)W, (13) 

φ(d)BLR | H0 ≈ − η(d)ξ(d)

2 + ξ(d)W. (14) 

Its first-order mean separation is ∆μ(d)BLR = η(d)ξ(d), which is independent of ρ. Thus, BLR does not retain variance contraction as a mean-separation signal. 

Together, Lemmas 4.1 and 4.2 identify a key pointwise distinction: GLR retains a variance-contraction signal that is absent from the first-order BLR mean separation. We next study how these statistics behave in pairwise comparisons. 10 

4.2. Pairwise Difference and Population Calibration. Population calibration converts pointwise statistics into a membership score by comparing a query point q with population points z. To isolate the effect of calibration under query-level heterogeneity ( ση > 0), we set 

ρ = 1 , thereby removing variance contraction from this comparison. LEMMA 4.3 (Pairwise Difference Distribution). Under ση > 0 and ρ = 1 , the GLR- and BLR-induced pairwise differences for a member query satisfy 

For GLR : φ(q)GLR − φ(z)GLR ∼ N 

 (η(q))2 + ( η(z))2

2 , (η(q))2 + ( η(z))2



.(15) 

For BLR : φ(q)BLR − φ(z)BLR ∼ N 

 η(q)ξ(q) + η(z)ξ(z)

2 , (ξ(q))2 + ( ξ(z))2



.(16) 

The corresponding non-member distributions are presented in Section S3.3 of the Supple-mentary Material. 

Population calibration aggregates these pairwise differences into a final membership score. We consider the binary-voting score in Equation 8 and the Cauchy-combined PL-MIA score in Equation 10. For binary voting, we consider the neutral threshold γ = 0 that asks whether the query point wins against a population point. LEMMA 4.4 (Membership Scores under Population Calibration). Let Q be a random non-member query point. The two population-calibrated membership scores have the follow-ing neutral baselines: 

Binary voting : E [Score Binary (Q) | H0] = 0 .5,(17) 

Cauchy combination : Median (Score PL (Q) | H0) ≈ 0.(18) 

Under binary voting, exchangeability gives a non-member query an average win probabil-ity of 0.5. Under Cauchy combination, null pairwise p-values are mapped to median-zero Cauchy evidence, giving the aggregated score median zero. We use the median because Cauchy evidence has no finite mean. 

4.3. Attack-Power Expressions and Advantage Analysis. We use attack power (Zhu et al., 2025) to compare attacks, defined as the TPR at a fixed FPR. To separate the contributions of pointwise statistics and evidence aggregation, let PL -MIA + denote the intermediate attack that uses the GLR pointwise statistic and binary-voting population calibration. PL-MIA is obtained from PL -MIA + by replacing binary voting with the Cauchy combination. For LiRA, RMIA, and PL-MIA +, their Gaussian score distributions yield a common CDF-based expression for attack power. For PL-MIA, the heavy-tailed Cauchy-combined score instead yields a low-FPR characterization in terms of exceptionally small pairwise p-values. THEOREM 4.5 (Attack-Power Expressions for the Compared Attacks). Under ρ = 1 , at a fixed FPR α, the attack power for LiRA, RMIA, and PL -MIA + has the unified form 

TPR A(α) = Φ  κA − Φ−1(1 − α) , A ∈ { LiRA , RMIA , PL -MIA +}.(19) 

Here Φ( ·) and Φ−1(·) denote the standard Gaussian CDF and its inverse. The parameter κA

measures the standardized separation between the member and non-member score distribu-tions for attack A, so a larger κA yields higher attack power. For PL-MIA, define 

uα, |Z| := 12 − 1

π arctan ( |Z|τα) ≈ α

|Z| , rα := Pr p(Q, Z ) ≤ uα, |Z| | H1

 ,(20) PAIRWISE LIKELIHOOD MIA 11 

where τα = tan ( π(0 .5 − α)) is the analytic decision threshold. Here, uα, |Z| is the pairwise 

p-value level corresponding to the analytic threshold, and rα is the probability that a member query reaches this level in one comparison. Aggregating over the |Z| comparisons gives 

TPR PL -MIA (α) ≈ 1 − (1 − rα)|Z|.(21) 

This expression captures the probability that, among |Z| pairwise comparisons, at least one comparison produces strong membership evidence. Lemma 4.3 explains why this event is more likely for member queries: larger pairwise differences translate into smaller p-values. 

THEOREM 4.6 (Advantage Analysis). Under Assumption 2.2 and at a fixed α, the three components of PL-MIA yield the following attack-power comparisons: 

(I) If σ2 

> η

> 0 and ρ = 1 , then TPR PL -MIA + (α) = TPR LiRA (α). Population calibration additionally places the GLR scores on a common null-relative scale. 

(II) If ρ < 1, or if ρ = 1 and σ2 

> ξ

> 0, then TPR PL -MIA + (α) > TPR RMIA (α).

(III) If |Z|rα > − log (1 − TPR PL -MIA + (α)) , then TPR PL -MIA (α) > TPR PL -MIA + (α).Part (I) characterizes the role of population calibration: it preserves LiRA’s attack power while placing heterogeneous GLR scores on a common relative scale, thereby reducing query-level heterogeneity. Part (II) quantifies the gain from GLR: unlike BLR, it retains the variance-contraction signal when ρ < 1 and avoids the query-dependent multiplicative vari-ation of the first-order BLR score when ρ = 1 and σ2 

> ξ

> 0. Part (III) identifies when Cauchy combination outperforms binary voting by retaining the evidence strength and amplifying exceptionally small p-values. Together, these results decompose the advantage of PL-MIA into pointwise signal extrac-tion, population calibration, and evidence-sensitive aggregation. 

5. Experiments. 

5.1. Experimental Setup. Datasets and Implementation Details. Our main experiments evaluate PL-MIA on three image datasets: CIFAR-10, CIFAR-100 (Krizhevsky et al., 2009), and CINIC-10 (Darlow et al., 2018). For a fair comparison, we use Wide-ResNet (Zagoruyko and Komodakis, 2016) as the backbone across all datasets and follow the model-training protocol of Zarifzadeh, Liu and Shokri (2024). Additional details on the model-training protocol and hyperparameters are provided in Section S1 of the Supplementary Material. Following the standard evaluation protocol (Carlini et al., 2022; Wen et al., 2023), we construct balanced member and non-member evaluation sets by randomly partitioning each dataset into two disjoint halves of equal size. One half is used to train the target model and constitutes the member set, while the other half is held out as the non-member set. We use all points in both sets as audit queries. We train multiple reference models (Ye et al., 2022; Watson et al., 2021) using the same Wide-ResNet backbone and training protocol. Each reference model is trained on an independently sampled 50% subset of the full dataset, so that each query point is included in approximately half of the reference-model training sets. For each query point, reference models trained on that point are referred to as IN reference models, while those not trained on it are referred to as OUT reference models. 

Attack Mode (Online vs. Offline) and Baselines. We consider both online and offline attack modes. In the online setting, the adversary uses both IN and OUT reference models, whereas the offline setting permits access only to OUT reference models. Under both attack modes, we compare PL-MIA with representative baselines, including Attack-R, Attack-P (Ye et al., 12 

TABLE 2

Performance of different attacks using 254 reference models, averaged over 10 random target models. For each scenario, the best-performing method is highlighted in bold , while the second-best result is underlined. Standard deviations over target models are shown as subscripts. The top part corresponds to offline attacks using only 127 OUT models, whereas the bottom part reports online attacks using 127 OUT and 127 IN models. IoS 

(Improvement over SOTA) reports the improvement of PL-MIA over the previously strongest baseline under the same setting, with absolute and relative gains shown in IoS (Abs.) and IoS (Rel.), respectively.                                                                                                                          

> Attack CIFAR-10 CIFAR-100 CINIC-10 AUC TPR@FPR AUC TPR@FPR AUC TPR@FPR 0.01% 0.0% 0.01% 0.0% 0.01% 0.0% Offline Attack-P 58 .15 0.45 0.00 0.00 0.00 0.00 77 .12 0.19 0.01 0.01 0.00 0.00 66 .30 0.23 0.00 0.01 0.00 0.00
> Attack-R 64 .24 0.39 1.33 0.24 0.81 0.31 83 .30 0.22 5.92 1.70 3.12 1.02 72 .86 0.18 1.70 0.58 1.03 0.68
> LiRA 55 .33 0.46 1.33 0.34 0.71 0.35 75 .87 0.32 2.73 1.28 1.52 1.26 64 .04 0.59 1.31 0.26 0.60 0.39
> RMIA 71 .64 0.34 4.03 0.82 3.08 0.62 90 .60 0.13 10 .79 2.77 7.73 2.47 82 .11 0.30 5.46 0.95 3.61 1.18
> PL-MIA 71 .18 0.37 4.56 0.79 3.34 0.77 91 .22 0.13 15 .82 2.23 12 .58 2.89 81 .45 0.28 7.04 0.92 5.44 1.41
> IoS (Abs.) −0.46 0.53 0.26 0.62 5.03 4.85 −0.66 1.58 1.83
> IoS (Rel.) ↓0.64% ↑13.15% ↑8.44% ↑0.68% ↑46.62% ↑62.74% ↓0.80% ↑28.94% ↑50.69% Online LiRA 72 .10 0.41 3.27 0.42 1.96 0.91 91 .48 0.13 11 .60 1.84 7.61 2.94 82 .23 0.28 4.78 1.34 3.01 1.36
> RMIA 72 .19 0.35 4.35 0.68 2.79 0.66 91 .00 0.14 12 .66 2.29 10 .37 2.67 82 .48 0.32 6.83 1.42 4.34 1.68
> PL-MIA 72 .49 0.34 4.45 0.54 3.43 0.80 92 .10 0.14 16 .81 2.41 13 .20 2.89 82 .71 0.27 7.12 1.01 5.54 1.22
> IoS (Abs.) 0.30 0.10 0.64 0.62 4.15 2.83 0.23 0.29 1.20
> IoS (Rel.) ↑0.42% ↑2.30% ↑22.94% ↑0.68% ↑32.80% ↑27.29% ↑0.28% ↑4.25% ↑27.65%

2022), LiRA (Carlini et al., 2022), and RMIA (Zarifzadeh, Liu and Shokri, 2024). For a fair comparison, we apply the same data-augmentation protocol of RMIA (Zarifzadeh, Liu and Shokri, 2024) to all compatible attacks. In our experiments, LiRA and RMIA are evaluated in both online and offline modes, whereas Attack-P and Attack-R are restricted to the offline mode, as they rely on global statistics (e.g., loss) that do not condition on the inclusion of the query point. 

Evaluation Metrics. We evaluate each attack using two standard metrics: the area under the receiver operating characteristic curve (AUC) and the true positive rate (TPR) at extremely low false positive rates (FPRs). Specifically, we report TPR at FPRs of 0.01% and 0.0%. AUC measures the attack’s overall discriminative ability by summarizing the trade-off between TPR and FPR across all possible decision thresholds. In contrast, TPR at low FPRs measures attack effectiveness under stringent false-positive constraints, which is particularly relevant to privacy audits that require a near-zero false-accusation rate. 5.2. Main Results. We compare PL-MIA against baseline attacks across datasets and at-tack modes, with the results reported in Table 2. In the online setting, PL-MIA consistently achieves the strongest performance for all datasets and metrics. The gains are most pro-nounced in the low-FPR regime: at TPR@0.0%FPR, PL-MIA improves over the strongest baseline by 22.94%, 27.29%, and 27.65% on CIFAR-10, CIFAR-100, and CINIC-10, respec-tively. In the offline setting, PL-MIA remains competitive in AUC and achieves the strongest low-FPR performance across all datasets. In particular, on CIFAR-100 and CINIC-10, PL-MIA improves TPR@0.0%FPR over the strongest baseline by 62.74% and 50.69% , respec-tively. These results support our theoretical findings in Section 4 that PL-MIA yields a more powerful test in the practically critical low-FPR regime. 5.3. Analysis. We further analyze the component contributions, sensitivity, computa-tional cost, and statistical properties of PL-MIA. Unless otherwise specified, we report the results on the image datasets. PAIRWISE LIKELIHOOD MIA 13 

5.3.1. Ablation and Sensitivity Analyses. Component-wise Ablation. We ablate each component of PL-MIA across datasets and at-tack modes, and report the results in Table 3. Row 1 uses only the Gaussian likelihood ratio (GLR) as the pointwise statistic, corresponding to LiRA. Row 2 adds binary-voting pop-ulation calibration to the GLR, yielding the intermediate attack PL-MIA +. This addition substantially improves TPR@0.0%FPR in all settings, consistent with our analysis that pop-ulation calibration neutralizes query-level heterogeneity (Lemma 4.4). Finally, Row 3 uses the continuous pairwise p-values and aggregates them using the Cauchy combination (CC), yielding the complete PL-MIA. This final step provides a further improvement and achieves the strongest performance under all reported settings.  

> TABLE 3
> Component-wise ablation of PL-MIA, evaluated using TPR@0%FPR across datasets and attack modes. GLR, PC, and CC denote Gaussian Likelihood Ratio, Population Calibration, and Cauchy Combination, respectively.

Component CIFAR-10 CIFAR-100 CINIC-10 GLR PC CC Online Offline Online Offline Online Offline 

✔ 1.96 0.71 7.61 1.52 3.01 0.60 

✔ ✔ 3.32 3.29 12.82 12.32 5.40 5.16 

✔ ✔ ✔ 3.43 3.34 13.20 12.58 5.54 5.44 

Sensitivity to Pairwise Comparison Threshold γ. We further study the sensitivity to the pairwise comparison threshold γ in Equation 8, which determines whether each pairwise comparison contributes positive binary evidence. As shown in Figure 1, RMIA is highly sensitive to γ and underperforms LiRA when γ = 1 . For comparison, we also report the performance of PL-MIA +, which uses the GLR pointwise statistic with the same binary-voting population calibration. It consistently outperforms RMIA for all considered γ and is substantially less sensitive to the threshold choice. The complete PL-MIA does not require γ

because it converts pairwise differences into continuous p-values and aggregates them using the Cauchy combination. It therefore eliminates the need to tune the hyperparameter γ while achieving the strongest performance.  

> Threshold γ
> TPR0.0%FPR

Fig 1: Sensitivity to the pairwise comparison threshold γ on CIFAR-10, evaluated using TPR@0.0% FPR. 14 

Offline PL-MIA. In the offline setting, the adversary has access only to OUT reference models and therefore cannot directly estimate the IN distribution. To enable PL-MIA in this setting, we estimate the IN-distribution parameters from the OUT-distribution statistics, based on the empirical observation that members typically exhibit higher confidence (posi-tive mean shift) and lower uncertainty (variance contraction). Specifically, we approximate the IN-distribution parameters as (22) σin ≈ ρ · σout , μin ≈ μout + η · σout ,

where ρ ∈ (0 , 1] is the variance-contraction ratio and η > 0 is the normalized mean shift. Table 4 shows that PL-MIA remains stable across the considered values of η and ρ. For ex-ample, on CIFAR-100, AUC ranges only from 90 .12 to 91 .22 , while TPR@0.0%FPR ranges from 12 .14 to 12 .78 , with η = 0 .3 and ρ = 0 .9 achieving the best trade-off. These results indicate that even without explicit IN reference models, approximating the normalized mean shift and variance contraction allows PL-MIA to maintain strong attack performance.                                                                             

> TABLE 4
> Offline PL-MIA performance under different normalized mean shifts ηand variance-contraction ratios ρ. For each dataset, the best result for each metric is highlighted in bold .
> ηρCIFAR-10 CIFAR-100 CINIC-10 AUC TPR@FPR AUC TPR@FPR AUC TPR@FPR 0.01% 0.0% 0.01% 0.0% 0.01% 0.0% 0.3 0.8 71.18 4.56 3.34 91.08 16.03 12.78 81.17 7.01 5.34 0.9 71.02 4.52 3.33 91.22 15.82 12.58 81.45 7.04 5.44
> 0.5 0.8 70.19 4.52 3.34 90.35 15.96 12.14 81.01 7.04 5.44 0.9 70.12 4.54 3.33 90.37 15.92 12.32 80.57 7.16 5.24 1.0 0.8 69.12 4.52 3.34 90.22 16.03 12.78 79.90 7.01 5.34 0.9 69.06 4.55 3.33 90.12 15.82 12.58 79.87 7.04 5.44

5.3.2. Cost Analysis. We analyze the cost-performance trade-off by varying three com-putational factors: the number of reference models, the number of data augmentations, and the number of population points. 

Number of Reference Models. We first vary the number of reference models to study their effect on the performance of different attacks. As shown in Figure 2a, all attacks benefit from an increasing number of reference models. However, PL-MIA is comparatively sensitive in the smallest-budget regime because both the IN- and OUT-distribution Gaussian parameters must be estimated from only a few reference-model observations. In particular, reliable es-timation of the variances requires a sufficiently large reference-model set. As the reference-model budget increases, these estimates become more stable, and PL-MIA rapidly closes the gap with RMIA before eventually outperforming the baselines. An additional compari-son under limited reference-model budgets is reported in Section S2.1 of the Supplementary Material. Overall, these results indicate that PL-MIA achieves its peak performance with a sufficient reference-model budget. 

Pooled Variance for Stable Estimation under Limited Reference-Model Budgets. As dis-cussed in Section 3, PL-MIA estimates the Gaussian parameters μin , σ in , μout , and σout in Equation 4 to compute the GLR statistic. When only a small number of reference models are available, the sample variance estimates can be unstable, thereby degrading attack power. To mitigate this, we consider a pooled variance that uses the information across augmented views of the same point to stabilize variance estimation. As shown in Table 5, pooled variance PAIRWISE LIKELIHOOD MIA 15 

> AUC TPR@0.01%FPR TPR@0.0%FPR

(a) # reference models (b) # data augmentations (c) # population samples 

Fig 2: Cost-performance analysis on CIFAR-10. Rows report AUC, TPR@0.01%FPR, and TPR@0.0%FPR, while columns vary the number of reference models, data augmentations, and population samples, respectively. is most beneficial when the reference-model budget is small. The largest AUC gain occurs at #Ref = 4 , where pooled variance increases AUC from 61 .71 to 66 .32 . As the number of reference models increases, the standard variance estimates become sufficiently stable, and the difference between the two variants largely disappears. This result highlights that incor-porating augmentation-level information can partially compensate for limited model-level diversity in computation-constrained settings.  

> TABLE 5
> Effect of the reference-model budget on PL-MIA on CIFAR-10, comparing the standard Gaussian variance estimator with the pooled variance estimator.

Metric Pooled variance # of Reference Models 2 4 8 16 32 64 128 254 AUC w/ 65.32 66.32 67.84 69.68 70.87 71.68 72.20 72.48 w/o 64.43 61.71 66.41 69.06 70.56 71.52 72.12 72.49 TPR@0.01%FPR w/ 0.71 0.98 1.12 2.00 3.19 3.88 4.46 4.40 w/o 0.68 0.18 0.55 1.88 3.17 3.86 4.45 4.45 TPR@0.0%FPR w/ 0.35 0.61 0.72 1.23 2.54 2.97 3.22 3.28 w/o 0.21 0.11 0.20 1.11 2.44 2.86 3.25 3.43 

Number of Data Augmentations. We then study the effect of the number of data aug-mentations on attack performance. As shown in Figure 2b, all MIAs consistently exhibit 16 

performance gains across metrics as the augmentation budget increases. PL-MIA consis-tently outperforms both LiRA and RMIA across all considered augmentation budgets. At TPR@0.0%FPR, the performance gap becomes more pronounced with additional augmented views, indicating that PL-MIA effectively aggregates membership signals across multiple augmented views of the same point. We further compare strategies for aggregating these augmented signals in Section S2.2 of the Supplementary Material and show that mean aggre-gation yields the strongest overall performance. 

Number of Population Points. Figure 2c shows the effect of the number of population points 

|Z| used for pairwise comparison on the attack performance. In terms of AUC, both RMIA and PL-MIA improve as |Z| increases, indicating that a larger population set provides more reliable population calibration. PL-MIA consistently outperforms RMIA and gradually ap-proaches the strong AUC performance of LiRA as more population points become avail-able. The effect of |Z| is more pronounced at critical low-FPR operating points. With a lim-ited number of population points, such as |Z| = 25 , the discrete voting mechanism used by RMIA becomes unstable and performs worse than LiRA at both TPR@0.01%FPR and TPR@0.0%FPR. In contrast, PL-MIA remains competitive with LiRA in this low-|Z| regime and surpasses both baselines once more population points are available. These results sup-port our theoretical insight that continuous p-value aggregation is more robust to limited population-calibration data than discrete voting-based aggregation. 5.3.3. Statistical Properties of PL-MIA. Validity of Pairwise p-values. We examine the empirical distributions of pairwise p-values on CIFAR-10, CIFAR-100, and CINIC-10. As shown in Figure 3, the pairwise p-values for non-members are relatively flat and close to the reference uniform density, with mild bound-ary deviations caused by finite-sample estimation and the Gaussian approximation. By con-trast, members show a clear departure from uniformity and assign more probability mass to small p-values. These results support the validity of the proposed pairwise p-values as directional, continuous measures of membership evidence for the subsequent Cauchy combi-nation. The mild deviations from the idealized null motivate the finite-sample FPR analysis below. 

Empirical FPR Control with the Analytic Decision Threshold. Under the ideal exact-Cauchy null, the Cauchy-combined score yields the analytic decision threshold (23) τα = tan {π(0 .5 − α)},

where α is the nominal FPR level. Exact agreement between the nominal and realized FPR requires that each pairwise p-value p(q, z ) follow Uniform(0 , 1) under H0 and that the aggre-gated pairwise p-values be mutually independent. These conditions are only approximately satisfied in our setting. For a fixed query point q, all pairwise p-values share the same query statistic ϕ(q)PL and are therefore dependent. Moreover, Figure 3 shows that the p-values for non-members exhibit mild boundary deviations from uniformity. The exact-Cauchy null should therefore be interpreted as an idealized reference distribution rather than the exact finite-sample null distribution. To assess the finite-sample behavior of the analytic threshold, we compare the nominal FPR with the empirically realized FPR in Figure 4. Let Qout denote the non-member audit queries. For each nominal level α, the realized FPR is computed as (24) [FPR( α) = 1

|Qout |

X

> q∈Qout

I(Score PL (q) ≥ τα).PAIRWISE LIKELIHOOD MIA 17 

> (a) CIFAR-10
> (b) CIFAR-100
> (c) CINIC-10

Fig 3: Empirical distribution of pairwise p-values for member and non-member queries across datasets. The dashed line indicates the density of the uniform distribution 

Uniform(0 , 1) , which serves as the idealized reference distribution under H0.

Fig 4: Nominal versus realized FPR under the analytic decision threshold. The dashed diag-onal denotes exact agreement between the nominal and realized FPR. 18 

The dashed diagonal line y = x represents exact agreement, where the realized FPR equals the nominal level. As shown in Figure 4, the realized FPR remains below the nominal FPR for all three datasets. Thus, the analytic decision threshold is conservative in our finite-sample experiments. The deviation from exact agreement becomes more pronounced in the extreme low-FPR regime. For example, at α = 0 .01% , the realized FPR is already below the nomi-nal level, meaning that the analytic threshold flags fewer non-members than a finite-sample threshold that attains the nominal FPR exactly. As α approaches zero, the ideal exact-Cauchy threshold diverges, whereas an empirical threshold yielding zero observed false positives re-mains finite for a finite non-member audit set. Overall, the finite-sample departures from the Cauchy null occur in a conservative direc-tion: the analytic threshold flags fewer non-members than the nominal FPR permits. This behavior is consistent with the tail-validity result of Liu and Xie (2020), which supports Cauchy-combination tail control under dependence. Although the finite-sample pairwise p-values are not exactly uniform and the transformed terms are dependent, the empirical results show that the analytic threshold provides conservative low-FPR control in the evaluated set-tings. 5.3.4. Generalization to Tabular Data and Different Model Families. We further evalu-ate PL-MIA’s generalizability to tabular data and different model families. Specifically, we consider Gradient Boosting Decision Tree (GBDT) models (Ke et al., 2017) on the tabular Purchase-100 dataset to examine whether PL-MIA extends beyond image data and neural networks to tabular data and tree-based models. We train GBDT models with 250 estimators, a learning rate of 0.1, and a subsampling rate of 0.2. We vary the maximum tree depth over 

3, 5, 7 and use the true-label prediction probability as the attack signal. For each setting, all attacks follow the same member/non-member evaluation protocol and use the same sets of target and reference models.                                                                                            

> TABLE 6
> Attack performance on Purchase-100 using GBDT models with different maximum depths. The maximum depth controls the complexity of the tree-based statistical model. Results are averaged over target models and reported as mean ±standard deviation.
> max_depth Method AUC TPR@0.01%FPR TPR@0%FPR 3PL-MIA 94 .36 ±0.08 6.20 ±2.01 3.31 ±1.65
> LiRA 93 .79 ±0.10 1.34 ±0.68 0.42 ±0.38
> RMIA 94 .23 ±0.08 4.66 ±1.35 2.45 ±1.44
> 5PL-MIA 98 .71 ±0.03 23 .62 ±4.75 15 .53 ±5.71
> LiRA 98 .47 ±0.04 3.86 ±1.79 1.29 ±1.28
> RMIA 98 .61 ±0.03 19 .66 ±3.06 12 .48 ±4.80
> 7PL-MIA 99 .14 ±0.02 31 .25 ±1.74 21 .86 ±5.90
> LiRA 98 .93 ±0.02 3.83 ±1.94 0.81 ±0.64
> RMIA 99 .04 ±0.03 23 .11 ±2.40 12 .41 ±5.92

Table 6 and Figure 5 show that PL-MIA remains effective across GBDT models of dif-ferent depths. Attack performance improves substantially as max_depth grows, indicat-ing that deeper GBDT models expose more distinguishable membership evidence. For all depths, PL-MIA achieves the highest AUC and the strongest performance under stringent FPR constraints among the compared attacks. The improvement is especially pronounced at TPR@0%FPR: when max_depth =7, PL-MIA reaches 21 .86 , exceeding RMIA’s 12 .41 by 

76 .15% . We also evaluate Multi-Layer Perceptron (MLP) models on Purchase-100, with the results reported in Section S2.4 of the Supplementary Material. Together, these experiments demonstrate that PL-MIA generalizes across data modalities and model families, from image data with neural networks to tabular data with both neural and tree-based models. PAIRWISE LIKELIHOOD MIA 19 

Fig 5: ROC curves on Purchase-100 with GBDT models. Each panel corresponds to one maximum-depth setting and compares PL-MIA against attack baselines. 

6. Conclusion. Our findings indicate that the low-FPR gains of PL-MIA arise from the interaction of three statistical mechanisms. First, the Gaussian likelihood ratio uses both the mean shift and the variance contraction between the IN and OUT distributions. The latter represents an additional membership signal because models tend to exhibit lower predictive uncertainty on training members. Second, population calibration treats query-specific het-erogeneity as a nuisance effect. Comparing a query point with held-out non-members places its statistic on a relative scale, reducing the likelihood that an intrinsically easy non-member receives a high membership score. Third, continuous p-values and the Cauchy combination preserve more information than binary voting. Individual pairwise comparisons may be weak or noisy, but combination testing can synthesize them into an overall assessment while allow-ing a small number of highly informative comparisons to contribute substantial evidence (Liu et al., 2019; Liu and Xie, 2020). PL-MIA primarily improves separation in the tail of the non-member distribution, which is the region most relevant to reliable privacy auditing. Several extensions could strengthen the statistical foundation and practical scope of PL-MIA. An important problem is the optimal allocation of a fixed computational budget among reference models, population points, data augmentations, and audit queries. For large-scale models, independently training many reference models may be infeasible. Training check-points, smaller proxy models, or token- and representation-level reference distributions may provide alternative sources of calibration information (Tao and Shokri, 2026; Hayes et al., 2025). Together, these directions could extend the proposed framework from benchmark at-tack comparison toward reliable, uncertainty-aware, and reproducible privacy auditing. 

Appendices: Supplementary Materials 

The supplementary materials are organized as follows. Section A provides additional de-tails on the model-training protocol and hyperparameters for the image-dataset experiments. Section B presents supplementary experiments on limited reference-model budgets, aug-mentation aggregation strategies, the Gaussian assumption, and MLP models on Purchase-100. Section C presents detailed proofs of the theoretical results in Section 4 of the main manuscript. APPENDIX A: MODEL TRAINING PROTOCOL AND HYPERPARAMETERS We describe the overall experimental setup, including the datasets, attack modes, base-lines, and evaluation metrics in Section 5.1 of the main manuscript. Here, we provide ad-ditional details on the model-training protocol and hyperparameters for the image-dataset experiments. Following prior work (Carlini et al., 2022; Zarifzadeh, Liu and Shokri, 2024), 20 

each target and reference model is trained on a 50% subset of the full dataset. For each tar-get model, the training subset defines the member set, while the remaining half is held out as non-members. Each reference model is trained on an independently sampled 50% subset, so that each query point is included in approximately half of the reference models, yielding approximately balanced IN and OUT reference sets. For CIFAR-10, CIFAR-100, and CINIC-10, we use Wide-ResNet as the backbone for both target and reference models. All models are trained using stochastic gradient descent (SGD) with cosine learning-rate decay and a brief linear warm-up over the first 1% of the training epochs. We do not apply data augmentation during model training. Table 7 summarizes the training hyperparameters and the average training/test accuracy across target models.                                    

> TABLE 7
> Training hyperparameters and average training/test accuracy (%) for the image-dataset experiments.
> Dataset Epoch Batch size Learning rate Momentum Weight decay Train Acc. Test Acc. CIFAR-10 100 256 0.1 0.9 5×10 −4100.0 91.7 CIFAR-100 100 256 0.1 0.9 5×10 −499.9 67.1 CINIC-10 100 256 0.1 0.9 5×10 −499.7 78.0

APPENDIX B: SUPPLEMENTARY EXPERIMENTAL RESULTS This section presents additional experimental results related to the analyses in Section 5.3 of the main manuscript.      

> TABLE 8
> Attack performance on CIFAR-100 under different reference-model budgets. Here, # Ref denotes the total number of reference models; # Ref = 4 corresponds to two IN and two OUT reference models per query point. “PL-MIA (pool var.)” denotes the pooled-variance variant of PL-MIA, which uses a pooled variance estimate to stabilize Gaussian modeling when only a small number of reference models is available. Results are averaged over target models and reported as mean ±standard deviation.

# Ref Attack AUC TPR@FPR 0.01% 0.0% 4LiRA 87 .74 ± 0.12 3.71 ± 1.17 1.49 ± 0.94 

RMIA 89 .54 ± 0.13 3.84 ± 1.76 2.15 ± 1.74 

PL-MIA 67 .55 ± 0.15 0.01 ± 0.01 0.00 ± 0.01 

PL-MIA (pool var.) 86 .03 ± 0.14 0.68 ± 0.27 0.21 ± 0.23 

8LiRA 89 .27 ± 0.13 6.07 ± 1.44 2.88 ± 1.65 

RMIA 90 .19 ± 0.15 6.97 ± 1.36 4.28 ± 2.52 

PL-MIA 85 .76 ± 0.20 0.12 ± 0.09 0.06 ± 0.06 

PL-MIA (pool var.) 88 .92 ± 0.14 1.96 ± 0.84 0.73 ± 0.32 

16 LiRA 90 .29 ± 0.14 7.41 ± 1.80 4.39 ± 1.90 

RMIA 90 .55 ± 0.14 10 .82 ± 1.10 4.38 ± 3.69 

PL-MIA 90 .06 ± 0.17 4.32 ± 0.86 2.16 ± 1.17 

PL-MIA (pool var.) 90 .49 ± 0.15 6.00 ± 1.82 3.16 ± 1.88 

32 LiRA 90 .90 ± 0.10 9.91 ± 2.26 6.07 ± 3.40 

RMIA 90 .78 ± 0.13 11 .50 ± 2.21 5.63 ± 5.36 

PL-MIA 91 .24 ± 0.14 10 .29 ± 2.45 6.18 ± 2.50 

PL-MIA (pool var.) 91 .33 ± 0.13 11 .12 ± 2.41 6.84 ± 2.33 

64 LiRA 91 .22 ± 0.12 10 .67 ± 1.98 7.17 ± 3.63 

RMIA 90 .89 ± 0.14 11 .80 ± 3.06 6.61 ± 5.53 

PL-MIA 91 .73 ± 0.14 13 .15 ± 2.89 10 .09 ± 2.95 

PL-MIA (pool var.) 91 .75 ± 0.14 13 .43 ± 2.69 10 .35 ± 2.49 PAIRWISE LIKELIHOOD MIA 21 

B.1. Performance under Limited Reference-Model Budgets. To complement the cost analysis in Section 5.3.2 of the main manuscript, we further compare attacks on CIFAR-100 under limited reference-model budgets. We start with #Ref = 4 , which corresponds to approximately two IN and two OUT reference models per query point. We do not consider 

#Ref = 2 , since this leaves only one IN and one OUT reference model per query point on average, precluding reliable estimation of the corresponding distributions. Table 8 reveals a clear budget-dependent trade-off. When the reference-model budget is extremely limited ( #Ref ∈ { 4, 8, 16 }), RMIA achieves the strongest low-FPR performance, whereas the standard PL-MIA is more sensitive to unstable Gaussian variance estimates. Pooling variance across augmented views substantially stabilizes PL-MIA in this regime. The largest gain occurs at #Ref = 4 , where the pooled variance increases AUC from 67 .55 

to 86 .03 .As the reference-model budget increases, PL-MIA rapidly closes the gap. At #Ref = 32 ,PL-MIA surpasses RMIA in AUC and TPR@0.0%FPR while remaining competitive at TPR@0.01%FPR. At #Ref = 64 , both the standard and pooled-variance variants outperform RMIA across all reported metrics. These results indicate that RMIA is preferable under ex-tremely limited reference-model budgets, whereas PL-MIA becomes more effective once the Gaussian parameters can be estimated with sufficient stability. Pooled variance partially mit-igates this requirement in low-budget settings. 

B.2. Effect of Augmentation Aggregation Strategies. In addition to the augmentation-budget analysis in Section 5.3.2 of the main manuscript, we further investigate how member-ship signals should be aggregated across multiple augmented views. Given K augmented views for each query q and a population point z, we compute augmentation-specific pair-wise differences and aggregate them into a single pairwise signal before converting it into membership evidence. We compare three aggregation strategies: Mean , Median , and Vot-ing (Zarifzadeh, Liu and Shokri, 2024), where Voting records the fraction of augmented comparisons for which q appears more member-like than z.                   

> TABLE 9
> Effect of augmentation aggregation strategies on attack performance. Mean and Median aggregate the augmentation-specific pairwise differences, whereas Voting records the fraction of positive pairwise comparisons.
> Aggregation AUC TPR@FPR 0.01% 0.0% Voting 71.71 4.49 3.06 Median 72.39 4.39 3.38 Mean 72.49 4.45 3.43

As shown in Table 9, Mean aggregation provides the strongest overall performance. It achieves the highest AUC and TPR@0%FPR, while Voting is only marginally higher at TPR@0.01%FPR ( 4.49 versus 4.45 ). Median aggregation remains competitive but does not improve the overall trade-off. These results indicate that retaining the magnitude of augmentation-specific signals through averaging is generally more effective than using a ro-bust summary or discretizing each comparison into a binary vote. We therefore use mean aggregation throughout the experiments. 

B.3. Gaussian Assumption Diagnostics and Robustness. PL-MIA models the per-point IN and OUT reference distributions using Gaussian approximations, as specified in 22 

Assumption 2.2 of the main manuscript. Although this assumption makes the likelihood-ratio statistic analytically tractable, it may not hold exactly for every query point. We there-fore examine both the empirical plausibility of the Gaussian approximation and PL-MIA’s robustness to departures from Gaussianity. For each query point q, we collect the logit-scaled true-label confidence scores from ref-erence models that include q in their training sets and from those that exclude it, forming the empirical IN and OUT distributions, respectively. Since our experiments use 254 reference models, each distribution contains 127 observations. We apply the Shapiro-Wilk normality test (Shapiro and Wilk, 1965) separately to the IN and OUT distributions. For observations 

r1, . . . , r n, the Shapiro-Wilk statistic is (25) W =

 P ni=1 ωir(i)

2

Pni=1 (ri − 1

> n

Pni=1 ri)2 ,

where r(i) denotes the i-th order statistic and ωi denotes the corresponding Shapiro-Wilk weights. A value of W closer to one indicates stronger agreement with a Gaussian distribu-tion, while a small associated p-value provides evidence against the normality hypothesis. 

Fig 6: Representative per-point normality diagnostics on CIFAR-10. Each panel shows the empirical IN and OUT distributions of logit-scaled true-label confidence scores, with fitted Gaussian densities overlaid. The annotations report the Shapiro–Wilk statistics and corre-sponding p-values for the two reference distributions. As illustrated in Figure 6, the empirical histograms are generally aligned with the fitted Gaussian densities, supporting the use of the Gaussian approximation as a practical model for the reference distributions. However, the degree of agreement varies across query points. To quantify the effect of this variation, we define the Gaussianity score as (26) G(q) = min {Win (q), W out (q)},

where WIN (q) and WOUT (q) are the Shapiro-Wilk statistics for the IN and OUT distributions of q, respectively. This score is conservative: a query point receives a high Gaussianity score only when both its IN and OUT distributions are close to Gaussian. We sort the audited query points by G(q) and partition them into three equal-sized groups: the most Gaussian third, the middle third, and the least Gaussian third. We then evaluate PL-MIA separately on each group. Table 10 shows that attack performance degrades gradually rather than collapsing as the reference distributions become less Gaussian. On the most Gaussian third, PL-MIA achieves the best performance, with an AUC of 73 .03 , TPR@1%FPR of 17 .48 , and TPR@0.1%FPR of 9.62 . The performance degradation is mild across the three groups. Even on the least PAIRWISE LIKELIHOOD MIA 23          

> TABLE 10
> PL-MIA performance under different degrees of Gaussianity. Query points are partitioned into three equal-sized groups by the Gaussianity score G(q) = min Win (q), W out (q), where Win and Wout are the Shapiro-Wilk statistics of the IN and OUT reference distributions. Results are averaged over target models and reported as mean ±standard deviation.

Group AUC TPR@1%FPR TPR@0.1%FPR Most Gaussian 73 .03 ± 0.58 17 .48 ± 0.66 9.62 ± 0.75 

Middle third 72 .73 ± 0.40 16 .14 ± 0.80 7.94 ± 0.99 

Least Gaussian 71 .68 ± 0.31 14 .19 ± 0.60 7.45 ± 0.65 

Gaussian third, PL-MIA still achieves an AUC of 71 .68 , a TPR@1%FPR of 14 .19 , and a TPR@0.1%FPR of 7.45 . Thus, moving from the most Gaussian group to the least Gaus-sian group reduces AUC by only 1.35 percentage points, while the low-FPR TPR remains meaningful. These results suggest that moderate departures from Gaussianity do not prevent PL-MIA from extracting useful membership signal. The Gaussian approximation is used locally to estimate the pointwise IN/OUT likelihood ratio, whereas the final score aggregates evidence over many population comparisons; this aggregation may reduce the impact of local distri-butional misspecification. We therefore view Assumption 2.2 of the main manuscript as a practical modeling approximation rather than a strict empirical requirement. Replacing the Gaussian estimator with more flexible parametric or nonparametric density estimators is a natural direction for future work. 

B.4. Multi-Layer Perceptron on Purchase-100. To test whether PL-MIA general-izes to tabular data while the target model remains a neural network, we further evaluate PL-MIA on Purchase-100 using a Multi-Layer Perceptron (MLP) (Rumelhart, Hinton and Williams, 1986). This experiment complements the GBDT results in Section 5.3.4 of the main manuscript, which evaluates PL-MIA on a tree-based model. We use a four-layer MLP with hidden-layer sizes [512 , 256 , 128 , 64] , trained on 25k samples for 50 epochs. All attacks follow the same member/non-member evaluation protocol and use the same sets of target and reference models as in the main experiments.  

> TABLE 11
> Attack performance on Purchase-100 using the four-layer MLP model. Results are averaged over target models and reported with standard deviations.

Method AUC TPR@0.01%FPR TPR@0%FPR PL-MIA 84 .23 ± 0.33 3.70 ± 1.01 2.07 ± 0.90 

LiRA 83 .23 ± 0.30 1.95 ± 0.36 0.83 ± 0.28 

RMIA 83 .71 ± 0.30 3.20 ± 0.56 1.59 ± 0.64 

As shown in Table 11 and Figure 7, PL-MIA achieves the strongest performance among the compared attacks. It obtains an AUC of 84 .23 , compared with 83 .23 for LiRA and 83 .71 for RMIA. The advantage becomes more pronounced in the low-FPR regime: PL-MIA achieves 

3.70 TPR@0.01%FPR and 2.07 TPR@0%FPR, improving over RMIA’s 3.20 and 1.59 , re-spectively. These results further demonstrate that PL-MIA remains effective on tabular data even when the target model is a neural network. APPENDIX C: PROOFS This section provides detailed proofs of the theoretical results in Section 4 of the main manuscript. We first derive the GLR and BLR statistics (Lemmas 4.1 and 4.2), then establish 24 

Fig 7: ROC curves on Purchase-100 with the four-layer MLP model. The comparison evalu-ates PL-MIA against baselines under the tabular neural-network setting. the pairwise-difference distributions and the neutral baselines induced by population calibra-tion (Lemmas 4.3 and 4.4). Finally, we derive the attack-power expressions and prove the resulting theoretical ordering (Theorems 4.5 and 4.6). 

C.1. Proof of Lemma 4.1. 

PROOF . For a Gaussian distribution N (μ, σ 2), the log-density is: 

log pdf( x | N (μ, σ 2)) = − 12 log(2 π) − log σ − (x − μ)2

2σ2 .(27) It follows that the Gaussian LR (GLR) is 

φ(d)GLR = log pdf  conf (d) | N (μ(d)in , (σ(d)in )2) − log pdf  conf (d) | N (μ(d)out , (σ(d)out )2)

= − log σ(d)in − (conf (d) − μ(d)in )2

2( σ(d)in )2

!

− − log σ(d)out − (conf (d) − μ(d)out )2

2( σ(d)out )2

!

= log σ(d)out 

σ(d)in 

+ (conf (d) − μ(d)out )2

2( σ(d)out )2 − (conf (d) − μ(d)in )2

2( σ(d)in )2 .(28) Using σ(d)in = ρσ (d)out and η(d) = ( μ(d)in − μ(d)out )/σ (d)out , we obtain the two forms stated in Lemma 4.1. Let W ∼ N (0 , 1) denote a standard normal variable. 

Non-member case. Under H0, write conf (d) = μ(d)out + σ(d)out W . Substitution into Equa-tion 28 gives 

φ(d)GLR | H0 = − log ρ + W 2

2 − (W − η(d))2

2ρ2 .(29) 

Member case. Under H1, write conf (d) = μ(d)in + σ(d)in W = μ(d)out + σ(d)out (η(d) + ρW ). Sub-stitution into Equation 28 gives 

φ(d)GLR | H1 = − log ρ + (η(d) + ρW )2

2 − W 2

2 .(30) PAIRWISE LIKELIHOOD MIA 25 

We next compute the mean separation of GLR for ρ ≤ 1. From Equation 30, 

E[φ(d)GLR | H1] = − log ρ + E[( η(d) + ρW )2]2 − E[W 2]2= − log ρ + ρ2 + ( η(d))2 − 12 .(31) From Equation 29, 

E[φ(d)GLR | H0] = − log ρ + E[W 2]2 − E[( W − η(d))2]2ρ2

= − log ρ + 12 − 1 + ( η(d))2

2ρ2 .(32) Subtracting the two expectations gives 

∆μ(d)GLR = E[φ(d)GLR | H1] − E[φ(d)GLR | H0]=

 ρ2 + ( η(d))2 − 12



−

 12 − 1 + ( η(d))2

2ρ2



= (ρ2 − 1) 2 + ( η(d))2(1 + ρ2)2ρ2 .(33) When ρ < 1, the separation exceeds (η(d))2 and grows as O(1 /ρ 2) as ρ decreases. Thus, GLR captures variance contraction as an additional membership signal. For the pairwise analysis in Lemma 4.3, setting ρ = 1 in Equation 29 and Equation 30 gives 

φ(d)GLR | H1 = η(d)W + (η(d))2

2 ∼ N 

 (η(d))2

2 , (η(d))2



,(34) 

φ(d)GLR | H0 = η(d)W − (η(d))2

2 ∼ N 



− (η(d))2

2 , (η(d))2



.(35) These Gaussian forms provide the GLR inputs to the pairwise-difference derivation. 

C.2. Proof of Lemma 4.2. 

PROOF . Recall that the BLR pointwise statistic is φ(d)BLR := log( fθt (xd)[ yd]/¯c(d)), where 

¯c(d) = E[fθ(xd)[ yd]] is the average TLC over all reference models. Let h(u) = sigmoid( u),

g(u) = log h(u), and s(d) = h(μ(d)out ). Under H0 and H1, respectively, 

fθt (xd)[ yd] | H0 = h



μ(d)out + σ(d)out W



,fθt (xd)[ yd] | H1 = h



μ(d)out + σ(d)out (η(d) + ρW )



,

where W ∼ N (0 , 1) . By definition, ξ(d) = [1 − s(d)]σ(d)out .We first expand the normalizing term ¯c(d). Since the reference models are balanced be-tween the IN and OUT populations, ¯c(d) is the average of the two conditional expectations above. The first-order expansion 

h



μ(d)out + u



≈ s(d) + s(d)(1 − s(d))u26 

gives 

E

h

h



μ(d)out + σ(d)out W

i 

≈ s(d) + s(d)(1 − s(d))σ(d)out E[W ]= s(d),

and 

E

h

h



μ(d)out + σ(d)out (η(d) + ρW )

i 

≈ s(d) + s(d)(1 − s(d))σ(d)out E

h

η(d) + ρW 

i

= s(d) + s(d)(1 − s(d))η(d)σ(d)out ,

where we use E[W ] = 0 . Therefore, 

¯c(d) = 12

n

Eh(μ(d)out + σ(d)out W ) + Eh(μ(d)out + σ(d)out (η(d) + ρW )) o

≈ 12

n

s(d) + s(d) + s(d)(1 − s(d))η(d)σ(d)out 

o

= s(d)



1 + η(d)ξ(d)

2



,

Using log(1 + u) ≈ u then gives 

log ¯ c(d) ≈ log s(d) + log 



1 + η(d)ξ(d)

2



≈ log s(d) + η(d)ξ(d)

2 .(36) 

Member case. Since g(μ(d)out ) = log s(d) and g′(μ(d)out ) = 1 − s(d), under H1,

log fθt (xd)[ yd] = g



μ(d)out + σ(d)out (η(d) + ρW )



≈ log s(d) + (1 − s(d))σ(d)out (η(d) + ρW )= log s(d) + η(d)ξ(d) + ρξ (d)W. (37) Substituting Equation 36 into the BLR statistic yields 

φ(d)BLR | H1 ≈ η(d)ξ(d)

2 + ρξ (d)W. (38) 

Non-member case. Under H0, the same first-order expansion gives 

log fθt (xd)[ yd] = g



μ(d)out + σ(d)out W



≈ log s(d) + (1 − s(d))σ(d)out W

= log s(d) + ξ(d)W. (39) Combining this expression with Equation 36 yields 

φ(d)BLR | H0 ≈ − η(d)ξ(d)

2 + ξ(d)W. (40) PAIRWISE LIKELIHOOD MIA 27 

Since E[W ] = 0 , the first-order mean separation of BLR is 

∆μ(d)BLR = E[φ(d)BLR | H1] − E[φ(d)BLR | H0] ≈ η(d)ξ(d).(41) Thus, the contraction ratio ρ affects the first-order variance term of the BLR statistic, but not its first-order mean separation. This confirms that BLR does not use variance contraction as a mean-separation signal. 

C.3. Proof of Lemma 4.3. 

PROOF . At ρ = 1 , the GLR forms derived above are Gaussian. For fixed points q and z,let Wq, W z ind 

∼ N (0 , 1) . For independent random variables X and Y ,

E[X − Y ] = E[X] − E[Y ], Var[ X − Y ] = Var[ X] + Var[ Y ].(42) 

GLR-induced difference. From Lemma 4.1, conditional on η(q) and η(z),• a member query satisfies φ(q)GLR ∼ N 

 (η(q))2 

> 2

, (η(q))2



;• a non-member population point satisfies φ(z)GLR ∼ N 



− (η(z))2 

> 2

, (η(z))2



.Therefore, for a member query, 

φ(q)GLR − φ(z)GLR | H1 ∼ N 

 (η(q))2 + ( η(z))2

2 , (η(q))2 + ( η(z))2



.(43) 

BLR-induced difference. From Lemma 4.2, conditional on (η(q), ξ (q)) and (η(z), ξ (z)),• a member query satisfies φ(q)BLR ∼ N 

 η(q)ξ(q) 

> 2

, (ξ(q))2



;• a non-member population point satisfies φ(z)BLR ∼ N 



− η(z)ξ(z) 

> 2

, (ξ(z))2



.Therefore, for a member query, 

φ(q)BLR − φ(z)BLR | H1 ∼ N 

 η(q)ξ(q) + η(z)ξ(z)

2 , (ξ(q))2 + ( ξ(z))2



.(44) We finally consider a non-member query. Under H0, both q and z follow their respective non-member distributions, giving 

φ(q)GLR − φ(z)GLR | H0 ∼ N 

 (η(z))2 − (η(q))2

2 , (η(q))2 + ( η(z))2



,(45) 

φ(q)BLR − φ(z)BLR | H0 ∼ N 

 η(z)ξ(z) − η(q)ξ(q)

2 , (ξ(q))2 + ( ξ(z))2



.(46) Although the conditional means need not be zero, the marginal null distributions are sym-metric about zero. Indeed, exchanging the i.i.d. non-member draws Q and Z reverses the sign of the pairwise difference without changing its distribution. This symmetry leads directly to the neutral baselines in Lemma 4.4. 

C.4. Proof of Lemma 4.4. 

PROOF . We establish the two neutral baselines in turn. 28 

Binary-voting population calibration. For binary-voting population calibration, we use the neutral threshold γ = 0 . Let Q and Z be independent non-member points drawn from the same data distribution. Under H0, the pair (Q, Z ) is exchangeable, and hence 

∆( Q, Z ) d

= ∆( Z, Q ) = −∆( Q, Z ).(47) Since the pairwise difference is continuous, ties have probability zero. Therefore, 

Pr [∆( Q, Z ) > 0 | H0] = Pr [∆( Q, Z ) < 0 | H0] = 12 .(48) The binary score is the query point’s population win probability. Taking the expectation over a random non-member query gives 

E [Score Binary (Q) | H0] = 12 ,(49) which establishes the binary-voting baseline in Lemma 4.4 of the main manuscript. 

Cauchy-combination population calibration. Let P = p(Q, Z ) be an exact continuous pairwise null p-value. Under H0, P ∼ Uniform(0 , 1) . Define the Cauchy evidence trans-formation (Liu and Xie, 2020) 

T (P ) = tan((0 .5 − P )π).(50) For any t ∈ R,

Pr [ T (P ) ≤ t | H0] = Pr 



P ≥ 12 − 1

π arctan( t) | H0



= 12 + 1

π arctan( t).(51) This is the CDF of a standard Cauchy random variable. Hence, each null pairwise p-value is mapped to Cauchy evidence with median zero and undefined mean: 

Median ( T (p(Q, Z )) | H0) = 0 .(52) The Cauchy-combined PL-MIA score averages these transformed pairwise evidence terms. Under independence, Cauchy stability gives an exact median-zero Cauchy score. In the pair-wise setting, the terms share the same query point; the score-level approximation used in the main manuscript is therefore 

Median (Score PL (Q) | H0) ≈ 0.(53) This establishes the Cauchy-combination baseline in Lemma 4.4 of the main manuscript. The median is used in place of the expectation because Cauchy evidence has no finite mean. 

C.5. Proof of Theorem 4.5. 

PROOF . We first derive the Gaussian-CDF form for LiRA, RMIA, and PL-MIA +. For each attack A, let SA denote the corresponding pointwise statistic before binary popu-lation calibration. Binary population calibration applies an increasing null-CDF transfor-mation to this statistic and therefore preserves its rejection region and attack power. It is thus sufficient to derive the Gaussian power expression using SA. At ρ = 1 , for A ∈{LiRA , RMIA , PL -MIA +}, write the null and member score distributions as 

SA | H0 ∼ N (μA 

> 0

, (σA)2),(54) 

SA | H1 ∼ N (μA 

> 1

, (σA)2).(55) PAIRWISE LIKELIHOOD MIA 29 

At fixed FPR α, the null threshold is 

τ Aα = μA 

> 0

+ σAΦ−1(1 − α).(56) Therefore, 

TPR A(α) = Pr SA > τ Aα | H1



= 1 − Φ

 τ Aα − μA

> 1

σA



= Φ 

 μA 

> 1

− μA

> 0

σA

− Φ−1(1 − α)



.(57) This is the standard fixed-level Gaussian power calculation used in likelihood-ratio test-ing and LiRA-style MIA analyses (Neyman and Pearson, 1933). By the rejection-region preservation above, the same expression gives the attack power of the corresponding binary-calibrated attack. Defining 

κA := μA 

> 1

− μA

> 0

σA

(58) gives the Gaussian attack-power expression in Theorem 4.5 of the main manuscript. Since 

Φ( ·) is strictly increasing, a larger κA gives a higher TPR at the same FPR. We next derive the low-FPR expression for the Cauchy-combined PL-MIA score. The score is 

Score PL (Q) = 1

|Z|

X

> z∈Z

T (p(Q, z )) , T (p) = tan((0 .5 − p)π).(59) The analytic Cauchy upper-tail threshold at FPR α is 

τα = tan( π(0 .5 − α)) .(60) 

In the low-FPR regime, the upper tail of the Cauchy average is dominated by one or a few very large transformed terms (Liu and Xie, 2020). A transformed term reaches the scale |Z|τα when 

T (p(Q, z )) ≥ | Z|τα.(61) Since T (p) is strictly decreasing in p, this corresponds to 

p(Q, z ) ≤ 12 − 1

π arctan ( |Z|τα) := uα, |Z|.(62) For small α, uα, |Z| ≈ α/ |Z|. Let 

rα = Pr p(Q, Z ) ≤ uα, |Z| | H1

 .(63) Here, rα is the probability that one pairwise comparison from a member query produces such a very small p-value. Approximating the upper-tail event of the Cauchy average by the event that at least one population comparison produces such evidence gives 

TPR PL -MIA (α) ≈ Pr ∃z ∈ Z : p(Q, z ) ≤ uα, |Z| | H1

 .(64) Applying the product approximation over the |Z| population comparisons yields 

TPR PL -MIA (α) ≈ 1 − (1 − rα)|Z|

(65) Equation 65 establishes the Cauchy-combination attack-power expression in Theorem 4.5 of the main manuscript. It characterizes the low-FPR regime in which one or a few very small pairwise p-values drive the upper tail of the Cauchy average. Lemma 4.3 explains why member queries are more likely to produce such evidence: larger pairwise differences correspond to smaller p-values. 30 

C.6. Proof of Theorem 4.6. 

PROOF . We prove the three comparisons in turn. 

Part (I): PL -MIA + versus LiRA. Let G(q) := φ(q)GLR , and let F0,G (t) := Pr[ G(Z) ≤ t |

H0] denote the marginal null CDF of the GLR score, where Z is an independent non-member population draw. Under ρ = 1 , Lemma 4.1 shows that the null GLR score is conditionally Gaussian for every point. Hence, F0,G is continuous and strictly increasing. With population calibration, the binary score is the probability that the query GLR score exceeds that of a non-member population point. Conditional on G(q), the query score is fixed, and therefore 

Score Binary (q) = Pr  

> Z

[G(Z) < G (q) | G(q)] = F0,G (G(q)) .(66) Thus, population calibration maps the raw GLR score of q to its percentile under the non-member distribution. 

For a non-member query Q, the random variable G(Q) follows the same null CDF F0,G .The probability-integral transform therefore gives 

Score Binary (Q) | H0 ∼ Uniform(0 , 1) .(67) Consequently, the size-α rejection region of PL -MIA + is 

{F0,G (G(Q)) > 1 − α} =

n

G(Q) > F −10,G (1 − α)

o

.(68) The event on the right is precisely the size-α rejection event obtained by thresholding the original GLR score, as in LiRA. Hence, the two attacks have the same rejection event and therefore the same attack power: 

TPR PL -MIA + (α) = TPR LiRA (α).(69) When σ2 

> η

> 0, the raw GLR distributions vary across query points. Equation 66 expresses these heterogeneous scores on the common null-percentile scale [0 , 1] , while preserving their ordering and attack power. This establishes Part (I). 

Part (II): PL -MIA + versus RMIA. Both attacks use population calibration, so their dif-ference lies in the pointwise statistic: PL -MIA + uses GLR, whereas RMIA uses BLR. Let (70) G := φ(Q)GLR , B := φ(Q)BLR .

For either pointwise statistic S ∈ { G, B }, binary population calibration maps S to its null percentile F0,S (S). Since this transformation is increasing, it leaves the rejection region in-duced by S unchanged. It is therefore sufficient to compare GLR and BLR before calibration. 

Variance contraction: ρ < 1. By Equation (5) and Lemma 4.1 of the main manuscript, GLR is the likelihood-ratio statistic for the observable confidence score. Under H0, expand-ing its expression gives 

G(W ) = − log ρ + W 2

2 − (W − η(Q))2

2ρ2

= 1 − ρ−2

2 W 2 + η(Q)

ρ2 W − log ρ − (η(Q))2

2ρ2 .(71) Its coefficient on W 2 is (1 − ρ−2)/2 < 0. Hence, G(W ) is a concave quadratic function, and a nontrivial upper GLR rejection region therefore has the form {W : G(W ) > c G} =(w−, w +), which is a bounded interval. In contrast, the exact BLR statistic is 

B(W ) = log sigmoid 



μ(Q)out + σ(Q)out W



− log ¯ c(Q).(72) PAIRWISE LIKELIHOOD MIA 31 

For a fixed query, ¯c(Q) is constant, and 

dB(W )dW = σ(Q)out 

h

1 − sigmoid 



μ(Q)out + σ(Q)out W

i 

> 0.(73) Thus, BLR is strictly increasing in W , and its upper rejection region has the form {W :

B(W ) > c B } = ( wB , ∞), which is a half-line. A bounded interval and a half-line cannot coincide. Their symmetric difference therefore contains a nonempty interval, which has positive null probability because W ∼ N (0 , 1) has a strictly positive density on R. By the Neyman-Pearson lemma (Neyman and Pearson, 1933), the GLR rejection region is most powerful among size-α tests. Since its null distribution is continuous and the two rejection regions differ on a set of positive null probability, the comparison is strict. Population calibration preserves both rejection regions, and hence 

TPR PL -MIA + (α) > TPR RMIA (α), ρ < 1.(74) 

ξ-heterogeneity: ρ = 1 . We now fix η(Q) = η0 > 0 to isolate ξ-heterogeneity. Let ¯ξ := 

E[ξ(Q)] and Var( ξ(Q)) = σ2 

> ξ

. At ρ = 1 , Lemma 4.1 gives 

G | H1 = η0W + η20

2 , G | H0 = η0W − η20

2 .(75) The GLR mean separation and common within-class variance are therefore 

∆μGLR = η20 , σ2GLR = η20 .(76) Since η0 > 0,

κGLR = ∆μGLR 

σGLR 

= η0.(77) Using the first-order BLR score in Lemma 4.2 gives 

B | H1 ≈ ξ(Q) 

W + η0

2



, B | H0 ≈ ξ(Q) 

W − η0

2



.(78) Because W is independent of ξ(Q), with E[W ] = 0 and E[W 2] = 1 , the class means satisfy 

E[B | H1] ≈ η0 ¯ξ

2 , E[B | H0] ≈ − η0 ¯ξ

2 .(79) Thus, the BLR mean separation is ∆μBLR ≈ η0 ¯ξ. Moreover, E(ξ(Q))2 = ¯ξ2 + σ2 

> ξ

. The covariance between ξ(Q)W and ξ(Q) is zero because W is independent of ξ(Q) and E[W ] = 0 .Hence, under either hypothesis, 

Var( B | Hb) ≈ Var 



ξ(Q)W



+ η20

4 Var 



ξ(Q)

= E

h

(ξ(Q))2i

+ η20

4 σ2

> ξ

= ¯ξ2 +



1 + η20

4



σ2 

> ξ

, b ∈ { 0, 1}.(80) It follows that 

κBLR = η0 ¯ξ

q ¯ξ2 +  1 + η20 /4 σ2

> ξ

.(81) 32 

Under Assumption 2.2, ξ(Q) > 0 and hence ¯ξ > 0. Therefore, when σ2 

> ξ

> 0,

κBLR = η0 ¯ξ

q ¯ξ2 +  1 + η20 /4 σ2

> ξ

< η 0 = κGLR , σ2 

> ξ

> 0.(82) Since the Gaussian attack-power expression in Theorem 4.5 is strictly increasing in κA, this yields 

TPR PL -MIA + (α) > TPR RMIA (α).(83) Together, Equation 74 and Equation 83 establish Part (II). 

Part (III): PL-MIA versus PL -MIA +. Recall that rα is the probability that a single pairwise comparison from a member query produces an exceptionally small p-value. The low-FPR expression in Theorem 4.5 approximates the power of PL-MIA by the probability that at least one of the |Z| comparisons produces such evidence: 

TPR PL -MIA (α) ≈ 1 − (1 − rα)|Z|.(84) Since 1 − x ≤ exp( −x) for x ∈ [0 , 1] , we have (1 − rα)|Z| ≤ exp( −| Z|rα) and therefore 

1 − (1 − rα)|Z| ≥ 1 − exp( −| Z|rα).(85) Under the condition in Part (III), 

1 − exp( −| Z|rα) > TPR PL -MIA + (α).(86) Within this low-FPR approximation, combining Equations 84–86 gives 

TPR PL -MIA (α) > TPR PL -MIA + (α).(87) This establishes Part (III) and completes the proof. REFERENCES 

BACKES , M., B ERRANG , P., H UMBERT , M. and M ANOHARAN , P. (2016). Membership privacy in MicroRNA-based studies. In Proceedings of the 2016 ACM SIGSAC Conference on Computer and Communications Secu-rity 319–330. BERTRAN , M., T ANG , S., R OTH , A., K EARNS , M., M ORGENSTERN , J. H. and W U, S. Z. (2023). Scalable membership inference attacks via quantile regression. Advances in Neural Information Processing Systems 36 

314–330. CARLINI , N., C HIEN , S., N ASR , M., S ONG , S., T ERZIS , A. and T RAMER , F. (2022). Membership inference attacks from first principles. In 2022 IEEE symposium on security and privacy (SP) 1897–1914. IEEE. DARLOW , L. N., C ROWLEY , E. J., A NTONIOU , A. and S TORKEY , A. J. (2018). Cinic-10 is not imagenet or cifar-10. arXiv preprint arXiv:1810.03505 .HAYES , J., SHUMAILOV , I., CHOQUETTE -C HOO , C. A., JAGIELSKI , M., KAISSIS , G., NASR , M., GHALEBIKESABI , S., A NNAMALAI , M. S. M. S., M IRESHGHALLAH , N., S HILOV , I. et al. (2025). Exploring the limits of strong membership inference attacks on large language models. arXiv preprint arXiv:2505.18773 .HERNANDEZ AROS , L., B USTAMANTE MOLANO , L. X., G UTIERREZ -P ORTELA , F., M ORENO HERNAN -

> DEZ

, J. J. and R ODRÍGUEZ BARRERO , M. S. (2024). Financial fraud detection through the application of machine learning techniques: a literature review. Humanities and Social Sciences Communications 11 1–22. HOMER , N., S ZELINGER , S., R EDMAN , M., D UGGAN , D., T EMBE , W., M UEHLING , J., P EARSON , J. V., STEPHAN , D. A., N ELSON , S. F. and C RAIG , D. W. (2008). Resolving individuals contributing trace amounts of DNA to highly complex mixtures using high-density SNP genotyping microarrays. PLoS genetics 

4 e1000167. JAYARAMAN , B., W ANG , L., K NIPMEYER , K., G U, Q. and E VANS , D. (2020). Revisiting membership inference under realistic assumptions. arXiv preprint arXiv:2005.10881 .KE, G., M ENG , Q., F INLEY , T., W ANG , T., C HEN , W., M A, W., Y E, Q. and L IU , T.-Y. (2017). Lightgbm: A highly efficient gradient boosting decision tree. Advances in neural information processing systems 30 .KRIZHEVSKY , A., H INTON , G. et al. (2009). Learning multiple layers of features from tiny images. PAIRWISE LIKELIHOOD MIA 33 

LIU , Y. and X IE , J. (2020). Cauchy combination test: a powerful test with analytic p-value calculation under arbitrary dependency structures. Journal of the American Statistical Association 115 393–402. LIU , Y., C HEN , S., L I, Z., M ORRISON , A. C., B OERWINKLE , E. and L IN , X. (2019). ACAT: a fast and powerful p value combination method for rare-variant analysis in sequencing studies. The American Journal of Human Genetics 104 410–421. NEYMAN , J. and P EARSON , E. S. (1933). IX. On the problem of the most efficient tests of statistical hypotheses. 

Philosophical Transactions of the Royal Society of London. Series A, Containing Papers of a Mathematical or Physical Character 231 289–337. RUMELHART , D. E., H INTON , G. E. and W ILLIAMS , R. J. (1986). Learning representations by back-propagating errors. nature 323 533–536. SABLAYROLLES , A., D OUZE , M., S CHMID , C., O LLIVIER , Y. and J ÉGOU , H. (2019). White-box vs black-box: Bayes optimal strategies for membership inference. In International Conference on Machine Learning 

5558–5567. PMLR. SALEM , A., Z HANG , Y., H UMBERT , M., B ERRANG , P., F RITZ , M. and B ACKES , M. (2018). Ml-leaks: Model and data independent membership inference attacks and defenses on machine learning models. arXiv preprint arXiv:1806.01246 .SHAPIRO , S. S. and W ILK , M. B. (1965). An analysis of variance test for normality (complete samples). 

Biometrika 52 591–611. SHOKRI , R., S TRONATI , M., S ONG , C. and S HMATIKOV , V. (2017). Membership inference attacks against machine learning models. In 2017 IEEE symposium on security and privacy (SP) 3–18. IEEE. TAO , J. and S HOKRI , R. (2026). Information-Theoretic Membership Inference for Granular Quantification of Memorization. In The Fourteenth International Conference on Learning Representations .VAN DER VAART , A. W. (2000). Asymptotic statistics 3. Cambridge university press. WATSON , L., G UO , C., C ORMODE , G. and S ABLAYROLLES , A. (2021). On the importance of difficulty calibra-tion in membership inference attacks. arXiv preprint arXiv:2111.08440 .WEN , Y., B ANSAL , A., K AZEMI , H., B ORGNIA , E., G OLDBLUM , M., G EIPING , J. and G OLDSTEIN , T. (2023). Canary in a Coalmine: Better Membership Inference with Ensembled Adversarial Queries. In The Eleventh International Conference on Learning Representations .YE, J., M ADDI , A., M URAKONDA , S. K., B INDSCHAEDLER , V. and S HOKRI , R. (2022). Enhanced membership inference attacks against machine learning models. In Proceedings of the 2022 ACM SIGSAC conference on computer and communications security 3093–3106. YEOM , S., G IACOMELLI , I., F REDRIKSON , M. and J HA , S. (2018). Privacy risk in machine learning: Analyzing the connection to overfitting. In 2018 IEEE 31st computer security foundations symposium (CSF) 268–282. IEEE. ZAGORUYKO , S. and K OMODAKIS , N. (2016). Wide residual networks. arXiv preprint arXiv:1605.07146 .ZARIFZADEH , S., L IU , P. and S HOKRI , R. (2024). Low-cost high-power membership inference attacks. In Pro-ceedings of the 41st International Conference on Machine Learning .ZHU , M., G UO , C., F ENG , C. and S IMEONE , O. (2025). On the Impact of Uncertainty and Calibration on Likelihood-Ratio Membership Inference Attacks. IEEE Transactions on Information Forensics and Security .