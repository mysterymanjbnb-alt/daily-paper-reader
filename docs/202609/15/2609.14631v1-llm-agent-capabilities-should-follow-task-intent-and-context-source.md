---
title: LLM Agent Capabilities Should Follow Task Intent and Context Source
title_zh: LLM Agent 能力应遵循任务意图与上下文来源
authors: "Yusheng Zheng, Wenhui Zhang, Yu Mao"
date: 2026-09-13
pdf: "https://arxiv.org/pdf/2609.14631v1"
tags: ["query:ai-agents"]
score: 9.0
evidence: 智能体安全：能力应遵循任务意图与上下文来源以抵御注入
tldr: LLM Agent 能执行代码、改文件、调服务，但用户请求、工具结果、文档、记忆等上下文源共享同一规划通道，易被注入或意外扩权。本文提出按任务意图而非沙箱或会话生命周期限定能力，并设计 IntentCap：从用户意图、工作流指令、工具 schema 与运行环境合成字段级权限租约，单调收窄。确定性检查器在副作用提交前验证，工具与 OS 层信息流策略执行。评估显示其能阻断违规且不误拒正常操作，各源边界均必要，为 Agent 安全提供任务级多源权限组合思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.14631v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 888, \"height\": 283, \"label\": \"Figure\"}]"
motivation: Agent 能力需随任务动态最小化，但多源输入同等影响规划，用户请求与文档文本信任不同，现有防御未解决任务级多源权限组合。
method: IntentCap 从用户意图、工作流、工具 schema 和运行环境四源生成短期租约，字段级归属且只能收窄用户授权，检查器在副作用前校验并落实信息流策略。
result: 评估表明 IntentCap 阻断测试违规且不误拒良性操作，四类源边界各自必要，检查器可泛化至工具、执行、部署和委派边界。
conclusion: 应将 Agent 能力绑定当前任务意图，以多源字段级租约和确定性检查实现任务范围最小权限，兼顾安全与可用。
---

## 摘要
LLM 智能体执行真实动作，包括执行代码、修改文件、调用服务以及委派任务，这些动作由上下文来源驱动：用户请求、工具结果、文档、shell 输出、Skill 和 MCP 指令、记忆。与传统系统中能力被预先定义不同，智能体所需的最小权限能力是动态的，取决于其任务意图：它想做什么以及如何做。这带来了安全与保障挑战：所有输入都进入同一个共享规划通道并具有同等影响，无论是通过对抗性注入还是意外范围扩大，但用户的显式请求与文档的提取文本具有不同的信任级别，不应共享选择目的地或扩大访问范围的权限。现有防御措施约束操作和信息流；我们研究任务范围限定、多来源的权限组合。我们认为，智能体能力应限定于当前任务意图，而不是沙箱或会话生命周期，并且没有任何单一上下文来源能定义完整能力。IntentCap 从四类此类来源组合能力：用户意图、工作流指令、工具 schema 和运行时环境，并具有字段级所有权和单调收窄。每个来源贡献特定字段，任何来源都不能填补另一来源的字段，并且租约只会收窄用户已授权的权限，绝不会扩大它。IntentCap 使用 LLM 从这些来源生成短期租约，在任何副作用提交之前由确定性检查器验证，并通过工具级和 OS 级信息流策略强制执行。评估表明，IntentCap 能阻止所测试的违规行为，而不会拒绝良性动作；每个来源边界都是独立必要的；并且检查器可泛化到工具、执行、放置和委派边界。

## Abstract
LLM agents take real actions, including executing code, modifying files, calling services, and delegating tasks, driven by context sources: user requests, tool results, documents, shell outputs, Skill and MCP instructions, memory. Unlike traditional systems, where capability is predefined, the least-privilege capability an agent needs is dynamic, depending on its task intent: what it wants to do and how. This creates a security and safety challenge: all inputs enter one shared planning channel with equal influence, by adversarial injection or accidental scope widening, yet a user's explicit request and a document's extracted text carry different trust and should not share authority to choose a destination or widen access. Existing defenses constrain operations and information flow; we study task-scoped, multi-source authority composition.   We argue that agent capabilities should be scoped to the current task intent, not a sandbox or session lifetime, and that no single context source defines a complete capability. IntentCap composes capabilities from four such sources: user intent, workflow instructions, tool schemas, and runtime environment, with field-level ownership and monotonic narrowing. Each source contributes specific fields, none can fill another's, and the lease only narrows the user's authorized authority, never widens it. IntentCap uses an LLM to generate short-lived leases from these sources, validated by a deterministic checker before any side effect commits and enforced by tool- and OS-level information flow policies. Evaluation shows IntentCap blocks tested violations without rejecting benign actions, each source boundary is independently necessary, and the checker generalizes across tool, execution, placement, and delegation boundaries.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义

- **论文定位**：这是一篇 3 页短文/立场与初步评估论文，作者为 Yusheng Zheng、Wenhui Zhang、Yu Mao，提出 **IntentCap**，主张 LLM Agent 的能力应绑定“当前任务意图”，而非沙箱或会话生命周期。
- **核心问题**：LLM Agent 会执行代码、改文件、调用服务、委派子任务，其行为受多种上下文来源驱动：用户请求、工具结果、文档、shell 输出、Skill/MCP 指令、记忆等。问题在于，这些来源进入同一个共享规划通道，并具有同等影响力。
- **信任不对称**：用户显式请求与文档中提取的文本、工具输出、网页内容等具有不同信任级别，但现有 Agent 架构中它们可能都能影响“目的地选择”“权限范围”“工具选择”等关键决策字段。
- **攻击与意外风险**：
  - 对抗性注入：例如 PDF 隐藏文本诱导 Agent 把 GitHub issue 创建到 `attacker/repo`。
  - 意外扩权：例如 Skill 把 PDF 文本拼接进输出，planner 误把表头解析成仓库名，覆盖用户选择。
- **现有防御不足**：已有工作强调控制/数据分离、工具权限、执行隔离、信息流控制、OS 策略等，但未系统解决“任务范围限定的多源权限组合”问题。
- **整体含义**：Agent 所需最小权限是动态的，取决于任务意图；完整能力无法由单一来源定义，必须从多个上下文来源按字段组合，并保证只收窄、不扩权。

## 2. 方法论：IntentCap 的核心思想与关键技术

- **核心主张**：
  - Agent 能力应限定于当前任务意图，而不是整个沙箱或会话。
  - 能力应由四类上下文来源组合：**用户意图、工作流指令、工具 schema、运行时环境**。
  - 每个决策字段有且仅有一个授权来源，任何来源不能填写其他来源的字段。
  - 租约只能单调收窄用户已授权权限，绝不能扩权。

- **四类
