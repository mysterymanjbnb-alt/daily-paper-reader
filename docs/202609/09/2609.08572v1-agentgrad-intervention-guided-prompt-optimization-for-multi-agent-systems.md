---
title: "AgentGrad: Intervention-guided Prompt Optimization for Multi Agent Systems"
title_zh: AgentGrad：面向多智能体系统的干预引导提示优化
authors: "Jaewon Chu, Jinwoo Seo, Jaewon Cho, Jeehye Na, Yunyang Xiong, Youngdae Kim, Hyunwoo J. Kim"
date: 2026-09-08
pdf: "https://arxiv.org/pdf/2609.08572v1"
tags: ["query:ai-agents"]
score: 9.0
evidence: 面向大模型多智能体系统的干预引导提示优化
tldr: 现有文本梯度方法在多智能体提示优化中，存在目标智能体选择未经验证、缺乏智能体级监督、梯度聚合混杂无关失效模式等弊端。AgentGrad提出顺序干预和语义梯度抽象：逐智能体干预定位真正的失效源头，以其修正输出作为监督生成细粒度梯度，并按语义聚类提炼泛化性梯度。在五个基准上达到最佳结果，优化时间平均减少2.5倍。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.08572v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1439, \"height\": 596, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.08572v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 702, \"height\": 834, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.08572v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1436, \"height\": 343, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.08572v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 583, \"height\": 371, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.08572v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1432, \"height\": 1058, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.08572v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1429, \"height\": 278, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.08572v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1427, \"height\": 280, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.08572v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 736, \"height\": 279, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.08572v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1240, \"height\": 295, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.08572v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1451, \"height\": 314, \"label\": \"Table\"}]"
motivation: 现有文本梯度方法在梯度提取时未验证目标提示修改能否解决失效，且缺少智能体级监督；梯度聚合随意分组，混杂无关失效模式。
method: AgentGrad对每次失败顺序干预单智能体行为，定位可解决问题的目标智能体；用其修正输出作为监督提取细粒度文本梯度，并语义聚类为泛化梯度。
result: 在五个多智能体基准上取得最优性能，相比下一最快基线平均降低2.5倍优化时间。
conclusion: AgentGrad以验证干预和语义抽象实现更精准的梯度，显著提升多智能体系统提示优化的效果与效率。
---

## 摘要
基于大型语言模型（LLM）的多智能体系统（MAS）通过采用专业化的多个智能体实现了强性能，但其性能取决于每个智能体的提示设计。对于MAS提示优化，利用自然语言反馈指导提示更新的文本梯度方法已成为主流范式。在本文中，我们指出现有文本梯度方法在梯度提取和梯度聚合两个阶段的局限性。在梯度提取中，先前的工作选择目标提示时未验证修改它是否能解决失败，并且在没有对应智能体中间输出的智能体级监督下推导梯度。在梯度聚合中，单个梯度被随机分组并拼接，常常混合不相关的失败模式，生成的提示无法泛化。为解决这些局限性，我们提出AgentGrad，一种基于顺序干预和语义文本梯度抽象的多智能体系统提示优化框架。对于每个失败案例，顺序干预每次修改一个智能体的行为，以识别其修改能解决失败的目标智能体。目标智能体的修改后输出随后作为智能体级监督，用于提取细粒度梯度。语义文本梯度抽象对语义相似的梯度进行聚类，以防止混合不相关的失败模式，并将每个聚类抽象为捕获共享纠正模式的泛化梯度。实验结果表明，AgentGrad在五个MAS基准上实现了最先进的性能，并且与次快基线相比，平均将优化墙钟时间减少了2.5倍。

## Abstract
Large language model (LLM)-based multi-agent systems (MAS) achieve strong performance by employing specialized multiple agents, yet their performance depends on the prompt design of each agent. For MAS prompt optimization, textual gradient methods that guide prompt updates using natural-language feedback have emerged as a leading paradigm. In this paper, we identify limitations in two stages of existing textual gradient approaches: gradient extraction and gradient aggregation. In gradient extraction, previous works select a target prompt without verifying whether modifying it resolves the failure, and derive gradients without agent-level supervision over the corresponding agent's intermediate output. In gradient aggregation, individual gradients are randomly grouped and concatenated, often mixing unrelated failure modes and producing prompts that fail to generalize. To address these limitations, we propose \textbf{AgentGrad}, a prompt optimization framework for multi-agent systems based on sequential intervention and semantic textual gradient abstraction. For each failure, sequential intervention modifies the behavior of one agent at a time to identify the target agent whose modification resolves the failure. The modified output of the target agent then serves as agent-level supervision for extracting a fine-grained gradient. Semantic textual gradient abstraction clusters semantically similar gradients to prevent mixing unrelated failure modes, and abstracts each cluster into a generalized gradient that captures the shared corrective pattern. Experimental results show that AgentGrad achieves state-of-the-art performance across five MAS benchmarks and reduces wall-clock optimization time by $2.5\times$ on average compared to the next-fastest baseline.

---

## 论文详细总结（自动生成）

### 1. 研究动机与核心问题

- **研究背景**：基于大型语言模型（LLM）的多智能体系统（Multi-Agent Systems, MAS）在复杂任务中表现出色，它通过多个专业化智能体的分工协作，实现多跳推理、规划与信息综合等能力。MAS 中每个智能体的行为由其输入提示（Prompt）决定，因而提示设计直接决定系统整体性能，提示优化也成为提升 MAS 能力的核心途径。
- **已有方法的范式**：现有提示优化方法中，文本梯度（Textual Gradient）方法占据主导地位——它利用自然语言反馈作为"梯度"信号，迭代指导提示修改。典型代表包括 TextGrad、GEPA、MIPROv2 等。
- **本文指出的两大阶段缺陷**：
  - **梯度提取阶段**：
    - 选择目标提示时**未经验证**：不清楚修改哪个智能体的提示就能真正解决失效；
    - 提取梯度时**缺少智能体级监督**：现有方法没有利用目标智能体的中间输出进行细粒度监督，通常只能访问系统级输出，难以定位具体偏差来源。
  - **梯度聚合阶段**：
    - 现有方法将样本级梯度**随机分组并直接拼接**，容易混合无关的失败模式，导致提示更新方向不连贯、难以泛化。

### 2. 方法论：AgentGrad

AgentGrad 是一种面向 MAS 的提示优化框架，核心由两大机制构成：

#### （1）顺序干预（Sequential Intervention）——解决梯度提取问题

- **目标识别**：对于来自训练集 Dtrain 中的每个失败样本 (xi, yi)，仅需找出"其修正能单独解决系统失败"的目标智能体。按照**反向执行顺序**，逐一对某一智能体注入提示（Hint，由正确输出/约束条件和辅助上下文构成，仅在训练阶段使用，推断时不注入）进行干预，验证该智能体行为修正后，系统最终输出是否正确。能解决失败的那一个智能体即被识别为目标智能体。
- **目标安全分离**：按上述方法逐步从"未解决失败集 Fn+1"中移除"被当前智能体修正解决"的失败子集 Tn；无法通过干预修复的样本暂不纳入当前轮训练，下轮重新参与。
- **提取细粒度梯度**：对每个目标智能体 πn，获得其在同一输入下的**原始输出 ŷₙ**与**干预后输出 ỹₙ**。两输出对比可作为该智能体的行为纠正方向，进而由梯度提取 LLM 生成样本级文本梯度 δₙ = LLM∇(pₙ, xₙ, ŷₙ, ỹₙ)，无需显式的系统级损失函数。

#### （2）语义文本梯度抽象（Semantic Textual Gradient Abstraction）——解决梯度聚合问题

- **语义聚类**：将给定智能体的所有样本级文本梯度 Ωₙ 交由聚合 LLM 聚类成语义一致的组（语义 minibatch），聚类时提供软性簇大小下限，并采用循环调度（如 5→3→1）平衡聚合粒度（粗粒度共性模式与细粒度局部修正）。
- **泛化抽象**：每个语义簇提炼为一条"广义文本梯度" δ̄，捕获簇内共享的修正模式，为后续提示更新提供唯一连贯方向，避免多条无关信号混杂。

#### （3）提示更新与验证

- 更新顺序按广义梯度的聚类样本量降序排列，先应用广泛有效的更新，再处理细粒度修正；
- 候选提示先在其语义 minibatch上评估，若有效，再在验证集上评估，两阶段均通过才接受更新。

#### 算法流程（文字概述）

1. 用当前提示集合在训练集上收集失败集；
2. 对每个失败样本按 n=N→1 反向注入干预，记录每个智能体可解决的子集及其干预前后输出；
3. 为每个智能体生成样本级文本梯度；
4. 聚合 LLM 聚类并抽象为广义梯度；
5. 生成候选提示并在语义 minibatch 和验证集上验证后决定是否更新；
6. 在给定 rollout 预算内迭代至退出。

### 3. 实验设计

- **数据集/场景（5个基准）**：
  - HotpotQA（多跳问答）
  - HoVer（事实验证的声明核验）
  - IFBench（指令遵循）
  - PUPA（面向隐私保护的委派）
  - MATH（数学推理）
- **骨干模型（Backbone）**：GPT-5-mini（闭源）和 Qwen3-8B（开源），同一模型同时担任任务 LLM 与优化器组件。
- **对比方法**：
  - MIPROv2
  - TextGrad
  - GEPA
  - 无优化基线（No PO）
- **评估与指标**：在固定 rollout 预算内优化，报告测试集性能，并额外统计优化墙钟时间、minibatch/validation改进率、跨域迁移性能。

### 4. 资源与算力

文本并未明确指出训练所使用的 GPU 型号、数量、显存或总训练算力等具体资源配置信息，也未详述单个 LLM 调用开销、推理总量等资源统计数据，仅在时间维度报告了五个基准上的"优化墙钟时间"（见 Table 4）。因此，关于算力的精确细节无法从论文内容中总结。

### 5. 实验数量与充分性

- **主实验**：两个骨干模型 × 五个基准；各基准报告了 3 个随机种子的均值和标准误，实验结果具有统计意义。
- **消融实验**：在 HotpotQA 和 PUPA 上逐步加入三类组件（TI：干预目标识别；AS：智能体级监督；STGA：语义文本梯度抽象），共验证了五个组合（Vanilla、+TI、+TI&AS、+TI&STGA、完整 AgentGrad）；
- **优化轨迹分析**：绘制验证集性能随 rollout 数量变化曲线，对比各方法的收敛速度与稳定性；
- **时间效率对比**：5 个基准上的优化墙钟时间对比；
- **改进率分析**：minibatch 改进率与验证改进率，对比基线与消融组件效果；
- **可迁移性实验**：在 5 个同域但未见过的目标基准（2WikiMultiHopQA、EX-FEVER、PUPA-TNB、IFEval、OlympiadBench）上检验优化后提示的泛化性；
- **定性分析**：展示隐私保护场景中语义梯度聚类与抽象过程示例。

**结论**：整体实验组数较丰富，覆盖了多种任务类型、多个模型层级，具有消融、效率、稳定性和迁移性分析，**充分性较好**。同时，各实验采用统一多智能体系统、数据划分、奖励函数与固定 rollout 预算，在相同骨干模型上对比，**公平性较有保障**。

### 6. 主要结论与发现

- AgentGrad 在**全部五个基准**上均超过所有基线（含 MIPROv2、TextGrad、GEPA）：使用 GPT-5-mini 相比无优化基线平均提升 +11.76 分，使用 Qwen3-8B 平均提升 +9.67 分；
- 在**效率方面**，AgentGrad 在全部基准上均为最快，平均 136 分钟，比次快的方法 GEPA（337 分钟）快 **2.5×**，相比 TextGrad（647 分钟）快 4.7×；
- AgentGrad 的 minibatch 改进率高于基线（0.72 vs 0.44/0.28），即更高的 rollout 利用率带来优化加速；验证改进率同样最高（0.27 vs 0.21/0.14），说明更新的提示泛化性更好；
- 优化后提示在**未见的同域目标基准**上同样表现最佳，说明其收益不只停留在源基准上；
- 消融实验表明干预目标识别（TI）、智能体级监督（AS）和语义文本梯度抽象（STGA）三个组件互补且均有效。

### 7. 优点

- **精准失效定位**：将顺序干预引入提示优化，通过实际"干预—验证"确认目标智能体，而非随机或整体更新，优化方向更可靠。
- **细粒度监督**：利用干预后的输出构造智能体级伪标签，在不依赖显式系统级损失的情况下同时提供局部化的梯度信号，显著提升更新精度。
- **语义聚合思路新颖**：不随机拼接梯度，而是按修正模式聚类并抽象成统一的广义梯度，解决梯度冲突，提升提示更新的泛化能力。
- **效率与性能兼得**：通过高质量样本级信号提升改进率，使有限 rollout 预算的使用率更高；实验同时验证了在收敛速度、峰值性能、时间成本和迁移性方面均占优。
- **广泛的验证覆盖**：模型层次（开源 vs 闭源）、任务类型（QA、核验、指令遵循、隐私、数学）和跨域泛化均被纳入评估，具有很强的说服力。

### 8. 不足与局限

- **对干预质量依赖**：顺序干预的效果依赖于 hint 的设计质量与正确输出/约束信息的可获得性；若给定的 hint 不足以指导特定智能体产出正确中间行为，部分失败样本会被当作"硬案例"搁置，虽然后续轮次会重新考察，但硬案例占比大时可能影响优化上限。
- **实验广度限制**：评估基于 5 个基准和 2 个骨干，尽管任务类型多样，但更多复杂真实应用（如代码生成、长周期规划、具身环境等）尚未覆盖；消融实验也仅在两个基准上执行。
- **Agent 数量与规模**：文中使用的 MAS 一般含数个智能体，极大规模（如几十甚至上百个智能体）场景下的干预成本与聚类抽象有效性仍有待检验。
- **未报告算力资源**：缺少对 GPU 型号、数量和总调用量的详细披露，"优化时间快 2.5×"的结论虽真实，但缺少对底层计算开销的深入对比分析。
- **应用限制**：公式中的处理主要面向任务反馈易得的监督场景；在奖励函数极稀疏、很难判断"何谓正确回答"的环境中，干预构造和语义聚类的稳定性仍需后续验证。

（完）
