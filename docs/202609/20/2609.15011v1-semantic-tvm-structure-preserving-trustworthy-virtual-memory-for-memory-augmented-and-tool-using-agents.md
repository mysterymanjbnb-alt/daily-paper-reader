---
title: "Semantic-TVM: Structure-Preserving Trustworthy Virtual Memory for Memory-Augmented and Tool-Using Agents"
title_zh: Semantic-TVM：面向记忆增强与工具使用型智能体的结构保持可信虚拟内存
authors: "Yu Li, Qikun Cai, Tao Huang, Chen Hou"
date: 2026-09-14
pdf: "https://arxiv.org/pdf/2609.15011v1"
tags: ["query:ai-agents"]
score: 8.0
evidence: 面向记忆增强与工具调用智能体的可信虚拟内存
tldr: "记忆增强与工具调用智能体在远程大模型处理检索记忆、工具动作和中间观察时会暴露精确隐私值。单向掩码虽减少直接暴露，却会移除可信执行所需值，并可能经后续观察泄漏。为此提出可信虚拟内存 TVM：在本地保留精确值状态，仅向远程模型呈现受保护视图；其中 Semantic-TVM 只替换由可信本地模型预测的敏感片段，保留周围任务上下文。在 Memory-EHR 与 Memory-RAP 上，片段级投影大幅挽回整字段替换损失的效用，DeepSeek 上任务成功率由 52.33% 升至 84.17%，且暴露低、工作流可执行。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.15011v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 876, \"height\": 685, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.15011v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 855, \"height\": 617, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.15011v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 752, \"height\": 181, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.15011v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1389, \"height\": 255, \"label\": \"Table\"}]"
motivation: 记忆增强与工具调用智能体把检索记忆、工具动作和中间观察交给远程大模型处理，导致精确隐私值暴露；单向掩码又牺牲可信执行所需值并可能经后续观察泄漏。
method: 提出可信虚拟内存 TVM，在闭环运行时本地保留精确值状态，仅向远程模型呈现受保护视图；Semantic-TVM 只替换可信本地模型预测的敏感片段，保留任务上下文。
result: "在 Memory-EHR 和 Memory-RAP 上，片段级投影挽回整字段替换损失的大部分效用；DeepSeek 任务成功率从 52.33% 升至 84.17%，暴露低且工作流可执行。"
conclusion: Semantic-TVM 表明，片段级敏感信息投影能在保持低暴露和可执行工作流的同时，显著恢复记忆增强智能体的任务效用，为可信虚拟内存提供可行路径。
---

## 摘要
记忆增强型与工具使用型智能体在远程 LLM 处理检索到的记忆、工具操作和中间观察时，会暴露精确的私有值。单向掩码限制了直接暴露，但会移除可信执行所需的值，并可能通过后续观察将其泄露。我们提出可信虚拟内存（TVM），一种闭环运行时，它将精确值状态保留在本地，同时向远程模型呈现受保护的视图。在这一单一运行时内，Rule-TVM 用本地可恢复的句柄替换整个受保护字段，而 Semantic-TVM 则仅替换由可信本地模型预测的敏感片段，从而保留周围与任务相关的上下文。在两个提供商的 Memory-EHR 和 Memory-RAP 上，片段级投影恢复了在整字段替换下损失的大部分 EHR 效用（在 DeepSeek 上任务成功率为 84.17% 对 52.33%），同时测得暴露保持较低，且工作流仍可执行。

## Abstract
Memory-augmented and tool-using agents expose exact private values when remote LLMs process retrieved memory, tool actions, and intermediate observations. One-way masking limits direct exposure but removes values needed for trusted execution and can leak them through later observations. We propose Trustworthy Virtual Memory (TVM), a closed-loop runtime that keeps exact-value state local while presenting a protected view to the remote model. Within this single runtime, Rule-TVM replaces whole protected fields with locally recoverable handles, and Semantic-TVM instead replaces only sensitive spans predicted by a trusted local model, preserving surrounding task-relevant context. On Memory-EHR and Memory-RAP across two providers, span-level projection recovers most of the EHR utility lost under whole-field replacement (Task Success 84.17% vs. 52.33% on DeepSeek) while measured exposure stays low and workflows remain executable.

---

## 论文详细总结（自动生成）

# Semantic-TVM 论文中文总结

## 1. 核心问题与整体含义
- **研究背景**：记忆增强型与工具使用型 LLM 智能体会把检索记忆、历史动作、私有记录、工具观察和中间状态序列化后发送给远程在线 LLM。若在线 LLM 由远端托管，这些请求可能暴露用户标识、记录绑定、历史搜索词等精确私有值。
- **核心问题**：传统“单向掩码”虽能减少初始请求中的直接暴露，但存在两个关键缺陷：
  - 掩码不可恢复，而可信工具执行往往需要精确标识符或参数；
  - 后续工具观察、重试请求或中间状态可能再次包含这些值，导致泄漏。
- **整体含义**：论文提出 **Trustworthy Virtual Memory（TVM，可信虚拟内存）**，在本地可信运行时中保留精确值状态，只向远程模型呈现受保护的投影视图；并通过“投影—解析—执行—重投影”的闭环，把隐私保护从单次提示扩展到完整智能体执行周期。
- **核心主张**：保护初始 prompt 不足以覆盖智能体全生命周期；需要在本地-远程之间建立可恢复、可审计、结构保持的虚拟内存层。

## 2. 方法论
### 2.1 核心思想
- **TVM 闭环运行时**：将远程可见上下文与本地精确值状态分离。远程模型只看到投影后的安全视图，本地保存恢复状态与精确绑定。
- **信任边界**：
  - 可信域：TVM、本地投影模型、恢复存储、本地执行器；
  - 不可信域：远程在线 LLM 提供商；
  - 所有发往在线 LLM 的序列化消息都被审计。
- **生命周期**：
  1. **Projection**：本地中介 `T_project` 将检索记忆 `R_t`、公共任务 `x` 和场景适配器 `A_s` 投影为在线安全视图
