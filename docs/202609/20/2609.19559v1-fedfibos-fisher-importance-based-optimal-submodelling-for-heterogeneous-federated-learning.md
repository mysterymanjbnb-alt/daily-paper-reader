---
title: "FedFIbOS: Fisher Importance based Optimal Submodelling for Heterogeneous Federated Learning"
title_zh: FedFIbOS：基于Fisher重要性的异构联邦学习最优子建模
authors: "Yasmeen Afzal, Jeremiah D. Deng, Haibo Zhang"
date: 2026-09-17
pdf: "https://arxiv.org/pdf/2609.19559v1"
tags: ["query:federated-learning"]
score: 8.0
evidence: 基于费舍尔信息选择子模型参数
tldr: "异构联邦学习需让算力不同的客户端训练容量受限子模型，但现有子模型参数选择多依赖参数幅值等启发式重要性，缺乏收敛理论支撑。FedFIbOS 从最小化子模型掩码误差出发，用 Fisher 信息构造选择准则，并证明 Fisher top-k 规则在 Fisher 主导排序条件下可解该二次掩码代理。它利用平方梯度估计对角 Fisher 分数，无需额外优化开销即可稳定选参。在 CIFAR-10/100 和 AGNews 的非独立同分布实验中，精度比 SOTA 高约 10%，异质性越强提升越明显。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.19559v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 844, \"height\": 598, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.19559v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 609, \"height\": 404, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.19559v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1373, \"height\": 591, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.19559v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 612, \"height\": 406, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.19559v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1468, \"height\": 259, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.19559v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1468, \"height\": 260, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.19559v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 737, \"height\": 346, \"label\": \"Table\"}]"
motivation: 现有异构联邦子模型选择依赖参数幅值等启发式准则，缺乏收敛理论支撑，且部分客户端参与会干扰 Fisher 分数估计。
method: FedFIbOS 以最小化掩码误差推导 Fisher 加权二次代理，采用对角 Fisher 的平方梯度估计执行 top-k 子模型参数选择。
result: "在 CIFAR-10、CIFAR-100 与 AGNews 的病态及 Dirichlet 非独立同分布设定下，精度较 SOTA 提升约 10%，异质性越强增益越显著。"
conclusion: 该工作为异构联邦子模型选择提供收敛理论依据，并给出高效稳定的 Fisher top-k 实现，在强异质性场景优势明显。
---

## 摘要
异构联邦学习要求具有不同计算能力的客户端协作训练一个全局模型，其中每个客户端训练一个受容量约束的子模型。现有方法使用启发式重要性度量来选择子模型参数——其中最突出的是参数幅值——但缺乏关于这些度量为何能够支持收敛的理论依据。我们指出了一个根本性缺口：现有参数选择准则在收敛框架中缺乏理论基础，部分客户端参与会在Fisher得分中引入额外的估计效应。我们提出 \textbf{FedFIbOS}：面向异构联邦学习的基于Fisher重要性的最优子建模，将Fisher信息用于由最小化子模型掩码误差推导出的原则性准则。我们从理论上通过一个Fisher加权的二次掩码代理来形式化子模型选择，并证明FedFIbOS所实现的原始Fisher top-$k$规则在Fisher主导排序条件下求解该代理问题。所得方法保留了底层掩码联邦优化界的收敛结构。Fisher得分通过使用平方梯度从经验对角Fisher信息中高效估计，从而实现稳定且自适应的参数选择，且不增加额外优化开销。在病理性和Dirichlet非独立同分布设置下，在CIFAR-10、CIFAR-100和AGNews上的实验表明，FedFIbOS比当前最优方法实现了 ${\approx}10\%$ 更高的准确率，且异构性越强，改进越显著。

## Abstract
Heterogeneous federated learning requires clients with diverse computational capacities to collaboratively train a global model, where each client trains a capacity-constrained submodel. Existing methods select submodel parameters using heuristic importance measures---most prominently parameter magnitude---without theoretical justification for why these measures support convergence. We identify a fundamental gap: existing parameter selection criteria lack theoretical grounding in the convergence framework, partial client participation introduces additional estimation effects in the Fisher scores. We propose \textbf{FedFIbOS}: Fisher Importance-based Optimal Submodelling for heterogeneous federated learning, using Fisher Information in a principled criterion derived from minimizing submodel masking error. %We formally establish when magnitude selection is equivalent to Fisher selection fail under non-IID heterogeneous federated learning. We theoretically formulate submodel selection through a Fisher-weighted quadratic masking surrogate and show that the raw Fisher top-$k$ rule implemented by FedFIbOS solves this surrogate under a Fisher-dominant ranking condition. The resulting method retains the convergence structure of the underlying masked federated optimization bound. Fisher scores are efficiently estimated from empirical diagonal Fisher information using squared gradients, enabling stable and adaptive parameter selection without additional optimization overhead. Experiments on CIFAR-10, CIFAR-100, and AGNews under pathological and Dirichlet non-IID settings show FedFIbOS achieves ${\approx}10\%$ higher accuracy than the state of the art, with improvements becoming more pronounced under stronger heterogeneity.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义

- **研究背景**：异构联邦学习（Heterogeneous Federated Learning）中，不同客户端算力、内存、通信预算差异巨大，需要为每个客户端分配一个由全局模型导出的容量受限子模型进行本地训练。
- **核心问题**：现有子模型参数选择方法多采用启发式重要性度量，最典型的是参数幅值（magnitude），但缺乏与收敛框架的理论联系。论文指出：参数幅值只反映全局模型参数大小，不反映客户端本地数据分布下的优化敏感性；在非独立同分布（non-IID）和部分客户端参与场景下，这种选择准则可能导致较大的掩码近似误差，从而影响收敛速度和最终精度。
- **整体含义**：论文提出 FedFIbOS，用 Fisher 信息构造子模型选择准则，将“选择哪些参数”与“最小化子模型掩码误差”直接联系起来，并为 Fisher top-k 规则提供理论最优性和收敛保证。其意义在于：为模型异构联邦学习中的子模型提取提供原则性理论依据，同时保持较低计算开销。

## 2. 方法论

- **核心思想**：客户端应保留对本地目标函数最敏感的参数，而不是仅保留幅值最大的参数。Fisher 信息衡量参数扰动对本地损失的影响，因此适合作为子模型选择的重要性分数。
- **关键定义**：
  - 参数 \(j\) 在客户端 \(n\) 的 Fisher 分数为  
    \[
    F_j^{(n)} = \mathbb{E}_{(x,y)\sim P_n}\left[\left(\frac{\partial L}{\partial \theta_j}\right)^2\right].
    \]
  - 实际使用经验对角 Fisher，通过本地训练中已计算的平方梯度平均估计：
    \[
    \hat f_j^{(n)} = \frac{1}{KB}\sum_{k=0}^{K-1}\sum_{b=1}^{B}\left(\frac{\partial L}{\partial \theta_j}\right)^2.
    \]
  - 服务器维护每个客户端的 Fisher 指数移动平均（EMA）：
    \[
    \bar f_j^{(n,t+1)} = \alpha \bar f_j^{(n,t)} + (1-\alpha)\hat f_j^{(n)}.
    \]
- **子模型选择问题（SSP）**：
  - 论文将选择依赖的掩码误差近似为 Fisher 加权二次代理：
    \[
    \epsilon_t^{(n)}(S)=\sum_{j\notin S}(F_{t,j}^{(n)})^2\tilde x_{t,j}^2.
    \]
  - 最优选择为 Fisher 加权 top-k：
    \[
    S^\star = \operatorname{TopK}_{k_n}\left((F_{t,j}^{(n)})^2\tilde x_{t,j}^2\right).
    \]
  - 在“Fisher 主导排序”假设下，原始 Fisher 分数排序与上述加权排序一致，因此 FedFIbOS 直接使用 Fisher top-k 选择参数。
- **算法流程**：
  1. 服务器初始化 EMA Fisher 分数为参数幅值 \(|\tilde x_0|\)；
  2. 每轮采样参与客户端；
  3. 对每个参与客户端，根据其 EMA Fisher 分数选出 top-\(\lceil\gamma_n d\rceil\) 参数，生成二值掩码；
  4. 发送掩码后的全局子模型给客户端；
  5. 客户端执行 K 步本地 SGD，同时累积平方梯度以估计 Fisher；
  6. 客户端上传参数更新和 Fisher 估计；
  7. 服务器聚合更新，更新参与客户端的 EMA Fisher，未参与客户端的 EMA 保持不变。
- **理论要点**：
  - 定理 1：在 Fisher 主导排序条件下，原始 Fisher top-k 规则可解 Fisher 加权二次 SSP；
  - 命题 1：Fisher 选择的二次掩码误差不大于幅值选择，且在集合不同且分数可区分时严格更小；
  - 定理 2：Fisher 引导选择保留 \(O(1/\sqrt{T})\) 收敛率，且选择相关项 \(\delta_{F,t}^2 \le \delta_{M,t}^2\)；
  - 命题 2：部分参与下，EMA 初始化偏差随参与轮数几何衰减。

## 3. 实验设计

- **数据集与模型**：
  - CIFAR-10、CIFAR-100：ResNet-18；
  - AGNews：预训练 RoBERTa-Base。
- **数据异质性**：
  - 病态划分（pathological）：
    - CIFAR-10：低异质每客户端 5 类，高异质每客户端 2 类；
    - CIFAR-100：低异质每客户端 50 类，高异质每客户端 20 类；
    - AGNews 因只有 4 类，未做病态划分。
  - Dirichlet 划分：
    - CIFAR-10/100：\(\alpha=0.3\)；
    - AGNews：\(\alpha=1.0\)。
- **模型异质性**：
  - 客户端容量比例：1.0、0.5、0.25、0.125、0.0625；
  - 两种容量分布：
    - 均匀分布：a20-b20-c20-d20-e20；
    - 非均匀分布：a10-b10-c30-d30-e20，80% 客户端为资源受限设备。
- **训练设置**：
  - CIFAR-10/100：100 客户端，每轮参与率 10%，800 通信轮；
  - AGNews：200 客户端，每轮参与率 10%，300 通信轮。
- **对比方法**：
  - HeteroFL、FedRolex、FedBRB、FIARSE，其中 FIARSE 是与 FedFIbOS 最接近的基于重要性的方法。
- **评价指标**：
  - Local Accuracy 与 Global Accuracy；
  - 额外分析 Fisher 选择与幅值选择的 Jaccard 相似度；
  - Fisher 分数变异系数 \(CV_F\) 与幅值分数变异的对比。

## 4. 资源与算力

- 论文**未明确说明**使用的 GPU 型号、数量、显存、训练总时长、能耗或具体硬件资源。
- 文中只报告了通信轮数、客户端数量、参与率、本地步数、批量大小等训练超参数，但未给出实际计算资源开销。
- 因此无法从论文中判断实验的算力规模、可复现成本以及不同方法在计算效率上的真实差异。

## 5. 实验数量与充分性

- **实验组数概览**：
  -
